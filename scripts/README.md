# Scripts

Build, lint, export, and verification helpers.

## Inventory

| Script | Purpose |
|---|---|
| `export-excalidraw.sh` | Export `.excalidraw` source → `.svg` (placeholder; wire to tool) |
| (Phase 2) `lint-quizzes.sh` | Validate quiz YAML schema |
| (Phase 2) `validate-calm.sh` | Validate every `.calm.json` in `code-examples/` |
| (Phase 2) `test-lab.sh` | End-to-end lab verification |
| (Phase 2) `build-slides.sh` | Generate slide deck from text + illustrations |
