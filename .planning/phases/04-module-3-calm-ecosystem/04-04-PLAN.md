---
phase: 04-module-3-calm-ecosystem
plan: "04"
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - labs/lab-03-cicd-gate/LAB.md
  - labs/lab-03-cicd-gate/starter/.github/workflows/calm-gate.yml
  - labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json
  - labs/lab-03-cicd-gate/starter/architectures/broken-api.calm.json
  - labs/lab-03-cicd-gate/starter/patterns/secure-api-service.pattern.json
  - labs/lab-03-cicd-gate/starter/README.md
  - labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml
  - quizzes/module-03-ecosystem.yaml
autonomous: true
requirements: [LAB-03, QUIZ-04]
tags: [module-03, calm-ecosystem, lab, quiz, cicd]

must_haves:
  truths:
    - "Learner can fork the starter, complete the GitHub Actions stub, and observe the gate pass on valid architecture and block on invalid"
    - "LAB.md follows Learnforge-compatible YAML frontmatter with all 5 step checks"
    - "Module 3 quiz covers all 7 chapters with 16-20 questions and passes bash scripts/lint-quizzes.sh"
    - "Solution workflow YAML passes yamllint and contains all required calm validate steps"
  artifacts:
    - path: "labs/lab-03-cicd-gate/LAB.md"
      provides: "Learnforge-compatible lab instructions with YAML frontmatter, 5 steps, check kinds"
      contains: "id: lab-03-cicd-gate"
    - path: "labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json"
      provides: "Valid architecture for Lab 3 (passes calm validate)"
    - path: "labs/lab-03-cicd-gate/starter/architectures/broken-api.calm.json"
      provides: "Invalid architecture for Lab 3 (fails calm validate — missing required field)"
    - path: "labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml"
      provides: "Complete working GitHub Actions workflow — the solution to the lab stub"
    - path: "quizzes/module-03-ecosystem.yaml"
      provides: "Module 3 quiz — 16-20 questions covering all 7 chapters"
      contains: "module: 3"
  key_links:
    - from: "labs/lab-03-cicd-gate/LAB.md"
      to: "labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json"
      via: "step-2 check command"
      pattern: "secure-api.calm.json"
    - from: "labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml"
      to: "labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json"
      via: "calm validate -a architectures/secure-api.calm.json"
      pattern: "secure-api.calm.json"
---

<objective>
Deliver Lab 3 (CI/CD gate) and the Module 3 quiz — the two hands-on/assessment artifacts for Phase 4. Lab 3 runs in Wave 2 in parallel with Plans 02 and 03 (text chapters) because the pattern file (`secure-api-service.pattern.json`) was created in Plan 01 (Wave 1). The quiz can be authored from RESEARCH.md quiz design without waiting for the slide deck.

Purpose: Lab 3 is the capstone hands-on exercise for Module 3 — learners implement the architecture gate they read about in Chapter 3.7. The quiz verifies retention across all 7 chapters before the slide deck (Wave 3) is produced.

Output: Lab 3 directory (LAB.md + starter/ + solution/), Module 3 quiz YAML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-module-3-calm-ecosystem/04-RESEARCH.md
@CLAUDE.md
@docs-meta/LAB-FORMAT.md
@docs-meta/QUIZ-FORMAT.md

# Pattern file from Wave 1 (used in Lab 3 starter/ directory)
@code-examples/module-03-calm-ecosystem/secure-api-service.pattern.json

# Prior lab for structure reference
@labs/lab-02-conference-signup/LAB.md

# Prior quiz for structure reference
@quizzes/module-02-calm-fundamentals.yaml

<interfaces>
<!-- Lab structure from LAB-FORMAT.md -->
LAB.md YAML frontmatter required fields:
  id, title, module, chapter, estimated_minutes, difficulty (1-3),
  prerequisites, requires_docker, workspace (cumulative, base_dir, creates),
  objectives, steps (each with id, title, check {kind, ...}, optional hints),
  resources

Step check kinds:
  - command_regex: {command: "...", pattern: "regex"}
  - exit_code: {command: "...", expected: 0}
  - file_state: {path: "...", exists: true} or {path: "...", content_regex: "regex"}
  - ai_judge: {prompt: "..."}

<!-- Lab 3 step checks from RESEARCH.md -->
5 steps verified in RESEARCH.md "Lab 3 steps" section:
Step 1: file_state — .github/workflows/calm-gate.yml exists
Step 2: command_regex — npx @finos/calm-cli validate -a architectures/secure-api.calm.json -f pretty → pattern: "0 errors"
Step 3: file_state — .github/workflows/calm-gate.yml content_regex: "calm validate"
Step 4: ai_judge — GitHub Actions run shows all steps green for valid architecture push
Step 5: command_regex — npx @finos/calm-cli validate -a architectures/broken-api.calm.json -f pretty → pattern: "0 errors"

<!-- Exact GitHub Actions solution YAML from RESEARCH.md — use verbatim -->
The solution workflow YAML is the same as in Plan 03 Chapter 3.7 (both derived from RESEARCH.md).
It has: checkout@v4, setup-node@v4 (node: '22'), npm install -g @finos/calm-cli,
calm validate -a architectures/secure-api.calm.json -f pretty,
calm validate -a architectures/secure-api.calm.json -p patterns/secure-api-service.pattern.json -f pretty,
calm validate -a architectures/secure-api.calm.json -p patterns/secure-api-service.pattern.json -f junit -o calm-validation-results.xml,
upload-artifact@v4 (if: always(), path: calm-validation-results.xml)

<!-- Quiz design from RESEARCH.md "Quiz Design" section — 16-20 questions, 7 chapters -->
Target: 16-20 questions, mix of multiple_choice, code_completion, short_answer
File: quizzes/module-03-ecosystem.yaml
Chapter groups:
  - 3.1 CLI: 3 questions
  - 3.2 Studio: 2 questions
  - 3.3 Hub: 3 questions
  - 3.4 Server: 2 questions
  - 3.5 VSCode: 2 questions
  - 3.6 Patterns: 3 questions
  - 3.7 CI/CD: 3 questions
Total: 18 questions

Sample questions from RESEARCH.md — use VERBATIM for q3.1.1 and q3.6.1 (they are verified content):
q3.1.1: multiple_choice — which flag produces JUnit output? Correct: -f junit
q3.6.1: code_completion — complete calm generate command
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lab 3 — LAB.md + starter + solution</name>
  <files>
    labs/lab-03-cicd-gate/LAB.md
    labs/lab-03-cicd-gate/starter/.github/workflows/calm-gate.yml
    labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json
    labs/lab-03-cicd-gate/starter/architectures/broken-api.calm.json
    labs/lab-03-cicd-gate/starter/patterns/secure-api-service.pattern.json
    labs/lab-03-cicd-gate/starter/README.md
    labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml
  </files>
  <read_first>
    - docs-meta/LAB-FORMAT.md (full — YAML frontmatter schema, step check kinds, authoring rules)
    - .planning/phases/04-module-3-calm-ecosystem/04-RESEARCH.md (section "Lab 3: CI/CD Gate — Design" — entire section including starter structure, step checks, solution YAML, local fallback)
    - labs/lab-02-conference-signup/LAB.md (full — LAB.md structure and YAML frontmatter pattern to mirror)
    - code-examples/module-03-calm-ecosystem/secure-api-service.pattern.json (created in Plan 01 — copy into starter/patterns/)
    - code-examples/module-03-calm-ecosystem/secure-api-generated.calm.json (valid architecture — use as base for starter/architectures/secure-api.calm.json)
  </read_first>
  <action>
    Create the entire Lab 3 directory structure: LAB.md (Learnforge-compatible), starter/ (forkable repo scaffold), and solution/ (reference solution). Lab 3 is a CLI + GitHub Actions lab — Hub is a STRETCH GOAL ONLY, not a required step.

    **File 1: `labs/lab-03-cicd-gate/LAB.md`**

    Write the complete Learnforge-compatible LAB.md. YAML frontmatter first, then the Markdown body.

    YAML frontmatter:
    ```yaml
    ---
    id: lab-03-cicd-gate
    title: Build a CALM Architecture Gate in GitHub Actions
    module: 3
    chapter: 3.7
    estimated_minutes: 35
    difficulty: 2
    prerequisites:
      - lab-02-conference-signup

    requires_docker: false

    workspace:
      cumulative: false
      base_dir: lab-03-cicd-gate
      creates:
        - starter/.github/workflows/calm-gate.yml
        - starter/architectures/secure-api.calm.json

    objectives:
      - Fork the starter repo and clone it locally
      - Verify that the valid architecture (secure-api.calm.json) passes calm validate
      - Complete the stub GitHub Actions workflow to add calm validate
      - Push the completed workflow and observe the CI gate pass
      - Fix the broken architecture (broken-api.calm.json) and push again

    steps:
      - id: step-1
        title: Fork and clone the starter
        check:
          kind: file_state
          path: .github/workflows/calm-gate.yml
          exists: true
        hints:
          - "Clone the lab starter: cd labs/lab-03-cicd-gate/starter"
          - "The starter/ directory contains the pre-built repo scaffold"
          - "In a real lab, you would fork this as a GitHub template repo"

      - id: step-2
        title: Verify the valid architecture passes calm validate
        check:
          kind: command_regex
          command: "npx @finos/calm-cli validate -a architectures/secure-api.calm.json -f pretty"
          pattern: "0 errors"
        hints:
          - "Run: npx @finos/calm-cli validate -a architectures/secure-api.calm.json -f pretty"
          - "You should see 0 errors — secure-api.calm.json is intentionally valid"
          - "Note: broken-api.calm.json will show errors — that is expected at this step"

      - id: step-3
        title: Add calm validate step to the GitHub Actions workflow stub
        check:
          kind: file_state
          path: .github/workflows/calm-gate.yml
          content_regex: "calm validate"
        hints:
          - "Open .github/workflows/calm-gate.yml — it has a stub with a TODO comment"
          - "Replace the TODO with the calm validate step from Chapter 3.7"
          - "Minimum: calm validate -a architectures/secure-api.calm.json -f pretty"
          - "Full solution: add the pattern validate step and JUnit output step too"

      - id: step-4
        title: Push the workflow and observe the gate pass for the valid architecture
        check:
          kind: ai_judge
          prompt: "The learner has pushed their completed calm-gate.yml to a GitHub repository. Their GitHub Actions run (or local 'act' run) shows all steps green for the valid secure-api.calm.json architecture. Evidence: screenshot, CI log, or 'act' output showing 'Job validate-architecture succeeded'."
        hints:
          - "Push to your forked GitHub repo: git push origin main"
          - "If you don't want to push to GitHub, use 'act' locally: brew install act && act push"
          - "Navigate to Actions tab in your GitHub repo to see the workflow run"
          - "All steps should be green for the valid secure-api.calm.json"

      - id: step-5
        title: Fix broken-api.calm.json so it passes calm validate
        check:
          kind: command_regex
          command: "npx @finos/calm-cli validate -a architectures/broken-api.calm.json -f pretty"
          pattern: "0 errors"
        hints:
          - "First, inspect what is wrong: npx @finos/calm-cli validate -a architectures/broken-api.calm.json -f pretty"
          - "The error will tell you which required field is missing"
          - "Add the missing field to the broken-api.calm.json file"
          - "Re-run validate until you see 0 errors"

    resources:
      - title: "Chapter 3.7: CI/CD Integration"
        url: "../../content/module-03-calm-ecosystem/cicd-integration.mdx"
      - title: "CALM CLI reference"
        url: "../../content/module-03-calm-ecosystem/cli-toolbox.mdx"
      - title: "GitHub Actions documentation"
        url: "https://docs.github.com/en/actions"
    ---
    ```

    Markdown body sections after the frontmatter:

    ## Goal
    Set up a complete CALM architecture validation gate in GitHub Actions. By the end of this lab, any PR to your repository will automatically validate the CALM architecture — passing architecture gets merged, failing architecture gets blocked.

    ## Setup
    No Docker required. You need: a GitHub account (free), Node.js 22+, and the CALM CLI (installed automatically in the GitHub Actions step).

    For a fully local workflow without GitHub, install `act`:
    ```bash
    brew install act    # macOS
    # or: https://github.com/nektos/act
    act push            # runs the workflow locally
    ```

    ## Walkthrough

    ### Step 1: Set up the starter
    The starter directory contains a pre-built repo scaffold. In a production setup, you would fork this as a GitHub template repository. For this lab, work directly in `labs/lab-03-cicd-gate/starter/`:

    ```bash
    cd labs/lab-03-cicd-gate/starter
    git init && git add . && git commit -m "initial commit"
    ```

    If you have a GitHub account, create a new repo and push:
    ```bash
    git remote add origin https://github.com/<your-username>/calm-lab-03
    git push -u origin main
    ```

    ### Step 2: Verify the valid architecture
    The starter contains two architecture files. The valid one should pass `calm validate`:

    ```bash
    npx @finos/calm-cli validate -a architectures/secure-api.calm.json -f pretty
    # Expected: 0 errors, 0 warnings
    ```

    The broken one should fail:
    ```bash
    npx @finos/calm-cli validate -a architectures/broken-api.calm.json -f pretty
    # Expected: errors about missing required fields
    ```

    ### Step 3: Complete the workflow stub
    Open `.github/workflows/calm-gate.yml`. You will see a stub with a `# TODO: add calm validate step here` comment. Replace it with the validation steps from Chapter 3.7. The minimum is:

    ```yaml
    - name: Validate architecture against schema
      run: calm validate -a architectures/secure-api.calm.json -f pretty
    ```

    The full solution (in `solution/`) adds pattern validation and JUnit output too.

    ### Step 4: Push and observe the gate
    Commit your workflow update and push:
    ```bash
    git add .github/workflows/calm-gate.yml
    git commit -m "feat: add CALM architecture gate"
    git push origin main
    ```

    In your GitHub repo, navigate to the **Actions** tab. You should see the "CALM Architecture Gate" workflow run. All steps should be green.

    If using `act` locally: `act push` — watch the output for "Job validate-architecture succeeded."

    ### Step 5: Fix the broken architecture
    The broken-api.calm.json file is intentionally missing a required field. Inspect what's wrong:

    ```bash
    npx @finos/calm-cli validate -a architectures/broken-api.calm.json -f pretty
    ```

    Add the missing field, re-validate, and push again. The gate should now pass for both architectures.

    ## Stretch goals
    - Add `calm diff --exit-code` between the two architecture versions (v1 vs v2) to the workflow
    - Add a Hub publish step: install `calm hub push architecture` and push to a local Hub Docker instance
    - Add the JUnit test report upload step and verify it appears in the Actions UI

    ## Solution
    See `solution/` for the complete working workflow. Try the lab without peeking first.

    **File 2: `labs/lab-03-cicd-gate/starter/.github/workflows/calm-gate.yml`**

    The STUB workflow — learner fills in the TODO:

    ```yaml
    name: CALM Architecture Gate

    on:
      push:
        branches: [main]
      pull_request:
        branches: [main]

    jobs:
      validate-architecture:
        name: Validate CALM Architecture
        runs-on: ubuntu-latest

        steps:
          - name: Checkout
            uses: actions/checkout@v4

          - name: Setup Node.js
            uses: actions/setup-node@v4
            with:
              node-version: '22'

          - name: Install CALM CLI
            run: npm install -g @finos/calm-cli

          # TODO: add calm validate step here
          # Hint: npx @finos/calm-cli validate -a architectures/secure-api.calm.json -f pretty
    ```

    **File 3: `labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json`**

    A valid CALM 1.2 architecture (same content as `code-examples/module-03-calm-ecosystem/secure-api-generated.calm.json` from Plan 01 — copy that file's exact content here). Must pass `npx @finos/calm-cli validate -a architectures/secure-api.calm.json`.

    **File 4: `labs/lab-03-cicd-gate/starter/architectures/broken-api.calm.json`**

    An INVALID CALM architecture that fails validation. Create this as a deliberately broken version of secure-api.calm.json:
    - Use the same $schema, unique-id, name, description
    - Include the nodes array with 3 nodes BUT omit the `description` field from one node (required field per CALM 1.2 schema)
    - Include the relationships array
    - When `npx @finos/calm-cli validate -a broken-api.calm.json` is run, it MUST produce errors
    - When the learner ADDS the missing `description` field, the file MUST then pass validate (step 5 of the lab)

    The broken field: remove `description` from the database node. This is the simplest, most instructive breakage.

    **File 5: `labs/lab-03-cicd-gate/starter/patterns/secure-api-service.pattern.json`**

    Copy the content of `code-examples/module-03-calm-ecosystem/secure-api-service.pattern.json` verbatim.

    **File 6: `labs/lab-03-cicd-gate/starter/README.md`**

    One-paragraph README for the starter directory:
    "# Lab 03: CALM CI/CD Gate — Starter\nThis is the starter scaffold for Lab 3 of the CALM Academy. Fork this repo, add a `calm validate` step to `.github/workflows/calm-gate.yml`, and watch the architecture gate work. See `labs/lab-03-cicd-gate/LAB.md` for full instructions."

    **File 7: `labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml`**

    The COMPLETE working workflow — all steps from RESEARCH.md "Lab 3" section, verbatim:
    name, on, jobs, validate-architecture, runs-on: ubuntu-latest, steps: checkout@v4, setup-node@v4 (node-version: '22'), npm install -g @finos/calm-cli, calm validate -a architectures/secure-api.calm.json -f pretty, calm validate -a architectures/secure-api.calm.json -p patterns/secure-api-service.pattern.json -f pretty, calm validate JUnit output -f junit -o calm-validation-results.xml, upload-artifact@v4 (if: always(), name: calm-validation-results, path: calm-validation-results.xml).

    After creating all files, verify:
    - `npx @finos/calm-cli validate -a labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json` exits 0
    - `npx @finos/calm-cli validate -a labs/lab-03-cicd-gate/starter/architectures/broken-api.calm.json` exits NON-ZERO (confirms the breakage works)
  </action>
  <verify>
    <automated>npx @finos/calm-cli validate -a labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json -f pretty &amp;&amp; npx @finos/calm-cli validate -a labs/lab-03-cicd-gate/starter/architectures/broken-api.calm.json -f pretty &amp;&amp; exit 1 || true &amp;&amp; test -f labs/lab-03-cicd-gate/LAB.md &amp;&amp; grep -q 'id: lab-03-cicd-gate' labs/lab-03-cicd-gate/LAB.md &amp;&amp; grep -q 'step-1' labs/lab-03-cicd-gate/LAB.md &amp;&amp; grep -q 'step-5' labs/lab-03-cicd-gate/LAB.md &amp;&amp; grep -q 'calm validate' labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml &amp;&amp; grep -q 'upload-artifact' labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml</automated>
  </verify>
  <acceptance_criteria>
    - `labs/lab-03-cicd-gate/LAB.md` exists with valid YAML frontmatter containing: id: lab-03-cicd-gate, module: 3, chapter: 3.7, 5 step entries (step-1 through step-5)
    - Each step has id, title, check with kind field; kinds used are: file_state, command_regex, ai_judge
    - `labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json` passes `npx @finos/calm-cli validate -a` with 0 errors
    - `labs/lab-03-cicd-gate/starter/architectures/broken-api.calm.json` FAILS `npx @finos/calm-cli validate -a` with at least 1 error (the intentional breakage)
    - `labs/lab-03-cicd-gate/starter/patterns/secure-api-service.pattern.json` exists (copy of code-examples version)
    - `labs/lab-03-cicd-gate/starter/.github/workflows/calm-gate.yml` exists and contains the TODO comment stub
    - `labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml` exists and contains both `calm validate` commands AND `upload-artifact`
    - Solution workflow contains `actions/checkout@v4`, `actions/setup-node@v4`, `upload-artifact@v4`
    - LAB.md `## Walkthrough` section explains `act` as local fallback (from RESEARCH.md risk mitigation)
    - LAB.md `## Stretch goals` section mentions Hub publish as optional
    - requires_docker: false in YAML frontmatter (Hub is NOT required for core lab)
  </acceptance_criteria>
  <done>
    Lab 3 directory complete — LAB.md with Learnforge-compatible YAML, starter/ with valid and broken architectures, solution/ with complete working workflow. Secure architecture validates; broken architecture intentionally fails.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Module 3 quiz — quizzes/module-03-ecosystem.yaml</name>
  <files>
    quizzes/module-03-ecosystem.yaml
  </files>
  <read_first>
    - docs-meta/QUIZ-FORMAT.md (full — schema, question types, authoring rules)
    - .planning/phases/04-module-3-calm-ecosystem/04-RESEARCH.md (section "Quiz Design (QUIZ-04: Module 3 Ecosystem Quiz)" — exact question scope per chapter and two sample questions to use verbatim)
    - quizzes/module-02-calm-fundamentals.yaml (first 80 lines — YAML structure and chapter grouping pattern to mirror)
    - CURRICULUM.md (Module 3 chapters 3.1-3.7 — teaching concepts to verify)
  </read_first>
  <action>
    Author `quizzes/module-03-ecosystem.yaml` with 18 questions (2-3 per chapter, 7 chapters). Mirror the QUIZ-FORMAT.md schema exactly. Mix question types: multiple_choice, code_completion, short_answer.

    File structure:
    ```yaml
    module: 3
    title: "Module 3: The CALM Ecosystem"

    chapters:
      - chapter: 3.1
        slug: cli-toolbox
        title: "CALM CLI: Your architecture toolbox"
        questions: [3 questions]
      - chapter: 3.2
        slug: calm-studio-visual-design
        title: "CALM Studio: visual-first architecture design"
        questions: [2 questions]
      - chapter: 3.3
        slug: calm-hub-architecture-registry
        title: "CALM Hub: the architecture registry"
        questions: [3 questions]
      - chapter: 3.4
        slug: calm-server-validation-service
        title: "CALM Server: validation as a service"
        questions: [2 questions]
      - chapter: 3.5
        slug: vscode-extension
        title: "VSCode extension: CALM in your editor"
        questions: [2 questions]
      - chapter: 3.6
        slug: patterns-and-standards
        title: "Patterns and standards: the blueprint system"
        questions: [3 questions]
      - chapter: 3.7
        slug: cicd-integration
        title: "CI/CD integration: architecture gates"
        questions: [3 questions]
    ```

    **Chapter 3.1 — 3 questions:**

    q3.1.1 (multiple_choice — use verbatim from RESEARCH.md sample):
    id: q3.1.1, Which CALM CLI flag produces output suitable for CI test dashboards?
    Options: -f pretty (wrong — human terminal output), -f json (wrong — machine-readable but not JUnit XML), -f junit (CORRECT — JUnit XML for GitHub Actions/Jenkins), --format xml (wrong — not a valid flag)
    reference_section: "../content/module-03-calm-ecosystem/cli-toolbox.mdx#ci-integration"

    q3.1.2 (multiple_choice):
    id: q3.1.2, What is the difference between `calm validate -a arch.calm.json` and `calm validate -a arch.calm.json -p pattern.json`?
    Options:
    - They are identical (wrong — -p adds pattern conformance check)
    - The first checks schema shape only; the second additionally checks pattern conformance (CORRECT)
    - The first validates the architecture; the second validates the pattern (wrong — both operate on the architecture)
    - The -p flag is required for all validate calls (wrong — -p is optional, adds pattern conformance)
    reference_section: "../content/module-03-calm-ecosystem/cli-toolbox.mdx#the-validate-command"

    q3.1.3 (short_answer):
    id: q3.1.3, Name the calm CLI subcommand used to compare two CALM documents and output a human-readable summary of what changed.
    accepted_answers: ["calm diff", "diff", "calm diff -f summary"]
    reference_section: "../content/module-03-calm-ecosystem/cli-toolbox.mdx#the-diff-command"

    **Chapter 3.2 — 2 questions:**

    q3.2.1 (multiple_choice):
    id: q3.2.1, What is the primary URL to access CALM Studio without any local installation?
    Options: https://calm.finos.org/studio (wrong), https://studio.calm.finos.org (CORRECT), npx @calmstudio/mcp (wrong — this is the MCP server, not Studio web), localhost:5173 (wrong — local dev only)
    reference_section: "../content/module-03-calm-ecosystem/calm-studio-visual-design.mdx#accessing-studio"

    q3.2.2 (multiple_choice):
    id: q3.2.2, Which description best explains CALM Studio's "bidirectional sync"?
    Options:
    - Canvas changes automatically push to CALM Hub (wrong — Hub is separate)
    - Editing the diagram canvas updates the JSON panel, and editing the JSON panel updates the canvas (CORRECT)
    - Two Studio windows can sync their state in real time (wrong — unrelated feature)
    - The Preview Panel in VSCode syncs with Studio web (wrong — independent tools)
    reference_section: "../content/module-03-calm-ecosystem/calm-studio-visual-design.mdx#bidirectional-sync"

    **Chapter 3.3 — 3 questions:**

    q3.3.1 (multiple_choice):
    id: q3.3.1, What storage mode does the CALM Hub docker-compose quickstart use?
    Options: MongoDB with authentication (wrong), MongoDB without authentication — no-auth profile (CORRECT), NitriteDB embedded (wrong — separate mode, not docker-compose default), PostgreSQL (wrong — not supported)
    reference_section: "../content/module-03-calm-ecosystem/calm-hub-architecture-registry.mdx#hub-architecture"

    q3.3.2 (short_answer):
    id: q3.3.2, Write the `calm hub` CLI command to list all architectures in a Hub instance running at http://localhost:8080.
    accepted_answers: ["calm hub list architectures --calm-hub-url http://localhost:8080", "calm hub list architectures", "npx @finos/calm-cli hub list architectures --calm-hub-url http://localhost:8080"]
    reference_section: "../content/module-03-calm-ecosystem/calm-hub-architecture-registry.mdx#cli-hub-commands"

    q3.3.3 (multiple_choice):
    id: q3.3.3, Why does the CALM Hub return 401 on all requests when started via Maven without any profile flag?
    Options:
    - Hub requires a license key by default (wrong)
    - The default Maven profile is "secure" — all requests require authentication (CORRECT)
    - Maven doesn't support Hub startup (wrong)
    - The no-auth profile must be manually disabled (wrong — it must be manually enabled)
    reference_section: "../content/module-03-calm-ecosystem/calm-hub-architecture-registry.mdx#auth-profiles"

    **Chapter 3.4 — 2 questions:**

    q3.4.1 (multiple_choice):
    id: q3.4.1, What HTTP endpoint does CALM Server expose for architecture validation?
    Options: GET /calm/architectures (wrong), POST /calm/validate (CORRECT), PUT /calm/schema (wrong), GET /calm/validate (wrong — must be POST)
    reference_section: "../content/module-03-calm-ecosystem/calm-server-validation-service.mdx#the-validate-endpoint"

    q3.4.2 (multiple_choice):
    id: q3.4.2, A platform team wants to enforce that all teams validate their architectures against the same schema version without requiring CLI installation on every developer machine. Which tool is most appropriate?
    Options: CALM Studio (wrong — visual tool, not a platform validation service), CALM Server (CORRECT — exposes validation as HTTP endpoint; no per-team CLI install), calm validate CLI per-developer (wrong — requires CLI install on every machine), CALM Hub only (wrong — Hub is a registry, not a validation service)
    reference_section: "../content/module-03-calm-ecosystem/calm-server-validation-service.mdx#cli-vs-server"

    **Chapter 3.5 — 2 questions:**

    q3.5.1 (multiple_choice):
    id: q3.5.1, What is the official name and publisher of the CALM VSCode extension?
    Options: "CALM Validator" by FINOS (wrong name), "CALM Tools" by FINOS (CORRECT), "Architecture as Code" by FINOS (wrong name), "CALM Studio" by FINOS (wrong — Studio is the web app)
    reference_section: "../content/module-03-calm-ecosystem/vscode-extension.mdx#installing-the-extension"

    q3.5.2 (short_answer):
    id: q3.5.2, The CALM VSCode extension validates your architecture automatically without network access. What enables this offline validation capability?
    accepted_answers: ["bundled schemas", "bundled CALM schemas", "built-in schemas", "the extension includes bundled CALM 1.2 schemas"]
    reference_section: "../content/module-03-calm-ecosystem/vscode-extension.mdx#real-time-validation"

    **Chapter 3.6 — 3 questions:**

    q3.6.1 (code_completion — use verbatim from RESEARCH.md sample):
    id: q3.6.1, Complete the command: npx @finos/calm-cli <BLANK> -p secure-api-service.pattern.json -o my-api.calm.json
    accepted_answers: ["generate"]
    reference_section: "../content/module-03-calm-ecosystem/patterns-and-standards.mdx#the-calm-generate-workflow"

    q3.6.2 (multiple_choice):
    id: q3.6.2, Which command validates an architecture's conformance against a specific pattern (in addition to schema validity)?
    Options: calm validate -a arch.calm.json (wrong — schema only), calm validate -a arch.calm.json -p pattern.json (CORRECT — both schema and pattern conformance), calm generate -p pattern.json (wrong — generate creates, not validates), calm diff -a arch.calm.json -b pattern.json (wrong — diff compares two architectures)
    reference_section: "../content/module-03-calm-ecosystem/patterns-and-standards.mdx#validating-conformance-against-a-pattern"

    q3.6.3 (short_answer):
    id: q3.6.3, In one sentence, explain the relationship between a CALM pattern file and a CALM architecture file.
    accepted_answers: ["A pattern is a template defining required nodes and relationships; an architecture is a conforming instance of the pattern", "Pattern is the template; architecture is the instance that conforms to it", "A pattern defines what must exist; an architecture is built to satisfy the pattern"]
    reference_section: "../content/module-03-calm-ecosystem/patterns-and-standards.mdx#what-a-pattern-is"

    **Chapter 3.7 — 3 questions:**

    q3.7.1 (multiple_choice):
    id: q3.7.1, In a GitHub Actions workflow using `calm diff --exit-code`, when does the step exit with a non-zero code?
    Options:
    - Only when the architecture has schema errors (wrong — that is calm validate's job)
    - Whenever changes are detected between the two architecture versions (CORRECT)
    - Only when nodes are added to the architecture (wrong — any change triggers it)
    - When the pattern conformance check fails (wrong — diff does not check patterns)
    reference_section: "../content/module-03-calm-ecosystem/cicd-integration.mdx#calm-diff-as-pr-review-automation"

    q3.7.2 (multiple_choice):
    id: q3.7.2, Why should schema validation and pattern validation run as SEPARATE steps in a CI pipeline?
    Options:
    - They are identical operations that must run twice (wrong)
    - Different steps produce clearer error messages — schema errors vs pattern conformance errors help engineers understand what to fix (CORRECT)
    - Pattern validation requires a separate CLI installation (wrong — same CLI)
    - Running them together causes a timeout (wrong)
    reference_section: "../content/module-03-calm-ecosystem/cicd-integration.mdx#two-validation-levels-in-one-pipeline"

    q3.7.3 (short_answer):
    id: q3.7.3, In a Hub-integrated CI pipeline, what happens to the architecture artifact immediately after a PR is merged to main?
    accepted_answers: ["calm hub push publishes the new architecture version to Hub", "the architecture is published to CALM Hub", "calm hub push architecture is run, storing a new version in Hub", "a new version is pushed to CALM Hub via calm hub push"]
    reference_section: "../content/module-03-calm-ecosystem/cicd-integration.mdx#hub-integrated-pipeline"

    After authoring, run the quiz linter: `bash scripts/lint-quizzes.sh`
    The linter checks: IDs match qNN.N.N format, multiple_choice has exactly one correct: true, all options have explanation, all questions have reference_section, no duplicate IDs.
    Fix any lint errors before marking task done.

    Every wrong option in every multiple_choice question MUST have an explanation that turns the mistake into a learning moment. Every correct option MUST have an explanation that reinforces the concept.
  </action>
  <verify>
    <automated>test -f quizzes/module-03-ecosystem.yaml &amp;&amp; grep -q 'module: 3' quizzes/module-03-ecosystem.yaml &amp;&amp; grep -c 'id: q3\.' quizzes/module-03-ecosystem.yaml | awk '$1 >= 16 {exit 0} {exit 1}' &amp;&amp; bash scripts/lint-quizzes.sh 2>&amp;1 | grep -v '^$' | grep -v 'Checking' | grep -v 'OK' | head -20; [ "$(bash scripts/lint-quizzes.sh; echo $?)" != "1" ] || { echo "FAIL: lint-quizzes.sh found errors"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `quizzes/module-03-ecosystem.yaml` exists with `module: 3` and `title: "Module 3: The CALM Ecosystem"`
    - File has 7 chapter sections (3.1 through 3.7) each with a `questions:` array
    - Total question count is between 16 and 20 (inclusive) — count entries starting with `id: q3.`
    - Question IDs follow the format q3.N.N (e.g., q3.1.1, q3.6.2)
    - Every `multiple_choice` question has exactly one option with `correct: true`
    - Every option (correct and wrong) has an `explanation` field
    - Every question has a `reference_section` field pointing to a real MDX file path
    - The two sample questions from RESEARCH.md are present verbatim: q3.1.1 (JUnit flag) and q3.6.1 (code completion)
    - `bash scripts/lint-quizzes.sh` exits 0 with no FAIL lines
    - At least one `code_completion` question and at least two `short_answer` questions are included
  </acceptance_criteria>
  <done>
    Module 3 quiz authored with 18 questions covering all 7 chapters, all passing the lint-quizzes.sh script. Mix of multiple_choice, code_completion, and short_answer types. All questions reference the corresponding MDX lesson sections.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lab starter YAML → learner's GitHub Actions | Learner copies workflow to their own repo; executes under their account |
| broken-api.calm.json → validate output | Intentionally malformed file; validate output is educational |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | labs/lab-03-cicd-gate/starter/architectures/*.calm.json | mitigate | secure-api.calm.json validated by `npx @finos/calm-cli validate -a` with 0 errors before commit. broken-api.calm.json intentionally invalid — acceptance criteria verifies it produces errors |
| T-04-04-02 | Tampering | GitHub Actions YAML in solution/ | mitigate | Solution workflow uses pinned action versions (checkout@v4, setup-node@v4, upload-artifact@v4) and installs `@finos/calm-cli` — official FINOS package. No third-party actions introduced |
| T-04-04-03 | Information Disclosure | Quiz reference_section paths | accept | Reference sections point to educational MDX files in the repo; no sensitive paths |
| T-04-04-SC | Tampering | npm install in GitHub Actions YAML | mitigate | `npm install -g @finos/calm-cli` installs the official FINOS package (previously verified at 1.45.0). Package legitimacy confirmed in RESEARCH.md audit table. No [ASSUMED] packages |

</threat_model>

<verification>
```bash
# 1. Lab architectures validate correctly
npx @finos/calm-cli validate -a labs/lab-03-cicd-gate/starter/architectures/secure-api.calm.json -f pretty
# Must exit 0

npx @finos/calm-cli validate -a labs/lab-03-cicd-gate/starter/architectures/broken-api.calm.json -f pretty
# Must exit NON-ZERO (expected to fail)

# 2. LAB.md has correct structure
grep -q 'id: lab-03-cicd-gate' labs/lab-03-cicd-gate/LAB.md
grep -q 'step-5' labs/lab-03-cicd-gate/LAB.md
grep -q 'requires_docker: false' labs/lab-03-cicd-gate/LAB.md

# 3. Solution workflow is complete
grep -q 'calm validate' labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml
grep -q 'upload-artifact' labs/lab-03-cicd-gate/solution/.github/workflows/calm-gate.yml

# 4. Quiz passes linter
bash scripts/lint-quizzes.sh

# 5. Quiz has at least 16 questions
test "$(grep -c 'id: q3\.' quizzes/module-03-ecosystem.yaml)" -ge 16
```
</verification>

<success_criteria>
This plan succeeds when:

1. `labs/lab-03-cicd-gate/LAB.md` exists with Learnforge-compatible YAML frontmatter, 5 steps, correct check kinds
2. `starter/architectures/secure-api.calm.json` passes `calm validate` with 0 errors
3. `starter/architectures/broken-api.calm.json` FAILS `calm validate` with at least 1 error (intentional)
4. `solution/.github/workflows/calm-gate.yml` contains both validate steps plus upload-artifact
5. `quizzes/module-03-ecosystem.yaml` has 16-20 questions across 7 chapter sections, passes `bash scripts/lint-quizzes.sh`
</success_criteria>

<output>
Create `.planning/phases/04-module-3-calm-ecosystem/04-04-SUMMARY.md` when done, following the SUMMARY template.
</output>
