---
phase: 06-polish-milestone-close
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - site/src/components/Quiz.tsx
  - illustrations/exported/m00-three-paths.svg
  - illustrations/exported/m00-calm-studio-mock.svg
  - illustrations/exported/m00-architecture-as-prompt.svg
autonomous: false
requirements:
  - ILL-01
  - SITE-01

must_haves:
  truths:
    - "axe-cli accessibility check runs on two live pages with no Critical or Serious violations remaining"
    - "Three Module 0 illustration SVGs are real Excalidraw exports (each > 1KB), not placeholder stubs"
    - "Quiz component radio groups have fieldset/legend wrapping verified passing (already implemented in Phase 5)"
  artifacts:
    - path: "site/src/components/Quiz.tsx"
      provides: "Radio group questions wrapped in fieldset + legend elements"
      contains: "fieldset"
    - path: "illustrations/exported/m00-three-paths.svg"
      provides: "Real Excalidraw B&W SVG export, > 1KB"
    - path: "illustrations/exported/m00-calm-studio-mock.svg"
      provides: "Real Excalidraw B&W SVG export, > 1KB"
    - path: "illustrations/exported/m00-architecture-as-prompt.svg"
      provides: "Real Excalidraw B&W SVG export, > 1KB"
  key_links:
    - from: "site/src/components/Quiz.tsx"
      to: "WCAG 2.1 AA"
      via: "fieldset/legend wrapping radio groups"
      pattern: "<fieldset"
---

<objective>
Run the WCAG 2.1 AA accessibility spot check, fix Critical/Serious violations (most likely the
Quiz.tsx fieldset/legend gap), then wait for the user to author the 3 Module 0 Excalidraw
illustrations.

Purpose: The FINOS pitch will be reviewed by accessibility-aware programs (LF EdX requires
WCAG 2.1 AA). Module 0 illustrations are the first thing a reviewer sees — placeholder grey
boxes break the demo hook.

Output:
1. axe-cli run on two live pages; Critical/Serious violations fixed
2. Quiz.tsx radio groups wrapped in fieldset/legend (most likely actionable axe finding)
3. Human checkpoint: user authors 3 Module 0 SVGs in Excalidraw (B&W house style, > 1KB each)
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-polish-milestone-close/06-CONTEXT.md
@.planning/phases/06-polish-milestone-close/06-RESEARCH.md

<interfaces>
<!-- Quiz.tsx ARIA state (from 06-RESEARCH.md Accessibility Scope): -->
<!--   - role="status" + aria-live="polite" on results container (line ~253) — CORRECT -->
<!--   - aria-label on short-answer input — CORRECT -->
<!--   - fieldset/legend wrapping for radio groups: NOT VERIFIED — likely missing -->
<!-- Multiple-choice questions use input[type="radio"] — WCAG requires fieldset+legend for radio groups -->
<!-- Fix pattern: wrap each question's radio inputs in <fieldset><legend>{question text}</legend>...</fieldset> -->
<!-- Keep existing className/styling; only add semantic HTML wrappers -->

<!-- Illustration source files: -->
<!--   illustrations/source/m00-three-paths.excalidraw -->
<!--   illustrations/source/m00-calm-studio-mock.excalidraw (note: filename is mock not screenshot) -->
<!--   illustrations/source/m00-architecture-as-prompt.excalidraw -->
<!-- Export command after authoring: bash scripts/export-excalidraw.sh <name> -->
<!-- Verify: ls -la illustrations/exported/m00-*.svg | awk '{print $5, $9}' -- all must be > 1024 bytes -->

<!-- Live URLs for axe-cli: -->
<!--   https://gjs-opsflo.github.io/calm-academy/docs/module-00-on-ramp/three-paths-to-first-calm-doc -->
<!--   https://gjs-opsflo.github.io/calm-academy/docs/module-02-calm-fundamentals/nodes -->
<!-- axe-cli command (no install needed): npx axe-cli <url> -->
<!-- Fix only: critical and serious violations. Document moderate/minor as known tech debt. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run axe-cli accessibility check and fix Critical/Serious violations</name>
  <files>site/src/components/Quiz.tsx</files>
  <action>
Run the axe-cli accessibility spot check on both target pages (per D7). Use npx — no install
needed. Check the package name first to avoid slopcheck issues:

  npx axe-cli https://gjs-opsflo.github.io/calm-academy/docs/module-00-on-ramp/three-paths-to-first-calm-doc
  npx axe-cli https://gjs-opsflo.github.io/calm-academy/docs/module-02-calm-fundamentals/nodes

Capture the output. For each violation returned:
- Impact "critical" or "serious": fix before proceeding
- Impact "moderate" or "minor": document in task summary, do not fix now

Note: Quiz.tsx was built with fieldset/legend in Phase 5 (Wave 3). The 15 Quiz.test.tsx tests
include explicit fieldset/legend and aria-label assertions. This is already implemented —
axe-cli should confirm it as passing, not flag it as a violation.

If axe-cli returns Critical/Serious violations unrelated to fieldset/legend, fix the
applicable violations and document findings in the task summary.

If axe-cli returns a fieldset/legend violation (unexpected given Phase 5 implementation):
1. Read site/src/components/Quiz.tsx to confirm the current state
2. Verify the test suite still passes: cd site && npm test -- --testPathPattern=Quiz
3. If tests pass but axe-cli flags a violation, investigate the specific axe rule and fix

If axe-cli is unavailable via npx (package not found), try: npx axe-core-cli
If neither works, document the failure and verify fieldset/legend by reading Quiz.tsx source
and confirming the test suite passes.

Commit any Quiz.tsx fixes: `site(a11y): wrap Quiz radio groups in fieldset/legend for WCAG 2.1 AA`
  </action>
  <verify>
    <automated>cd /Users/gshah/work/opsflow-sh/calm/calm-academy/site && npm test -- --testPathPattern=Quiz --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>
    1. axe-cli has been run on both target pages; output captured in task summary
    2. Zero Critical violations remain on both pages
    3. Zero Serious violations remain on both pages
    4. Moderate/Minor violations documented in task summary
    5. If Quiz.tsx was modified: npm test passes with no regressions
    6. Findings committed (or noted as no changes needed)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Author 3 Module 0 illustrations in Excalidraw (user action)</name>
  <what-built>
Task 1 completed the automated accessibility fix. The three Module 0 illustration source files
exist at:
  illustrations/source/m00-three-paths.excalidraw
  illustrations/source/m00-calm-studio-mock.excalidraw
  illustrations/source/m00-architecture-as-prompt.excalidraw

Each .excalidraw stub contains a full DIAGRAM SPEC describing what to draw.
These are referenced in:
  content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx (three-paths + architecture-as-prompt)
  content/module-00-on-ramp/live-demo-diagram-to-calm-in-five-minutes.mdx (calm-studio-mock + architecture-as-prompt)

All three currently export as placeholder SVGs < 1KB (grey rectangle with text label).
  </what-built>
  <how-to-verify>
Using your Excalidraw skill (~/.claude/skills/):

1. Open each .excalidraw stub in Excalidraw (excalidraw.com or local install)
2. Read the embedded DIAGRAM SPEC in each file
3. Draw the diagram in B&W house style (no color — black strokes, white fill, grey for depth)
4. Export as SVG from Excalidraw: File > Export image > SVG
5. Save to illustrations/exported/ replacing the placeholder:
   - m00-three-paths.svg
   - m00-calm-studio-mock.svg
   - m00-architecture-as-prompt.svg
6. Verify each SVG is > 1KB:
   ls -la illustrations/exported/m00-*.svg
7. Commit:
   git add illustrations/exported/m00-three-paths.svg illustrations/exported/m00-calm-studio-mock.svg illustrations/exported/m00-architecture-as-prompt.svg
   git commit -m "feat(ill-01): author Module 0 illustrations (three-paths, calm-studio-mock, architecture-as-prompt)"
8. Push and verify the images render on the live site (wait ~2 min for Pages deploy):
   https://gjs-opsflo.github.io/calm-academy/docs/module-00-on-ramp/three-paths-to-first-calm-doc

Expected outcome: the grey placeholder boxes in Module 0 chapters 0.1 and 0.2 are replaced by
real B&W Excalidraw diagrams.
  </how-to-verify>
  <resume-signal>
Type "illustrations done" when all 3 SVGs are authored, exported, > 1KB each, committed, and
visible on the live site. Or type "illustrations deferred" to accept the placeholder stubs for
the FINOS pitch (ILL-01 will remain Partial in REQUIREMENTS.md).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npx axe-cli -> live site | axe-cli fetches the live URL; no auth required; read-only |
| Quiz.tsx edit -> built site | Component change must not break existing Quiz tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | Quiz.tsx fieldset edit | mitigate | Run npm test after edit; fieldset is additive semantic HTML — no logic change |
| T-06-09 | Information Disclosure | axe-cli scanning live site | accept | Live site is intentionally public; axe-cli is read-only DOM analysis |
| T-06-10 | Tampering | Illustration SVG replacement | accept | SVGs are static image assets; replaced by the same user who authored the stubs |
| T-06-SC | Tampering | npm/npx installs | mitigate | axe-cli from Deque Systems is an established WCAG tooling package; verify package name before npx run; slopcheck: npmjs.com/package/axe-cli |
</threat_model>

<verification>
- [ ] axe-cli output captured for both target URLs
- [ ] Zero Critical violations on both pages
- [ ] Zero Serious violations on both pages
- [ ] Moderate/Minor violations documented in SUMMARY.md
- [ ] Quiz.tsx test suite passes (no regressions from fieldset/legend change)
- [ ] Human checkpoint completed: user confirms "illustrations done" or "illustrations deferred"
- [ ] If "illustrations done": all 3 m00-*.svg files are > 1KB
- [ ] If "illustrations done": images render on live site (not placeholder boxes)
</verification>

<success_criteria>
1. Accessibility spot check complete — no Critical/Serious WCAG 2.1 AA violations on Module 0 or Module 2 pages (D7 complete)
2. Quiz.tsx radio groups are screen-reader accessible via fieldset/legend
3. Module 0 illustration status resolved (authored or explicitly deferred with user sign-off) — D1 complete
</success_criteria>

<output>
Create `.planning/phases/06-polish-milestone-close/06-03-SUMMARY.md` when done.
Include: axe-cli findings table (violation / impact / fixed?), Quiz.tsx change summary,
illustration authoring outcome (done/deferred + file sizes if done).
</output>
