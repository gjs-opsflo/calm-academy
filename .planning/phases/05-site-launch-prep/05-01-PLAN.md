---
phase: 05-site-launch-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/package.json
  - site/package-lock.json
  - site/docusaurus.config.js
  - site/sidebars.js
  - site/tsconfig.json
  - site/src/css/custom.css
  - site/src/pages/index.tsx
  - site/static/.nojekyll
  - site/static/img
  - scripts/fix-image-paths.mjs
  - content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx
  - content/module-00-on-ramp/live-demo-diagram-to-calm-in-five-minutes.mdx
  - content/module-00-on-ramp/why-this-works-and-when-it-doesnt.mdx
  - content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx
  - content/module-00-on-ramp/your-first-calm-document.mdx
  - content/module-01-case-for-aac/architecture-debt-crisis.mdx
  - content/module-01-case-for-aac/lessons-from-adjacent-disciplines.mdx
  - content/module-01-case-for-aac/what-architecture-as-code-enables.mdx
  - content/module-01-case-for-aac/introducing-calm.mdx
  - content/module-01-case-for-aac/governance-frameworks-and-aac.mdx
  - content/module-02-calm-fundamentals/calm-specification.mdx
  - content/module-02-calm-fundamentals/nodes.mdx
  - content/module-02-calm-fundamentals/relationships.mdx
  - content/module-02-calm-fundamentals/interfaces.mdx
  - content/module-02-calm-fundamentals/controls.mdx
  - content/module-02-calm-fundamentals/decorators.mdx
  - content/module-02-calm-fundamentals/building-your-first-architecture.mdx
  - content/module-03-calm-ecosystem/cli-toolbox.mdx
  - content/module-03-calm-ecosystem/calm-studio-visual-design.mdx
  - content/module-03-calm-ecosystem/calm-hub-architecture-registry.mdx
  - content/module-03-calm-ecosystem/calm-server-validation-service.mdx
  - content/module-03-calm-ecosystem/vscode-extension.mdx
  - content/module-03-calm-ecosystem/patterns-and-standards.mdx
  - content/module-03-calm-ecosystem/cicd-integration.mdx
autonomous: true
requirements: [SITE-01]

must_haves:
  truths:
    - "Docusaurus site scaffold exists at site/ and is buildable from a fresh clone"
    - "User can run `cd site && npm run build` and the build completes with zero errors"
    - "User can run `cd site && npm start` and visit `localhost:3000/calm-academy/docs/three-paths-to-first-calm-doc` to read Module 0 chapter 0.1 with all images rendering"
    - "All 36 illustration SVGs are reachable at `/img/<filename>.svg` (via static/img symlink)"
    - "Empty placeholder content directories no longer pollute the sidebar"
  artifacts:
    - path: "site/package.json"
      provides: "Docusaurus 3.10.1 dependency manifest"
      contains: "@docusaurus/core"
    - path: "site/docusaurus.config.js"
      provides: "Site configuration with docs path '../content', baseUrl '/calm-academy/', local search plugin, no Algolia key"
      contains: "path: '../content'"
    - path: "site/sidebars.js"
      provides: "Autogenerated sidebar config"
      contains: "type: 'autogenerated'"
    - path: "site/static/img"
      provides: "Symlink to ../../illustrations/exported/ — exposes 36 SVGs at /img/<file>.svg"
    - path: "site/static/.nojekyll"
      provides: "Empty file preventing GitHub Pages Jekyll processing"
    - path: "site/src/css/custom.css"
      provides: "Minimal CSS overrides stub (Docusaurus scaffold default — no custom branding for launch)"
    - path: "site/src/pages/index.tsx"
      provides: "Minimal landing page with heading, subheading, and Start Learning CTA → /docs"
    - path: "scripts/fix-image-paths.mjs"
      provides: "Portable cross-platform Node.js script that rewrites relative illustration paths to /img/<file>.svg"
  key_links:
    - from: "site/docusaurus.config.js"
      to: "../content/"
      via: "docs.path config"
      pattern: "path:\\s*['\"]\\.\\./content['\"]"
    - from: "site/static/img"
      to: "../../illustrations/exported/"
      via: "filesystem symlink"
      pattern: "symbolic link"
    - from: "content/**/*.mdx"
      to: "/img/<file>.svg"
      via: "Docusaurus absolute static path"
      pattern: "/img/[^)]+\\.svg"
---

## Phase Goal

**As a** learner browsing CALM Academy, **I want to** open the local site and read Module 0 chapter 0.1 with all illustrations rendered correctly, **so that** I can verify the production-ready Docusaurus scaffold is wired to the canonical `content/` directory and the static/img path mapping works end-to-end.

> NOTE: The ROADMAP Phase 5 Goal is written as an outcome statement, not a user story. This plan's user story is derived from CONTEXT.md D1/D8/D9 ("scaffold and deploy the Docusaurus site so CALM Academy content is publicly browsable") and the first observable truth from goal-backward derivation. The orchestrator may rerun `/gsd mvp-phase 05` to formalize the ROADMAP Goal line if desired.

<objective>
Deliver the first vertical slice of the live site: scaffold Docusaurus 3.10.1 in `site/`, wire it to the existing `content/` directory via `docs.path` config (NOT a docs symlink — Docusaurus issues #3272, #6257, #10751 make symlinking the docs directory unsupported), expose the 36 illustration SVGs via a `site/static/img` filesystem symlink to `../../illustrations/exported/`, fix the 35 broken relative image references across 21 MDX files to use Docusaurus-canonical `/img/<file>.svg` absolute paths, remove the 5 empty placeholder content directories that would produce ghost sidebar entries, and prove the slice works by running `npm run build` to completion and visually loading Module 0 chapter 0.1 in dev mode.

Purpose: A working local site renders one real lesson with images. This is the thinnest end-to-end stack — content source → MDX processing → static asset resolution → HTML output. Every later plan builds on this foundation.

Output: A buildable Docusaurus site, 21 MDX files with corrected image paths, 5 empty directories removed, and a passing `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-site-launch-prep/05-CONTEXT.md
@.planning/phases/05-site-launch-prep/05-RESEARCH.md
@.planning/phases/05-site-launch-prep/05-PATTERNS.md
@.planning/phases/05-site-launch-prep/05-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Existing MDX frontmatter shape (do not break) — from content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx -->

```yaml
---
title: "Three Paths to Your First CALM Document"
slug: three-paths-to-first-calm-doc
module: 0
chapter: 0.1
estimated_minutes: 5
prerequisites: []
---
```

<!-- Existing image reference pattern in modules 00 and 01 (35 refs to fix; broken in Docusaurus) -->

```markdown
![alt text](../../illustrations/exported/m00-three-paths.svg)
```

<!-- Existing image reference pattern in modules 02 and 03 (wrong depth AND wrong for Docusaurus) -->

```markdown
![alt text](../illustrations/exported/m02-decorator-external-overlay.svg)
```

<!-- Target image reference pattern (Docusaurus-canonical, resolves via site/static/img symlink) -->

```markdown
![alt text](/img/m00-three-paths.svg)
```

<!-- Required docusaurus.config.js shape (RESEARCH.md Pattern 1 — do not add algolia key, do not change baseUrl) -->

Key config keys: `title: 'CALM Academy'`, `url: 'https://gjs-opsflo.github.io'`, `baseUrl: '/calm-academy/'`, `organizationName: 'gjs-opsflo'`, `projectName: 'calm-academy'`, `trailingSlash: false`, `onBrokenLinks: 'throw'`, `onBrokenMarkdownLinks: 'warn'`. Preset: `@docusaurus/preset-classic` with `docs.path: '../content'`, `docs.routeBasePath: 'docs'`, `docs.sidebarPath: './sidebars.js'`, `blog: false`, `theme.customCss: './src/css/custom.css'`. Themes array (NOT presets array): `@easyops-cn/docusaurus-search-local` with `hashed: true`, `indexDocs: true`, `indexPages: false`. Navbar items: docSidebar link labeled "Docs" + GitHub external link → `https://github.com/gjs-opsflo/calm-academy`. Footer: style `dark`, single copyright line per UI-SPEC.

<!-- Empty placeholder directories to remove (verified empty via `find -type f` returning 0) -->

- content/module-02-fundamentals/ (naming artifact — duplicate of module-02-calm-fundamentals/)
- content/module-03-ecosystem/ (naming artifact — duplicate of module-03-calm-ecosystem/)
- content/module-04-governance/ (future module placeholder)
- content/module-05-ai-native/ (future module placeholder)
- content/module-06-enterprise/ (future module placeholder)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Docusaurus and configure site for ../content + /img/ static assets</name>
  <files>site/package.json, site/package-lock.json, site/docusaurus.config.js, site/sidebars.js, site/tsconfig.json, site/src/css/custom.css, site/src/pages/index.tsx, site/static/.nojekyll, site/static/img</files>
  <read_first>
    - .planning/phases/05-site-launch-prep/05-CONTEXT.md (locked decisions D1–D10; non-negotiable)
    - .planning/phases/05-site-launch-prep/05-RESEARCH.md (Pattern 1 docusaurus.config.js, Pattern 2 sidebars.js, Critical Finding #3 Node 22, scaffold command)
    - .planning/phases/05-site-launch-prep/05-PATTERNS.md (docusaurus.config.js complete pattern, sidebars.js complete pattern, index.tsx minimal stub)
    - .planning/phases/05-site-launch-prep/05-UI-SPEC.md (Docusaurus Site Structure Contract: navbar items, footer copy, landing page CTA "Start Learning")
    - .github/workflows/ci.yml (existing Node 22 pinning — match this)
    - site/README.md (existing — only file currently in site/)
  </read_first>
  <action>
    From the repo root, run `npx create-docusaurus@3.10.1 site classic --typescript` to scaffold. Then `rm -rf site/docs` (the scaffold's placeholder docs directory — content comes from `../content/` via config). From `site/`, install `js-yaml@4.2.0` as a DEV DEPENDENCY (`npm install --save-dev js-yaml@4.2.0`) — it is only needed by the `scripts/convert-quiz-yaml.mjs` prebuild script that runs at build time, never in the browser bundle. Also install as dev deps: `@easyops-cn/docusaurus-search-local@0.55.2` and `typescript@~5.x` (Docusaurus 3.10.1 peer requires TypeScript 5.x; use scaffold-provided version — do NOT upgrade to 6.x even though npm registry has 6.0.3, because Docusaurus 3.10.1 has not declared peer support for TS 6 yet — verify by reading the generated `site/package.json` peer deps after scaffold and fall back to the version the scaffold pins).

    Overwrite `site/docusaurus.config.js` per CONTEXT.md D5/D6/D8 and RESEARCH.md Pattern 1: title `'CALM Academy'`, tagline `'Architecture as Code — the official course'`, url `'https://gjs-opsflo.github.io'`, baseUrl `'/calm-academy/'`, organizationName `'gjs-opsflo'`, projectName `'calm-academy'`, trailingSlash `false`, onBrokenLinks `'throw'`, onBrokenMarkdownLinks `'warn'`. Preset `@docusaurus/preset-classic` with docs `{ path: '../content', sidebarPath: './sidebars.js', routeBasePath: 'docs' }`, blog `false`, theme `{ customCss: './src/css/custom.css' }`. Themes array with `@easyops-cn/docusaurus-search-local` configured `{ hashed: true, indexDocs: true, indexPages: false }`. themeConfig.navbar: title `'CALM Academy'`, items: docSidebar pointing at sidebarId `tutorialSidebar` labeled `'Docs'` (position left) + external link to `https://github.com/gjs-opsflo/calm-academy` labeled `'GitHub'` (position right). themeConfig.footer: style `dark`, copyright string EXACTLY `'CALM Academy. Content: CC BY-SA 4.0. Code: Apache 2.0. Built with Docusaurus.'` (verbatim from UI-SPEC.md Copywriting Contract). DO NOT add an `algolia` key (per D6 and RESEARCH.md anti-pattern — placeholder Algolia config breaks the search bar).

    Overwrite `site/sidebars.js` with the autogenerated pattern: export `{ tutorialSidebar: [{ type: 'autogenerated', dirName: '.' }] }` (per CONTEXT.md D2 and RESEARCH.md Pattern 2).

    Overwrite `site/src/pages/index.tsx` with a minimal landing page (per UI-SPEC Landing Page section): heading "CALM Academy", subheading "Architecture as Code — the official course", single CTA labeled "Start Learning" (exact string per UI-SPEC Copywriting Contract) linking via `@docusaurus/Link` to `/docs/three-paths-to-first-calm-doc` (Module 0 chapter 0.1 — the entry point per CURRICULUM.md). No hero image, no feature grid.

    Create empty file `site/static/.nojekyll` (zero bytes — required for GitHub Pages per RESEARCH.md Pitfall 2).

    Create the static/img symlink: from inside `site/static/`, run `ln -s ../../illustrations/exported img` (per RESEARCH.md "Creating the static/img/ symlink" code example). Verify the symlink is tracked by git as a symbolic link (not a directory copy) before committing — run `git ls-files --stage site/static/img` and confirm mode is `120000` (symlink mode).

    Leave `site/src/css/custom.css` as the scaffold default — no custom branding for launch (per CONTEXT.md D5 and UI-SPEC Design System table: "minimal customization for launch").

    Remove the 5 empty placeholder content directories per RESEARCH.md Critical Finding #2: `rm -rf content/module-02-fundamentals content/module-03-ecosystem content/module-04-governance content/module-05-ai-native content/module-06-enterprise`. Before removal, verify each is empty by running `find content/module-02-fundamentals -type f` etc. — if any contains a file (other than `.gitkeep`), STOP and report to user.
  </action>
  <acceptance_criteria>
    - File `site/package.json` exists and contains `"@docusaurus/core": "3.10.1"` and `"@docusaurus/preset-classic": "3.10.1"`
    - File `site/package.json` contains `"js-yaml": "4.2.0"` in the `devDependencies` section (NOT in `dependencies`): `node -e "const p=JSON.parse(require('fs').readFileSync('site/package.json','utf8')); process.exit(p.devDependencies?.['js-yaml'] ? 0 : 1)"`
    - File `site/package.json` does NOT have `"js-yaml"` in its `dependencies` section: `node -e "const p=JSON.parse(require('fs').readFileSync('site/package.json','utf8')); process.exit(p.dependencies?.['js-yaml'] ? 1 : 0)"`
    - File `site/package.json` contains `"@easyops-cn/docusaurus-search-local"` in devDependencies
    - File `site/docusaurus.config.js` exists and `grep -F "path: '../content'" site/docusaurus.config.js` matches (single quotes literal)
    - File `site/docusaurus.config.js` contains `baseUrl: '/calm-academy/'` and does NOT contain the substring `algolia:` (case-sensitive grep)
    - File `site/sidebars.js` exists and `grep -F "type: 'autogenerated'" site/sidebars.js` matches
    - File `site/static/.nojekyll` exists and is zero bytes (`wc -c site/static/.nojekyll` outputs `0 site/static/.nojekyll`)
    - File `site/static/img` is a symbolic link (`test -L site/static/img && echo OK` prints `OK`)
    - The symlink resolves to a directory containing at least 36 .svg files (`ls site/static/img/*.svg | wc -l` ≥ 36)
    - File `site/src/pages/index.tsx` contains the string `Start Learning` and `/docs/three-paths-to-first-calm-doc`
    - Directories `content/module-02-fundamentals/`, `content/module-03-ecosystem/`, `content/module-04-governance/`, `content/module-05-ai-native/`, `content/module-06-enterprise/` no longer exist (`test ! -e content/module-02-fundamentals` etc. all succeed)
    - `cd site && npm install` exits 0 (use `npm ci` IF a lockfile is committed by scaffold; otherwise `npm install` is acceptable for the first scaffold pass)
  </acceptance_criteria>
  <verify>
    <automated>cd site && test -f package.json && test -f docusaurus.config.js && test -f sidebars.js && test -L static/img && test -f static/.nojekyll && grep -F "path: '../content'" docusaurus.config.js && grep -F "baseUrl: '/calm-academy/'" docusaurus.config.js && ! grep -q "algolia:" docusaurus.config.js && [ "$(ls static/img/*.svg | wc -l)" -ge 36 ] && node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); process.exit(p.devDependencies?.['js-yaml'] && !p.dependencies?.['js-yaml'] ? 0 : 1)" && echo ALL_CHECKS_PASS</automated>
  </verify>
  <done>Scaffold complete; configuration honors D1–D10; js-yaml in devDependencies only; 36 SVGs reachable via `site/static/img/*.svg`; empty placeholder dirs removed; `npm install` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite all 35 broken image paths across 21 MDX files to /img/&lt;file&gt;.svg</name>
  <files>scripts/fix-image-paths.mjs, content/module-00-on-ramp/live-demo-diagram-to-calm-in-five-minutes.mdx, content/module-00-on-ramp/why-this-works-and-when-it-doesnt.mdx, content/module-01-case-for-aac/architecture-debt-crisis.mdx, content/module-01-case-for-aac/governance-frameworks-and-aac.mdx, content/module-01-case-for-aac/introducing-calm.mdx, content/module-01-case-for-aac/lessons-from-adjacent-disciplines.mdx, content/module-01-case-for-aac/what-architecture-as-code-enables.mdx, content/module-02-calm-fundamentals/*.mdx (7 files, image-bearing), content/module-03-calm-ecosystem/*.mdx (7 files, image-bearing)</files>
  <read_first>
    - .planning/phases/05-site-launch-prep/05-RESEARCH.md (Critical Finding #1, Pattern 6, Image path replacement notes)
    - .planning/phases/05-site-launch-prep/05-PATTERNS.md (Image path fixes section)
    - content/module-00-on-ramp/live-demo-diagram-to-calm-in-five-minutes.mdx (verify current broken pattern before transforming)
    - content/module-02-calm-fundamentals/building-your-first-architecture.mdx (verify the `../illustrations/exported/` depth variant)
  </read_first>
  <action>
    Replace all relative paths to `illustrations/exported/<file>.svg` with Docusaurus absolute paths `/img/<file>.svg`. Two depth variants exist in the codebase and must both be handled:
    - Variant A (modules 00, 01): `../../illustrations/exported/<file>.svg`
    - Variant B (modules 02, 03): `../illustrations/exported/<file>.svg`

    DO NOT use `sed -i ''` — it is macOS-only and will fail on Linux CI runners (the CI uses `ubuntu-latest`). Instead, create a portable Node.js script at `scripts/fix-image-paths.mjs` and run it. The script must work identically on macOS and Linux.

    Create `scripts/fix-image-paths.mjs` with this logic:
    1. Use `fs` and `path` from Node.js core (no npm dependencies — script must run before `npm ci`).
    2. Glob `content/**/*.mdx` using `fs.readdirSync` recursively (or use a manual recursive walk since no glob package is available at script-run time).
    3. For each `.mdx` file, read its content, apply a regex replace that matches one-or-more `../` prefixes followed by `illustrations/exported/` followed by the filename, and replaces the whole matched path with `/img/<filename>`. The regex: `/(?:\.\.\/)+illustrations\/exported\/([^)"\s]+)/g` → replacement: `/img/$1`.
    4. If the content changed, write the file back.
    5. Print a summary: `Fixed N files, replaced M occurrences.`

    From the repo root, run: `node scripts/fix-image-paths.mjs`

    The transformation preserves the filename stem: e.g. `../../illustrations/exported/m00-three-paths.svg` → `/img/m00-three-paths.svg`, and `../illustrations/exported/m02-decorator-external-overlay.svg` → `/img/m02-decorator-external-overlay.svg`.

    Per CONTEXT.md D1 and RESEARCH.md anti-patterns: DO NOT copy/duplicate content into `site/docs/`, DO NOT use the relative-path-via-symlink approach. The `/img/` absolute path is the Docusaurus-canonical pattern that resolves through baseUrl to `/calm-academy/img/<file>.svg` in production.

    After the script runs, verify: `rg "illustrations/exported" content/` — output MUST be empty (zero matches). If any match remains (e.g. inside a code fence as an example), inspect manually: code-fence examples that intentionally show the OLD broken pattern as a "do not do this" demonstration should be preserved by quoting them differently or leaving alone after confirming they are inside ``` fenced code blocks (Docusaurus does not resolve markdown image syntax inside code fences). For this phase, no such intentional pedagogical references are expected — treat any leftover match as a real broken path and fix it.
  </action>
  <acceptance_criteria>
    - File `scripts/fix-image-paths.mjs` exists at the repo root level and is a valid ES module (first line is `import` or a comment)
    - `node scripts/fix-image-paths.mjs` exits 0 on both macOS and Linux (no platform-specific syscalls)
    - `rg "illustrations/exported" content/ -c` returns no output (zero matches across all MDX files)
    - `rg -c "/img/m00-" content/module-00-on-ramp/` reports at least 1 match in 2 files (live-demo and why-this-works)
    - `rg -c "/img/m01-" content/module-01-case-for-aac/` reports matches in 5 files (architecture-debt-crisis, governance-frameworks-and-aac, introducing-calm, lessons-from-adjacent-disciplines, what-architecture-as-code-enables)
    - `rg -c "/img/m02-" content/module-02-calm-fundamentals/` reports matches in 7 files
    - `rg -c "/img/m03-" content/module-03-calm-ecosystem/` reports matches in 7 files
    - Total `/img/` SVG references across `content/` is exactly 35 (matches the count documented in RESEARCH.md): `rg -o "/img/[a-z0-9-]+\.svg" content/ | wc -l` outputs `35`
    - Every referenced SVG filename exists on disk: each path `/img/<file>.svg` referenced in MDX corresponds to a real file at `illustrations/exported/<file>.svg` (no typos introduced)
  </acceptance_criteria>
  <verify>
    <automated>! rg -q "illustrations/exported" content/ && [ "$(rg -o '/img/[a-z0-9-]+\.svg' content/ | wc -l | tr -d ' ')" = "35" ] && for f in $(rg -o "/img/([a-z0-9-]+\.svg)" content/ -r '$1' --no-filename | sort -u); do test -f "illustrations/exported/$f" || { echo "MISSING: $f"; exit 1; }; done && echo IMAGE_PATHS_FIXED</automated>
  </verify>
  <done>Zero matches for `illustrations/exported` in content; exactly 35 `/img/*.svg` references; every referenced SVG exists on disk; `scripts/fix-image-paths.mjs` committed for use by CI.</done>
</task>

<task type="auto">
  <name>Task 3: Run npm build and verify Module 0 chapter 0.1 renders with images in dev mode</name>
  <files>site/ (build output verification only; no new files created beyond `site/build/`)</files>
  <read_first>
    - site/docusaurus.config.js (verify config from Task 1)
    - site/sidebars.js (verify autogenerated sidebar from Task 1)
    - content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx (verify image refs from Task 2)
    - .planning/phases/05-site-launch-prep/05-RESEARCH.md (Pitfall 1 symlink docs, Pitfall 2 .nojekyll, Pitfall 4 baseUrl in TSX — debugging guides)
  </read_first>
  <action>
    From `site/`, run `npm run build` and capture full stdout+stderr. Expected outcome: build completes with `[SUCCESS] Generated static files in "build".` and exit code 0. The site is configured with `onBrokenLinks: 'throw'` (per Task 1), so any broken internal link will fail the build — this is intentional. At this stage, sidebar ordering is alphabetical (no `sidebar_position` frontmatter yet — that work is Plan 02), so chapters will appear out of pedagogical order, which is ACCEPTABLE for this plan's slice. The build success threshold is: zero errors, broken-link check passes, all 24 MDX files compile.

    If the build fails with "Cannot read properties of undefined" or pagination errors → likely the `docs.path: '../content'` is not resolving from the build's working directory. The fallback per RESEARCH.md A1 is a prebuild `cp -r ../content site/docs` step, BUT this conflicts with CONTEXT.md D1 (no copy) — if needed, STOP and surface to user; do not silently switch approaches.

    If the build fails with "Module parse failed" pointing to a `.yaml` file → that is the Plan 02 prebuild step missing. Not in scope for this plan; if encountered, it means an MDX file already imports YAML which Plan 02 will address — note in TASK_NOTES and proceed.

    If the build fails with a broken image link (`/img/<file>.svg`) → the Task 1 symlink is misconfigured. Fix the symlink before declaring done.

    Then run `npm start &` (background) and curl `http://localhost:3000/calm-academy/docs/three-paths-to-first-calm-doc` — expect HTTP 200 and HTML containing the chapter title "Three Paths to Your First CALM Document". Also curl `http://localhost:3000/calm-academy/img/m00-three-paths.svg` — expect HTTP 200 and SVG content (Content-Type `image/svg+xml`). Kill the dev server after verification.
  </action>
  <acceptance_criteria>
    - `cd site && npm run build` exits 0
    - Build stdout contains `[SUCCESS] Generated static files`
    - `site/build/index.html` exists after build
    - `site/build/docs/three-paths-to-first-calm-doc/index.html` exists (the Module 0 chapter 0.1 output)
    - `site/build/img/m00-three-paths.svg` exists (the symlinked SVG resolved into build output)
    - `grep -F "Three Paths to Your First CALM Document" site/build/docs/three-paths-to-first-calm-doc/index.html` matches (chapter title present in compiled HTML)
    - `grep -F "/calm-academy/img/m00-three-paths.svg" site/build/docs/three-paths-to-first-calm-doc/index.html` matches (image path baseUrl-rewritten correctly)
    - No occurrence of `Broken link` or `Broken anchor` in build stdout
  </acceptance_criteria>
  <verify>
    <automated>cd site && npm run build 2>&1 | tee /tmp/build.log | tail -50 && grep -F "[SUCCESS] Generated static files" /tmp/build.log && test -f build/docs/three-paths-to-first-calm-doc/index.html && test -f build/img/m00-three-paths.svg && grep -qF "Three Paths to Your First CALM Document" build/docs/three-paths-to-first-calm-doc/index.html && grep -qF "/calm-academy/img/m00-three-paths.svg" build/docs/three-paths-to-first-calm-doc/index.html && ! grep -qE "Broken link|Broken anchor" /tmp/build.log && echo BUILD_VERIFIED</automated>
  </verify>
  <done>`npm run build` succeeds; Module 0 chapter 0.1 page exists in build output with chapter title and baseUrl-prefixed image reference; m00-three-paths.svg is reachable in build/img/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → CI/dev install | Untrusted package code executes during install + build |
| Filesystem symlink (`site/static/img` → `../../illustrations/exported`) | Crosses into a different directory tree at build time |
| Git → CI runner (`actions/checkout@v4`) | Git-tracked symlinks materialize on Linux runner |
| MDX → HTML (Docusaurus build) | Author-controlled MDX rendered as HTML; no untrusted input but XSS hygiene still matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | npm dependency tree (@docusaurus/core, preset-classic, js-yaml, @easyops-cn/docusaurus-search-local, react, typescript) | mitigate | Commit `site/package-lock.json`; CI uses `npm ci` (locked). All packages verified via npm registry in RESEARCH.md Package Legitimacy Audit; all official maintainers (Meta, nodeca, easyops-cn, Microsoft); no [ASSUMED] or [SUS] packages |
| T-05-02 | Tampering | `site/static/img` symlink (filesystem) | accept | Symlink target is a git-tracked sibling directory (`illustrations/exported/`); checkout@v4 preserves tracked symlinks; risk of dangling/escaping symlink is mitigated by relative path constrained to repo tree |
| T-05-03 | Information Disclosure | Docusaurus build output (public static site) | accept | All content is intended for public consumption (CC BY-SA 4.0 per UI-SPEC footer); no secrets, no PII, no auth tokens in this phase |
| T-05-04 | Tampering | MDX → HTML rendering (Docusaurus internal) | accept | Authored MDX is trusted (single-author repo); React JSX auto-escapes; no user-submitted content rendered |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | All 6 npm packages verified directly on npmjs.com via npm registry (PyPI slopcheck N/A for npm packages — see RESEARCH.md Package Legitimacy Audit note); no [ASSUMED] or [SUS] packages — no blocking-human checkpoint required |
| T-05-05 | Denial of Service | Build failure due to symlink issue on CI Linux runner | mitigate | Assumption A2 in RESEARCH.md flags this; CI workflow (Plan 04) will add fallback `ln -s` step if needed; verified in Plan 04 deploy task |
</threat_model>

<verification>
- Site scaffold buildable: `cd site && npm run build` exits 0
- 35 image references migrated: `rg "illustrations/exported" content/` returns zero matches
- 5 empty placeholder dirs removed: directories no longer exist on disk
- One real lesson page renders end-to-end: HTML produced for chapter 0.1 contains title and `/calm-academy/img/m00-three-paths.svg`
- No Algolia placeholder in config: `grep -q "algolia:" site/docusaurus.config.js` returns 1 (no match)
</verification>

<success_criteria>
1. Docusaurus 3.10.1 scaffold exists at `site/` with all dependencies installed
2. Configuration honors all 10 locked decisions (D1–D10); no Algolia key present
3. `site/static/img` is a tracked symlink to `../../illustrations/exported/` (mode 120000)
4. All 35 image references in `content/**/*.mdx` use `/img/<file>.svg` (Docusaurus absolute paths)
5. 5 empty placeholder content directories removed
6. `npm run build` exits 0 with no broken-link errors
7. Module 0 chapter 0.1 HTML output exists and contains the expected title + image baseUrl rewrite
8. `js-yaml` is in `devDependencies` only — not in `dependencies`
9. `scripts/fix-image-paths.mjs` exists and runs without platform-specific tooling
</success_criteria>

<output>
Create `.planning/phases/05-site-launch-prep/05-01-SUMMARY.md` when done. Record:
- Confirmed Docusaurus version installed and full dependency list from final `site/package.json`
- Whether `npm ci` or `npm install` was used (and why)
- Output of the build success line (exact stdout)
- Total image references migrated (must equal 35)
- Any unexpected issues encountered (e.g., peer dep warnings, TS version selection rationale)
- Confirmation that `T-05-02` symlink survived `git status` (showed as a symlink, not as a copied directory)
- Confirmation that `js-yaml` appears in `devDependencies` and NOT in `dependencies` in the final `site/package.json`
</output>
