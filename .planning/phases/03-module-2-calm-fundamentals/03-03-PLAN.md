---
phase: 03-module-2-calm-fundamentals
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - labs/lab-02-conference-signup/LAB.md
  - labs/lab-02-conference-signup/starter/conference-signup.architecture.json
  - labs/lab-02-conference-signup/starter/README.md
  - labs/lab-02-conference-signup/solution/conference-signup.architecture.json
  - quizzes/module-02-calm-fundamentals.yaml
autonomous: true
requirements: [LAB-02, QUIZ-03, CODE-01]
tags: [module-02, lab-02, quiz, calm-validation]

must_haves:
  truths:
    - "A learner who completes Lab 2 has hand-written a valid CALM 1.2 architecture and run `calm validate` to prove it"
    - "Lab 2 solution validates against CALM 1.2 schema with zero errors"
    - "Lab 2 starter skeleton exists but is intentionally incomplete (validates as JSON, may fail or pass calm validate depending on chosen incompleteness)"
    - "Module 2 quiz has 18-21 questions across all 7 chapters, passes lint-quizzes.sh"
    - "Every quiz question has a reference_section pointing to a real chapter file"
  artifacts:
    - path: "labs/lab-02-conference-signup/LAB.md"
      provides: "Lab 2 instructions in Learnforge-compatible YAML frontmatter + Markdown walkthrough"
      contains: "id: lab-02-conference-signup"
    - path: "labs/lab-02-conference-signup/starter/conference-signup.architecture.json"
      provides: "Skeleton CALM document that learner extends step-by-step"
      contains: "\"$schema\": \"https://calm.finos.org/release/1.2/meta/calm.json\""
    - path: "labs/lab-02-conference-signup/solution/conference-signup.architecture.json"
      provides: "Reference solution — passes calm validate"
      contains: "conference-attendee"
    - path: "quizzes/module-02-calm-fundamentals.yaml"
      provides: "Largest quiz in the course, 18-21 questions covering all 7 chapters"
      contains: "module: 2"
  key_links:
    - from: "labs/lab-02-conference-signup/LAB.md"
      to: "content/module-02-calm-fundamentals/building-your-first-architecture.mdx"
      via: "resources frontmatter link"
      pattern: "building-your-first-architecture"
    - from: "labs/lab-02-conference-signup/solution/conference-signup.architecture.json"
      to: "code-examples/module-02-calm-fundamentals/conference-signup.architecture.json"
      via: "same content"
      pattern: "conference-signup"
    - from: "quizzes/module-02-calm-fundamentals.yaml"
      to: "content/module-02-calm-fundamentals/*.mdx"
      via: "reference_section field"
      pattern: "../content/module-02-calm-fundamentals/"
---

## Phase Goal

**As a** learner who has read Module 2's 7 chapters, **I want to** hand-write a complete CALM 1.2 architecture in a lab and answer comprehensive vocabulary quiz questions, **so that** I can prove I have internalised the spec well enough to author and validate any CALM document.

<objective>
Deliver Lab 2 (the conference signup hand-write exercise) and the Module 2 quiz (the largest quiz in the course — 18-21 questions across all 7 chapters).

Purpose: Chapters and code examples teach; the lab proves the skill. The quiz tests vocabulary coverage. Together they close the Module 2 learning loop — a learner who passes the lab + quiz has demonstrated competence with the CALM 1.2 spec.

Output: complete `labs/lab-02-conference-signup/` directory (LAB.md + starter/ + solution/) and `quizzes/module-02-calm-fundamentals.yaml`. The Lab 2 solution must validate with `calm validate`. The quiz must pass `lint-quizzes.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md
@CLAUDE.md
@docs-meta/STYLE-GUIDE.md
@docs-meta/LAB-FORMAT.md
@docs-meta/QUIZ-FORMAT.md

# Prior phase patterns
@labs/lab-00-on-ramp/LAB.md
@quizzes/module-01-case-for-aac.yaml
@quizzes/module-00-on-ramp.yaml

# Phase 03 Wave outputs (this plan reads + uses them)
@.planning/phases/03-module-2-calm-fundamentals/03-01-SUMMARY.md
@.planning/phases/03-module-2-calm-fundamentals/03-02-SUMMARY.md
@code-examples/module-02-calm-fundamentals/conference-signup.architecture.json
@content/module-02-calm-fundamentals/calm-specification.mdx
@content/module-02-calm-fundamentals/nodes.mdx
@content/module-02-calm-fundamentals/relationships.mdx
@content/module-02-calm-fundamentals/interfaces.mdx
@content/module-02-calm-fundamentals/controls.mdx
@content/module-02-calm-fundamentals/decorators.mdx
@content/module-02-calm-fundamentals/building-your-first-architecture.mdx

# Validation tooling
@scripts/validate-calm.sh
@scripts/lint-quizzes.sh

<interfaces>
<!-- Patterns established by prior phases -->

LAB.md frontmatter (from labs/lab-00-on-ramp/LAB.md and docs-meta/LAB-FORMAT.md):
```
---
id: lab-NN-<slug>
title: "<title>"
module: N
chapter: N.N
estimated_minutes: <int>
difficulty: <1|2|3>
prerequisites:
  - <prior-lab-id or slug>
requires_docker: false
workspace:
  cumulative: false
  base_dir: lab-NN-<slug>
  creates:
    - <output-files>
objectives:
  - "<verb-led objective>"
steps:
  - id: step-N
    title: "<title>"
    check:
      kind: file_state | command_regex | exit_code | ai_judge
      ...
    hints:
      - "<tiered hint>"
resources:
  - title: "<title>"
    url: <relative-path>
---

# Lab N: <title>

## Goal
<one sentence>

## Setup
<commands>

## Walkthrough
<numbered steps matching YAML step IDs>

## Stretch goals (optional)

## Solution
```

Step check kinds (docs-meta/LAB-FORMAT.md):
- `command_regex` — runs shell command, matches output against regex
- `exit_code` — runs shell command, asserts exit code value
- `file_state` — asserts file exists / matches pattern
- `ai_judge` — LLM-graded (use sparingly)

Quiz YAML schema (docs-meta/QUIZ-FORMAT.md and quizzes/module-01-case-for-aac.yaml):
```yaml
module: <N>
title: "Module <N>: <title>"
chapters:
  - chapter: <N.N>
    slug: <chapter-slug>
    title: "<chapter title>"
    questions:
      - id: q<N>.<N>.<N>
        type: multiple_choice | short_answer | code_completion | multiple_correct
        prompt: |
          <prompt>
        options:           # for multiple_choice / multiple_correct
          - label: "..."
            correct: true | false
            explanation: "..."
        accepted_answers:  # for short_answer / code_completion
          - "..."
        snippet: |         # for code_completion only
          ...
        case_sensitive: false   # short_answer default
        reference_section: "<path-to-chapter-file>#<anchor>"
        explanation: "..."      # for non-multiple_choice
```

Lint requirements (scripts/lint-quizzes.sh, verified):
- Question IDs match `^q\d+\.\d+\.\d+$` (e.g. q2.1.1, q2.2.1)
- Every question has `reference_section` (non-empty)
- Every multiple_choice has exactly 1 option with `correct: true`
- Every option has `explanation`
- code_completion has `accepted_answers`
- No duplicate IDs

Conference signup solution must match code-examples/module-02-calm-fundamentals/conference-signup.architecture.json exactly — same 6 nodes, same 5 relationships, same interface, same control, same metadata. The solution is the canonical reference.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Lab 2 (LAB.md + starter + solution)</name>
  <files>
    labs/lab-02-conference-signup/LAB.md
    labs/lab-02-conference-signup/starter/conference-signup.architecture.json
    labs/lab-02-conference-signup/starter/README.md
    labs/lab-02-conference-signup/solution/conference-signup.architecture.json
  </files>
  <read_first>
    - labs/lab-00-on-ramp/LAB.md (full — Learnforge frontmatter pattern, step.check styles, hints tiering, resources block)
    - docs-meta/LAB-FORMAT.md (full — step check kinds, authoring rules)
    - code-examples/module-02-calm-fundamentals/conference-signup.architecture.json (full — this IS the solution; copy contents)
    - content/module-02-calm-fundamentals/building-your-first-architecture.mdx (full — the chapter that introduces the same 7-step build; Lab 2 mirrors this but learner does it themselves)
    - scripts/validate-calm.sh (full)
    - .planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md (section "The Conference Signup System — Lab 2 Design")
  </read_first>
  <action>
    Build the complete `labs/lab-02-conference-signup/` directory.

    **File 1 — `labs/lab-02-conference-signup/solution/conference-signup.architecture.json`**

    Copy the file `code-examples/module-02-calm-fundamentals/conference-signup.architecture.json` byte-for-byte to `labs/lab-02-conference-signup/solution/conference-signup.architecture.json`. The solution IS the anchor code example. After copying, run `npx @finos/calm-cli validate -a labs/lab-02-conference-signup/solution/conference-signup.architecture.json -f pretty` and confirm exit 0 with `hasErrors: false`.

    **File 2 — `labs/lab-02-conference-signup/starter/conference-signup.architecture.json`**

    The starter is intentionally incomplete — it gives the learner the top-level skeleton + `conference-attendee` (actor) + `conference-website` (webclient), but only ONE relationship (the interacts edge from attendee to website). The learner adds the remaining 4 nodes and 4 relationships themselves.

    Starter file contents:
    ```json
    {
      "$schema": "https://calm.finos.org/release/1.2/meta/calm.json",
      "unique-id": "conference-signup",
      "name": "Conference Signup System",
      "description": "Registration site where attendees sign up for a conference. STARTER — extend this file to match the spec described in LAB.md.",
      "nodes": [
        {
          "unique-id": "conference-attendee",
          "node-type": "actor",
          "name": "Conference Attendee",
          "description": "Person registering for the conference"
        },
        {
          "unique-id": "conference-website",
          "node-type": "webclient",
          "name": "Conference Website",
          "description": "Browser-based signup portal"
        }
      ],
      "relationships": [
        {
          "unique-id": "attendee-interacts-website",
          "description": "Conference attendee visits the signup site",
          "relationship-type": {
            "interacts": {
              "actor": "conference-attendee",
              "nodes": ["conference-website"]
            }
          }
        }
      ]
    }
    ```

    The starter file must itself validate (it is a valid CALM 1.2 document; the lab is not about errors in the starter — it is about completeness). Run `npx @finos/calm-cli validate -a labs/lab-02-conference-signup/starter/conference-signup.architecture.json -f pretty` and confirm hasErrors: false.

    **File 3 — `labs/lab-02-conference-signup/starter/README.md`**

    Short README pointing the learner at LAB.md and explaining the starter:
    ```markdown
    # Lab 2 Starter — Conference Signup Architecture

    This starter contains the minimum skeleton for the conference signup system. Two nodes (the actor and the webclient) and one relationship (interacts) are pre-defined.

    Your job (see ../LAB.md):

    1. Add the remaining 4 nodes: load-balancer (network), attendees (service), attendees-store (database), k8s-cluster (system).
    2. Add 4 more relationships: 3 connects (HTTPS, mTLS, JDBC) and 1 deployed-in.
    3. Add an interface to the attendees service.
    4. Add a top-level encryption-in-transit control.
    5. Run `npx @finos/calm-cli validate -a conference-signup.architecture.json -f pretty`.

    Follow LAB.md step by step. If you get stuck, check the hints. If you really get stuck, see ../solution/conference-signup.architecture.json.
    ```

    **File 4 — `labs/lab-02-conference-signup/LAB.md`**

    Full LAB.md with Learnforge frontmatter and Markdown walkthrough. Frontmatter:

    ```yaml
    ---
    id: lab-02-conference-signup
    title: "Write Your First CALM 1.2 Architecture by Hand"
    module: 2
    chapter: 2.7
    estimated_minutes: 35
    difficulty: 2
    prerequisites:
      - lab-00-on-ramp
    requires_docker: false
    workspace:
      cumulative: false
      base_dir: lab-02-conference-signup
      creates:
        - conference-signup.architecture.json
    objectives:
      - "Extend a CALM 1.2 starter file by adding 4 nodes and 4 relationships from a written spec"
      - "Add a freeform interface to a service node"
      - "Add an encryption-in-transit control at the architecture top level"
      - "Validate the final document with `npx @finos/calm-cli validate -a`"
      - "Visualize the result in CALM Studio web"
    steps:
      - id: step-1
        title: "Inspect the starter file"
        check:
          kind: file_state
          path: conference-signup.architecture.json
          exists: true
        hints:
          - "Open conference-signup.architecture.json in your editor — it should have 2 nodes and 1 relationship"
          - "Verify the $schema URL is https://calm.finos.org/release/1.2/meta/calm.json"

      - id: step-2
        title: "Add the load-balancer (network), attendees (service), attendees-store (database), and k8s-cluster (system) nodes"
        check:
          kind: command_regex
          command: "jq -r '.nodes | length' conference-signup.architecture.json"
          pattern: "^6$"
        hints:
          - "Each node needs all 4 required fields: unique-id, node-type, name, description"
          - "Node-type strings: network, service, database, system — exact spellings"
          - "Use kebab-case unique-ids (e.g. load-balancer, attendees-store)"

      - id: step-3
        title: "Add 4 more relationships: connects HTTPS (website to load-balancer), connects mTLS (load-balancer to attendees), connects JDBC (attendees to attendees-store), and deployed-in (k8s-cluster contains the 3 non-actor nodes)"
        check:
          kind: command_regex
          command: "jq -r '.relationships | length' conference-signup.architecture.json"
          pattern: "^5$"
        hints:
          - "protocol is a TOP-LEVEL field on the relationship, NOT inside the connects object"
          - "deployed-in uses `container: \"k8s-cluster\"` and `nodes: [...]` of unique-ids"
          - "Each relationship needs a unique-id and a description"

      - id: step-4
        title: "Add a freeform interface to the attendees service: unique-id attendees-api, name 'Attendees REST API', protocol HTTPS, port 8443"
        check:
          kind: command_regex
          command: "jq -r '.nodes[] | select(.\"unique-id\" == \"attendees\") | .interfaces | length' conference-signup.architecture.json"
          pattern: "^[1-9]"
        hints:
          - "Interfaces live in the node's `interfaces[]` array"
          - "Freeform interface = unique-id plus any additional properties (name, protocol, port)"

      - id: step-5
        title: "Add a top-level encryption-in-transit control"
        check:
          kind: command_regex
          command: "jq -r '.controls | keys[]' conference-signup.architecture.json"
          pattern: "encryption-in-transit"
        hints:
          - "controls is a keyed object at the top level"
          - "Each control has a description and a requirements[] array"
          - "Each requirement needs a requirement-url plus config or config-url"

      - id: step-6
        title: "Validate"
        check:
          kind: command_regex
          command: "npx @finos/calm-cli validate -a conference-signup.architecture.json -f pretty"
          pattern: "No issues found|hasErrors.*false"
        hints:
          - "Run: npx @finos/calm-cli validate -a conference-signup.architecture.json -f pretty"
          - "If you see errors: read the error path and field — the validator names the exact problem"
          - "Common causes: invented node-type (use only actor/ecosystem/system/service/database/network/ldap/webclient/data-asset), protocol nested inside connects instead of top-level, missing description on a node"

      - id: step-7
        title: "Visualize in CALM Studio"
        check:
          kind: ai_judge
          prompt: "The learner opened https://studio.calm.finos.org and imported their conference-signup.architecture.json. Ask: 'How many nodes do you see in the canvas? What relationships are visible?' A passing answer mentions seeing 6 nodes and at least mentions interacts, connects, and deployed-in by name."
        hints:
          - "Open https://studio.calm.finos.org"
          - "Use the Import button and select your conference-signup.architecture.json"
          - "You should see 6 boxes with arrows between them"

    resources:
      - title: "Chapter 2.7 — Building your first architecture"
        url: "../../content/module-02-calm-fundamentals/building-your-first-architecture.mdx"
      - title: "Chapter 2.2 — Nodes"
        url: "../../content/module-02-calm-fundamentals/nodes.mdx"
      - title: "Chapter 2.3 — Relationships"
        url: "../../content/module-02-calm-fundamentals/relationships.mdx"
      - title: "Chapter 2.4 — Interfaces"
        url: "../../content/module-02-calm-fundamentals/interfaces.mdx"
      - title: "Chapter 2.5 — Controls"
        url: "../../content/module-02-calm-fundamentals/controls.mdx"
      - title: "CALM 1.2 spec"
        url: "https://calm.finos.org/release/1.2/"
    ---
    ```

    Markdown body (after frontmatter):
    - `# Lab 2: Write Your First CALM 1.2 Architecture by Hand`
    - `## Goal` — one sentence: "Extend the conference signup starter into a complete, validated CALM 1.2 architecture by hand."
    - `## Setup` — `cd labs/lab-02-conference-signup/starter` and copy the starter file into your working area; no Docker needed
    - `## Walkthrough` — numbered steps matching the YAML step IDs 1-7. Each step in human-readable form with the same content as the YAML title but expanded with detail about what to add and why. Show expected output where relevant.
    - `## Stretch goals (optional)` — 2-3 ideas: (a) add a second interface to attendees using the definition-url form; (b) add a per-relationship mTLS control on the load-balancer→attendees connects edge; (c) draft a threat-model decorator document at `conference-signup.threat-model.decorator.json` targeting your architecture file
    - `## Solution` — "See `solution/conference-signup.architecture.json`. Resist the urge to peek until you've tried."

    Final step: run `bash scripts/validate-calm.sh` from repo root. It picks up the solution file via the recursive find on `code-examples/` (which it currently searches) — BUT lab solutions are NOT under code-examples/. Confirm scripts/validate-calm.sh searches `code-examples/` only; if so, also run `npx @finos/calm-cli validate -a labs/lab-02-conference-signup/solution/conference-signup.architecture.json` directly and confirm hasErrors:false. Both starter and solution must validate.
  </action>
  <verify>
    <automated>test -d labs/lab-02-conference-signup/starter &amp;&amp; test -d labs/lab-02-conference-signup/solution || { echo "FAIL: missing lab dirs"; exit 1; }; test -f labs/lab-02-conference-signup/LAB.md || { echo "FAIL: missing LAB.md"; exit 1; }; test -f labs/lab-02-conference-signup/starter/conference-signup.architecture.json || { echo "FAIL: missing starter file"; exit 1; }; test -f labs/lab-02-conference-signup/starter/README.md || { echo "FAIL: missing starter README"; exit 1; }; test -f labs/lab-02-conference-signup/solution/conference-signup.architecture.json || { echo "FAIL: missing solution file"; exit 1; }; head -3 labs/lab-02-conference-signup/LAB.md | head -1 | grep -q '^---' || { echo "FAIL: LAB.md missing YAML frontmatter"; exit 1; }; grep -q 'id: lab-02-conference-signup' labs/lab-02-conference-signup/LAB.md || { echo "FAIL: LAB.md missing correct id"; exit 1; }; STARTER_NODES=$(jq -r '.nodes | length' labs/lab-02-conference-signup/starter/conference-signup.architecture.json); test "$STARTER_NODES" = "2" || { echo "FAIL: starter must have exactly 2 nodes (got $STARTER_NODES)"; exit 1; }; SOLN_NODES=$(jq -r '.nodes | length' labs/lab-02-conference-signup/solution/conference-signup.architecture.json); test "$SOLN_NODES" = "6" || { echo "FAIL: solution must have exactly 6 nodes (got $SOLN_NODES)"; exit 1; }; SOLN_RELS=$(jq -r '.relationships | length' labs/lab-02-conference-signup/solution/conference-signup.architecture.json); test "$SOLN_RELS" = "5" || { echo "FAIL: solution must have exactly 5 relationships (got $SOLN_RELS)"; exit 1; }; jq -e '.controls."encryption-in-transit"' labs/lab-02-conference-signup/solution/conference-signup.architecture.json &gt; /dev/null || { echo "FAIL: solution missing encryption-in-transit control"; exit 1; }; npx @finos/calm-cli validate -a labs/lab-02-conference-signup/starter/conference-signup.architecture.json 2&gt;&amp;1 | grep -q "hasErrors.*false\|No issues found" || { echo "FAIL: starter does not validate"; exit 1; }; npx @finos/calm-cli validate -a labs/lab-02-conference-signup/solution/conference-signup.architecture.json 2&gt;&amp;1 | grep -q "hasErrors.*false\|No issues found" || { echo "FAIL: solution does not validate"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - Directory `labs/lab-02-conference-signup/` exists with subdirs `starter/` and `solution/`
    - `LAB.md` exists at `labs/lab-02-conference-signup/LAB.md` and begins with YAML frontmatter (`---` on first line)
    - LAB.md frontmatter contains exact `id: lab-02-conference-signup`, `module: 2`, `chapter: 2.7`, `estimated_minutes: 35`, `requires_docker: false`
    - LAB.md frontmatter `steps:` array has at least 6 steps (target 7) with `id`, `title`, `check`, `hints` fields
    - LAB.md frontmatter `resources:` includes a link to `../../content/module-02-calm-fundamentals/building-your-first-architecture.mdx`
    - LAB.md Markdown body has sections: `# Lab 2:`, `## Goal`, `## Setup`, `## Walkthrough`, `## Stretch goals`, `## Solution`
    - `starter/conference-signup.architecture.json` exists, is valid JSON, has exactly 2 nodes and 1 relationship, passes `npx @finos/calm-cli validate -a` (starter is intentionally minimal but spec-valid)
    - `starter/README.md` exists and mentions LAB.md and the solution path
    - `solution/conference-signup.architecture.json` exists, is byte-identical or content-identical to `code-examples/module-02-calm-fundamentals/conference-signup.architecture.json` (6 nodes, 5 relationships, 1 interface on attendees, 1 encryption-in-transit control, metadata block)
    - Solution file passes `npx @finos/calm-cli validate -a` with hasErrors:false
    - No `container`/`component`/`microservice` node-types in either starter or solution
  </acceptance_criteria>
  <done>
    Lab 2 is complete and runnable end-to-end: a fresh learner can copy the starter, follow the 7 steps in LAB.md, hand-write the missing 4 nodes / 4 relationships / 1 interface / 1 control, validate with calm CLI, and verify against the provided solution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Module 2 quiz (18-21 questions across all 7 chapters)</name>
  <files>
    quizzes/module-02-calm-fundamentals.yaml
  </files>
  <read_first>
    - quizzes/module-01-case-for-aac.yaml (full — pattern reference, especially question type mix and reference_section formatting)
    - quizzes/module-00-on-ramp.yaml (full — pattern reference)
    - docs-meta/QUIZ-FORMAT.md (full — schema, question types, authoring rules)
    - scripts/lint-quizzes.sh (full — exact validation requirements)
    - content/module-02-calm-fundamentals/calm-specification.mdx (chapter 2.1 — to identify section anchors for reference_section fields)
    - content/module-02-calm-fundamentals/nodes.mdx (chapter 2.2)
    - content/module-02-calm-fundamentals/relationships.mdx (chapter 2.3)
    - content/module-02-calm-fundamentals/interfaces.mdx (chapter 2.4)
    - content/module-02-calm-fundamentals/controls.mdx (chapter 2.5)
    - content/module-02-calm-fundamentals/decorators.mdx (chapter 2.6)
    - content/module-02-calm-fundamentals/building-your-first-architecture.mdx (chapter 2.7)
    - .planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md (section "Quiz Design")
  </read_first>
  <action>
    Author `quizzes/module-02-calm-fundamentals.yaml` — the largest quiz in the course. Target 21 questions distributed 3/4/3/3/3/3/2 across chapters 2.1 through 2.7 (lighter on 2.7 because the synthesis is verified by the lab; Chapter 2.2 has 4 questions to cover the actor-vs-webclient distinction per RESEARCH.md pitfall 6). Mix question types: multiple_choice (majority), short_answer, and code_completion (use for "complete the missing field" style questions on JSON fragments).

    Top-level YAML:
    ```yaml
    module: 2
    title: "Module 2: CALM Fundamentals"
    ```

    Then a `chapters:` array with 7 entries, each with `chapter`, `slug`, `title`, `questions[]`.

    Per chapter, follow this distribution and concept coverage (drawn from RESEARCH.md "Quiz Design" section, refined for actual chapter content):

    **Chapter 2.1 — slug `calm-specification`, title "The CALM specification" (3 questions)**
    - `q2.1.1` multiple_choice: Which $schema URL identifies a CALM 1.2 document? Correct: `https://calm.finos.org/release/1.2/meta/calm.json`. Distractors: 1.0-rc1, 1.1, calm.io. Explanations: 1.2 is the current stable release; older URLs identify older releases with different interface schemas.
    - `q2.1.2` multiple_choice: Which four fields are required on every CALM node? Correct: `unique-id, node-type, name, description`. Distractors: alternatives missing description or interfaces.
    - `q2.1.3` short_answer: What command validates a CALM architecture file? accepted_answers: `npx @finos/calm-cli validate`, `npx @finos/calm-cli validate -a`. case_sensitive: false.

    **Chapter 2.2 — slug `nodes`, title "Nodes: modeling system components" (4 questions)**
    - `q2.2.1` multiple_choice: Which of these is NOT a CALM 1.2 core node type? Correct: `container` (or `component` or `microservice`). Distractors: actor, service, database (which ARE core types). Explanation reinforces the 9-type canon.
    - `q2.2.2` multiple_choice: Which node type best models a Kafka topic where event payloads are persisted? Correct: `data-asset`. Distractors: database, service, ecosystem (each with explanation of why not).
    - `q2.2.3` code_completion: snippet showing a partial node with `"node-type": "<BLANK>"` and other fields suggesting a corporate LDAP directory; accepted_answers: `ldap`. Explanation reinforces that ldap is for directory services specifically.
    - `q2.2.4` multiple_choice: A mobile app that human users launch to submit loan applications — which CALM node type best models it? Correct: `actor` (models any human-operated client — web browsers, mobile apps, CLI tools). Distractors: `service` (runs autonomously, not human-operated), `webclient` (browser-only, not native mobile app), `system` (logical boundary, not a client). Explanation: "actor models any human-operated client. webclient is specifically for browser-based frontends — if it is a native mobile or desktop app, actor is the correct type." reference_section: anchor to the actor-vs-webclient subsection of Chapter 2.2 ().

    **Chapter 2.3 — slug `relationships`, title "Relationships: connecting the dots" (3 questions)**
    - `q2.3.1` multiple_choice: Where does the `protocol` field belong on a `connects` relationship? Correct: top-level (sibling of relationship-type). Distractors: inside connects object, on the destination node, on the source interface. This is THE pitfall from RESEARCH.md pitfall 2.
    - `q2.3.2` code_completion: snippet showing an `interacts` relationship with `"actor": "<BLANK>"`; the snippet's other fields indicate a human user. accepted_answers: the actor unique-id (e.g. `analyst` or whatever the snippet establishes). Explanation reinforces that the actor field must be a node with `node-type: actor`.
    - `q2.3.3` short_answer: List any 3 of the 12 CALM protocol enum values, comma-separated. `accepted_answers` must list valid protocol triplets drawn from the enum {HTTP, HTTPS, FTP, SFTP, JDBC, WebSocket, SocketIO, LDAP, AMQP, TLS, mTLS, TCP}. Representative accepted answers: "HTTPS, mTLS, JDBC" / "HTTPS, TCP, AMQP" / "HTTP, HTTPS, JDBC" / "FTP, SFTP, TLS" / "WebSocket, AMQP, mTLS". Use `case_sensitive: false` and include at least 5-6 common valid triplets in `accepted_answers[]`. Do NOT use an `accepted_patterns` field — the short_answer type uses only `accepted_answers[]`.

    **Chapter 2.4 — slug `interfaces`, title "Interfaces: describing communication contracts" (3 questions)**
    - `q2.4.1` multiple_choice: What is the difference between an interface and a relationship in CALM? Correct: interface = technical contract on a node; relationship = logical connectivity between nodes. Distractors get this swapped or conflate them.
    - `q2.4.2` multiple_choice: In CALM 1.2, which of these is NO LONGER a valid interface type? Correct: `url-interface` (or `host-port-interface`). Distractors: freeform interface-type, interface-definition with definition-url, the modern forms. Explanation reinforces the 1.0-rc1 deprecation.
    - `q2.4.3` short_answer: In an interface-definition (formal form), what does `definition-url` point to? accepted_answers: `JSON Schema`, `JSON schema`, `external schema`, `a schema that defines what config must look like`. case_sensitive: false.

    **Chapter 2.5 — slug `controls`, title "Controls: encoding non-functional requirements" (3 questions)**
    - `q2.5.1` multiple_choice: Where can a CALM control attach? Correct: any of top-level architecture, a node, a relationship, or a flow. Distractors limit it to one location.
    - `q2.5.2` multiple_choice: In a control's `requirements[]` array, what is the role of `requirement-url`? Correct: it points to a JSON Schema describing what the requirement EXPECTS (the contract). Distractors: where to download the implementation, the audit log URL, the CCC registration.
    - `q2.5.3` short_answer: Name the FINOS catalog that provides canonical `requirement-url` schemas for security and operational controls. accepted_answers: `FINOS CCC`, `FINOS Common Controls Catalog`, `Common Controls Catalog`, `CCC`. case_sensitive: false.

    **Chapter 2.6 — slug `decorators`, title "Decorators: cross-cutting concerns as separate documents" (3 questions)**
    - `q2.6.1` multiple_choice: Which statement about decorators in CALM 1.2 is TRUE? Correct: decorators are separate JSON documents that reference an architecture via `target[]`. Distractors: decorators are inline node properties, decorators replace the controls block, decorators are required on every architecture.
    - `q2.6.2` multiple_choice: What is the difference between a decorator's `target[]` and `applies-to[]`? Correct: `target[]` lists architecture documents the decorator points at; `applies-to[]` lists specific node/relationship unique-ids within those documents. Distractors swap or conflate them.
    - `q2.6.3` short_answer: Give one example of a real-world `type` value for a decorator (lowercase, single word or hyphen-separated). accepted_answers: `threat`, `governance`, `aigf`, `saif`, `pattern`, `deployment-info`, `threat-model`. case_sensitive: false.

    **Chapter 2.7 — slug `building-your-first-architecture`, title "Building your first architecture in CALM" (2 questions)**
    - `q2.7.1` multiple_choice: What does `npx @finos/calm-cli validate -a` check? Correct: JSON-Schema conformance of the architecture document (shape, required fields, enum values). Distractors: network reachability, runtime deployment status, semantic correctness of relationships.
    - `q2.7.2` code_completion: snippet showing a top-level `connects` relationship where the learner must place `protocol` correctly. Show the relationship with `"<BLANK>": "HTTPS"` either at the top level (correct position) or inside the connects block — the blank is the field NAME. accepted_answers: `protocol`. Explanation reinforces the placement rule from pitfall 2.

    Total: 21 questions (3/4/3/3/3/3/2 across chapters 2.1–2.7).

    For every question:
    - `id` matches `q2.<chapter>.<n>` pattern (e.g. q2.1.1, q2.6.3)
    - `reference_section` is a path to the chapter MDX file in form `../content/module-02-calm-fundamentals/<slug>.mdx` (per the prior-phase pattern in module-01-case-for-aac.yaml). Anchor (`#fragment`) optional but encouraged — only use anchors that match an H3 heading you actually wrote in Wave 1/2 chapters.
    - Every multiple_choice has exactly one `correct: true` and every option has an `explanation`
    - Every short_answer has `accepted_answers[]` and `case_sensitive: false` (default)
    - Every code_completion has `snippet`, `accepted_answers[]`, and `explanation`
    - Every question has an explanation/feedback path for wrong answers

    After authoring, run `bash scripts/lint-quizzes.sh`. The quiz file MUST pass — exit 0, every check PASS. If it fails, fix the YAML.

    Note on reference_section paths: scripts/lint-quizzes.sh only checks that the field is non-empty (per the script's source). The path must point at a real existing chapter file for site-rendering correctness; check each path with `ls` before commit. Anchors are not validated by the linter so they are aspirational only.
  </action>
  <verify>
    <automated>test -f quizzes/module-02-calm-fundamentals.yaml || { echo "FAIL: missing quiz file"; exit 1; }; bash scripts/lint-quizzes.sh 2&gt;&amp;1 | tee /tmp/m02-lint.log; grep -E "^FAIL" /tmp/m02-lint.log &amp;&amp; exit 1; QCOUNT=$(python3 -c "import yaml; d=yaml.safe_load(open('quizzes/module-02-calm-fundamentals.yaml')); print(sum(len(c['questions']) for c in d['chapters']))"); test "$QCOUNT" -ge 18 || { echo "FAIL: quiz has $QCOUNT questions, need 18+"; exit 1; }; test "$QCOUNT" -le 21 || { echo "FAIL: quiz has $QCOUNT questions, max 21"; exit 1; }; CHAPTERS=$(python3 -c "import yaml; d=yaml.safe_load(open('quizzes/module-02-calm-fundamentals.yaml')); print(len(d['chapters']))"); test "$CHAPTERS" = "7" || { echo "FAIL: quiz must cover 7 chapters, got $CHAPTERS"; exit 1; }; for slug in calm-specification nodes relationships interfaces controls decorators building-your-first-architecture; do grep -q "slug: $slug" quizzes/module-02-calm-fundamentals.yaml || { echo "FAIL: quiz missing chapter slug $slug"; exit 1; }; done; for slug in calm-specification nodes relationships interfaces controls decorators building-your-first-architecture; do test -f "content/module-02-calm-fundamentals/${slug}.mdx" || { echo "FAIL: reference target ${slug}.mdx does not exist"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `quizzes/module-02-calm-fundamentals.yaml` exists and is valid YAML
    - Top-level fields: `module: 2`, `title: "Module 2: CALM Fundamentals"`, `chapters[]`
    - `chapters[]` has exactly 7 entries with slugs `calm-specification`, `nodes`, `relationships`, `interfaces`, `controls`, `decorators`, `building-your-first-architecture`
    - Total question count is between 18 and 21 (inclusive)
    - Every question id matches `^q2\.[1-7]\.[1-9]$` (regex)
    - Every question has `reference_section` pointing at `../content/module-02-calm-fundamentals/<slug>.mdx` and that file exists
    - Every multiple_choice question has exactly one option with `correct: true` and every option has `explanation`
    - At least 2 code_completion questions exist
    - At least 4 short_answer questions exist
    - `bash scripts/lint-quizzes.sh` exits 0 with zero FAIL lines for the new quiz file
  </acceptance_criteria>
  <done>
    Module 2 quiz authored with 18-21 questions across all 7 chapters in the canonical YAML schema; passes `lint-quizzes.sh`; every reference_section points at a real chapter file.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lab learner workspace → calm validate CLI | Learner-supplied JSON validated locally; validator output drives lab progress checks |
| Quiz YAML → Docusaurus MDX renderer | Static data rendered by component; user-controlled author content only |
| Lab solution → public reference | Public anchor that learners compare against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | Lab 2 solution drifts from anchor code example | mitigate | Solution file is a byte-identical copy of `code-examples/module-02-calm-fundamentals/conference-signup.architecture.json`. Acceptance criteria enforces matching node/relationship counts and key features (interface, control) |
| T-03-10 | Spoofing | Quiz reference_section points at non-existent chapter | mitigate | Acceptance criteria runs `test -f` on each chapter file for every slug used in the quiz YAML before declaring done |
| T-03-11 | Information Disclosure | LAB.md hints reveal too much, defeating learning | accept | Hints follow LAB-FORMAT.md tiering (3 hints max per step, gentle → partial → directive). The Solution section explicitly says "resist the urge to peek". Educational tradeoff, not a security issue |
| T-03-12 | Denial of Service | `ai_judge` step type in step-7 requires LLM call | accept | Used sparingly per LAB-FORMAT.md guidance (only the visualisation step). Learner can self-verify by inspecting the canvas; ai_judge is for Learnforge-style automated grading |
| T-03-SC | Tampering | npm package installs | mitigate | No new packages. `npx @finos/calm-cli` and python3+yaml already established |

</threat_model>

<verification>

```bash
# 1. Lab directory and files exist
test -d labs/lab-02-conference-signup
test -f labs/lab-02-conference-signup/LAB.md
test -f labs/lab-02-conference-signup/starter/conference-signup.architecture.json
test -f labs/lab-02-conference-signup/starter/README.md
test -f labs/lab-02-conference-signup/solution/conference-signup.architecture.json

# 2. Lab solution validates
npx @finos/calm-cli validate -a labs/lab-02-conference-signup/solution/conference-signup.architecture.json -f pretty
# expect: hasErrors: false

# 3. Lab starter validates (it's minimal but spec-valid)
npx @finos/calm-cli validate -a labs/lab-02-conference-signup/starter/conference-signup.architecture.json -f pretty

# 4. Quiz file passes lint
bash scripts/lint-quizzes.sh
# expect: exit 0, no FAIL lines

# 5. Quiz covers all 7 chapters
python3 -c "import yaml; d=yaml.safe_load(open('quizzes/module-02-calm-fundamentals.yaml')); slugs=[c['slug'] for c in d['chapters']]; assert set(slugs) == {'calm-specification','nodes','relationships','interfaces','controls','decorators','building-your-first-architecture'}"

# 6. Quiz has 18-21 questions total
python3 -c "import yaml; d=yaml.safe_load(open('quizzes/module-02-calm-fundamentals.yaml')); n=sum(len(c['questions']) for c in d['chapters']); assert 18 <= n <= 21, f'got {n}'"
```

</verification>

<success_criteria>

This plan succeeds when:

1. `labs/lab-02-conference-signup/` exists with LAB.md, starter/, and solution/
2. LAB.md has Learnforge-compatible YAML frontmatter with at least 6 steps using `command_regex`, `file_state`, and at most 1 `ai_judge` step
3. Lab starter file validates as CALM 1.2 with exactly 2 nodes and 1 relationship
4. Lab solution file validates as CALM 1.2 with exactly 6 nodes, 5 relationships, 1 interface on attendees service, 1 encryption-in-transit control (matches the anchor code example byte-for-byte or content-for-content)
5. `quizzes/module-02-calm-fundamentals.yaml` exists with 18-21 questions, distributed across all 7 chapters
6. Quiz passes `lint-quizzes.sh` with exit 0
7. Every quiz `reference_section` points at an existing chapter file under `content/module-02-calm-fundamentals/`
8. At least one quiz question per chapter teaches a key pitfall (protocol placement for 2.3, invented node-types for 2.2, decorator separation for 2.6, etc.)

</success_criteria>

<output>
Create `.planning/phases/03-module-2-calm-fundamentals/03-03-SUMMARY.md` when done. Include: lab files created, lab starter+solution validation results, lint-quizzes.sh output, total quiz question count, any deviations from plan.
</output>
