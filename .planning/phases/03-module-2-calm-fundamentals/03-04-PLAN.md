---
phase: 03-module-2-calm-fundamentals
plan: "04"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - slides/module-02-calm-fundamentals.md
  - docs-meta/cheatsheets/module-02-cheatsheet.md
autonomous: true
requirements: [SLIDE-02, CODE-02]
tags: [module-02, slide-deck, cheatsheet, marp, reference]

must_haves:
  truths:
    - "Module 2 slide deck exists, is Marp-renderable, and covers all 7 chapters"
    - "Slide deck includes a 9-slide node type gallery — one slide per CALM core node type"
    - "Module 2 cheatsheet exists as a single-page printable Markdown reference"
    - "Cheatsheet documents all 9 node types, all 5 relationship types, the 12-value protocol enum, interface forms, control skeleton, and the $schema URL"
  artifacts:
    - path: "slides/module-02-calm-fundamentals.md"
      provides: "Module 2 slide deck (Marp Markdown), 30-40 slides with speaker notes"
      contains: "marp: true"
      min_lines: 300
    - path: "docs-meta/cheatsheets/module-02-cheatsheet.md"
      provides: "Module 2 1-page printable cheatsheet"
      contains: "https://calm.finos.org/release/1.2/meta/calm.json"
      min_lines: 60
  key_links:
    - from: "slides/module-02-calm-fundamentals.md"
      to: "illustrations/exported/m02-*.svg"
      via: "inline Markdown image"
      pattern: "../illustrations/exported/m02-"
    - from: "docs-meta/cheatsheets/module-02-cheatsheet.md"
      to: "https://calm.finos.org/release/1.2/meta/calm.json"
      via: "literal $schema reference"
      pattern: "calm.finos.org/release/1.2"
---

## Phase Goal

**As a** workshop instructor or self-paced learner who has finished Module 2's chapters and lab, **I want to** present Module 2 from a slide deck and keep a 1-page printable cheatsheet at my desk, **so that** I can teach the material or recall the vocabulary at a glance without rereading 7 chapters.

<objective>
Deliver the two derived artifacts for Module 2: the Marp slide deck (SLIDE-02) and the printable cheatsheet (CODE-02). Both derive their content from the 7 chapters and 13 illustrations created in plans 03-01 and 03-02 — text-first workflow applied.

Purpose: Slide deck enables instructor delivery and conference demos. Cheatsheet is the at-a-glance reference that experienced practitioners keep open in a second window. Both leverage existing chapter content — this plan does no new conceptual authoring, only restructuring for the target format.

Output: 1 Marp Markdown file (`slides/module-02-calm-fundamentals.md`) and 1 cheatsheet Markdown file (`docs-meta/cheatsheets/module-02-cheatsheet.md`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md
@CLAUDE.md
@docs-meta/STYLE-GUIDE.md

# Prior phase outputs (consume these)
@.planning/phases/03-module-2-calm-fundamentals/03-01-SUMMARY.md
@.planning/phases/03-module-2-calm-fundamentals/03-02-SUMMARY.md
@content/module-02-calm-fundamentals/calm-specification.mdx
@content/module-02-calm-fundamentals/nodes.mdx
@content/module-02-calm-fundamentals/relationships.mdx
@content/module-02-calm-fundamentals/interfaces.mdx
@content/module-02-calm-fundamentals/controls.mdx
@content/module-02-calm-fundamentals/decorators.mdx
@content/module-02-calm-fundamentals/building-your-first-architecture.mdx
@code-examples/module-02-calm-fundamentals/conference-signup.architecture.json

# Pattern reference — prior slide deck
@slides/module-01-case-for-aac.md

<interfaces>
<!-- Patterns established by SLIDE-01 in Phase 2 -->

Marp Markdown structure (from slides/module-01-case-for-aac.md):
```
---
marp: true
theme: default
paginate: true
---

# <Module title>

## <Subtitle or tagline>

<!-- Speaker note: <speaker guidance for this slide> -->

---

## <Slide 2 title>

- Bullet 1
- Bullet 2

<!-- Speaker note: <speaker guidance> -->

---
```

Slide separators: `---` on its own line between every slide. Speaker notes go inside HTML comments `<!-- Speaker note: ... -->` immediately after the slide content.

Slide image references use the same relative path as MDX chapters: `![alt](../illustrations/exported/m02-<name>.svg)`. Marp renders these.

Cheatsheet format (no prior file — design from RESEARCH.md "Cheatsheet Design"):
A single Markdown file with no frontmatter (it is a printable reference, not a Docusaurus page initially). Use H2 section headers, tables, and fenced code blocks. Target single A4/Letter page when rendered.

Sections (per RESEARCH.md):
1. Node Types (9) — table: name | use for | never use for
2. Relationship Types (5) — table: type | required fields | example use
3. Protocol Enum — comma-separated list
4. Node Required Fields
5. Relationship Required Fields
6. Interface Quick Ref — two forms shown as fenced json snippets
7. Control Quick Ref — fenced json skeleton
8. $schema URL — full literal string
9. CLI Quick Ref — validate command
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Module 2 slide deck (Marp Markdown, 30-40 slides)</name>
  <files>
    slides/module-02-calm-fundamentals.md
  </files>
  <read_first>
    - slides/module-01-case-for-aac.md (full — pattern: Marp frontmatter, slide separator convention, speaker note style, image reference format)
    - content/module-02-calm-fundamentals/calm-specification.mdx (chapter 2.1 — extract TL;DR + 2-3 H3 subsections for slides)
    - content/module-02-calm-fundamentals/nodes.mdx (chapter 2.2 — extract per-node-type sections for the gallery)
    - content/module-02-calm-fundamentals/relationships.mdx (chapter 2.3 — extract per-relationship-type sections)
    - content/module-02-calm-fundamentals/interfaces.mdx (chapter 2.4)
    - content/module-02-calm-fundamentals/controls.mdx (chapter 2.5)
    - content/module-02-calm-fundamentals/decorators.mdx (chapter 2.6)
    - content/module-02-calm-fundamentals/building-your-first-architecture.mdx (chapter 2.7 — extract build steps for slides 28-35)
    - illustrations/INVENTORY.md (confirm all 13 m02 SVG paths)
    - .planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md (section "Slide Deck Design" — 38 slide structure target)
  </read_first>
  <action>
    Author `slides/module-02-calm-fundamentals.md` as a Marp Markdown deck of 35-38 slides covering all 7 chapters. Mirror the format of `slides/module-01-case-for-aac.md` exactly: Marp frontmatter, `---` slide separators, HTML-comment speaker notes after each slide.

    Frontmatter:
    `marp: true` / `theme: default` / `paginate: true` (all inside a YAML frontmatter block delimited by `---`).

    Slide structure (target 38 slides — adjust within 30-40 range):

    Slide 1: Title — `# Module 2: CALM Fundamentals` / `## *"From vocabulary to your first validated architecture."*` / speaker note positioning Module 2 as the reference-grade vocabulary module.

    Slides 2-4: Chapter 2.1 — The CALM specification (3 slides):
    - Slide 2 "The CALM document anatomy" — bullet list of top-level fields; inline image `../illustrations/exported/m02-calm-document-structure.svg`
    - Slide 3 "The $schema URL is the version marker" — exact 1.2 URL highlighted; warning about 1.0-rc1 and 1.1
    - Slide 4 "Schema conformance vs spec discipline" — validator accepts arbitrary node-type strings; course canon is the 9 types

    Slides 5-13: Chapter 2.2 — Node type gallery (9 slides — ONE PER NODE TYPE; this is the visual signature of the deck per RESEARCH.md):
    - Slide 5 `actor` — one-line definition + 2 real-world examples + speaker note. Inline image `../illustrations/exported/m02-node-types-gallery.svg` (the same gallery image may appear on multiple slides — Marp renders it on every slide it appears, which is fine).
    - Slide 6 `ecosystem` — same template
    - Slide 7 `system` — same
    - Slide 8 `service` — same
    - Slide 9 `database` — same
    - Slide 10 `network` — same
    - Slide 11 `webclient` — same
    - Slide 12 `ldap` — same
    - Slide 13 `data-asset` — same. End the gallery with a speaker note saying "you have now seen all 9 — every CALM architecture uses some subset of these."

    Optionally insert a transition slide before the gallery: "All 9 types — at a glance" with the gallery image full-bleed and the 9 names listed.

    Slides 14-18: Chapter 2.3 — Relationships (5 slides):
    - Slide 14 "The 5 relationship types" — image `../illustrations/exported/m02-relationship-types.svg`, bullet list of the 5 type names with one-line meanings
    - Slide 15 "connects — node to node with a protocol" — fenced json fragment showing a connects relationship with protocol at top level; image `../illustrations/exported/m02-connects-vs-interacts.svg`
    - Slide 16 "interacts — actor pulls the trigger" — fenced json fragment; emphasis that actor field must reference an actor-typed node
    - Slide 17 "deployed-in vs composed-of" — side-by-side bullets; image `../illustrations/exported/m02-deployed-in-composed-of.svg`
    - Slide 18 "Protocol enum (12 values)" — the full list: HTTP, HTTPS, FTP, SFTP, JDBC, WebSocket, SocketIO, LDAP, AMQP, TLS, mTLS, TCP. Speaker note: protocol is a top-level field on the relationship, not nested inside the connects object.

    Slides 19-21: Chapter 2.4 — Interfaces (3 slides):
    - Slide 19 "Interface vs relationship" — bullet contrast; image `../illustrations/exported/m02-interface-anatomy.svg`
    - Slide 20 "Freeform form (interface-type)" — fenced json showing freeform interface from with-interfaces.architecture.json
    - Slide 21 "Formal form (interface-definition)" — fenced json showing definition-url form; brief note that 1.0-rc1 url-interface and host-port-interface types are gone

    Slides 22-24: Chapter 2.5 — Controls (3 slides):
    - Slide 22 "Controls — testable compliance, not prose" — image `../illustrations/exported/m02-control-anatomy.svg`
    - Slide 23 "Control anatomy" — fenced json skeleton showing controls keyed object with description and requirements[]
    - Slide 24 "Where controls attach" — top-level / per-node / per-relationship / per-flow; FINOS CCC as canonical requirement-url source

    Slides 25-27: Chapter 2.6 — Decorators (3 slides):
    - Slide 25 "Decorators are SEPARATE documents" — image `../illustrations/exported/m02-decorator-external-overlay.svg`
    - Slide 26 "Decorator anatomy" — fenced json showing unique-id / type / target / applies-to / data
    - Slide 27 "Common decorator types" — threat, governance, aigf, saif, pattern, deployment-info

    Slides 28-35: Chapter 2.7 — Building the conference signup architecture (8 slides — the build sequence):
    - Slide 28 "Step 1: The skeleton" — fenced json with $schema, unique-id, name, description, empty nodes[], empty relationships[]
    - Slide 29 "Step 2: Add 6 nodes" — image `../illustrations/exported/m02-conference-signup-step1-nodes.svg`; bullet list of the 6 nodes
    - Slide 30 "Step 3: Add the relationships" — image `../illustrations/exported/m02-conference-signup-step2-relationships.svg`; bullet list of 5 relationships
    - Slide 31 "Step 3 detail: connects with protocol at the TOP LEVEL" — code excerpt showing correct placement (a callout reinforcing the protocol-placement pitfall)
    - Slide 32 "Step 4: Add an interface on attendees" — fenced json showing the attendees-api freeform interface
    - Slide 33 "Step 5: Add encryption-in-transit control" — fenced json fragment
    - Slide 34 "Step 6: Validate" — command line: `npx @finos/calm-cli validate -a conference-signup.architecture.json -f pretty`; expected output line
    - Slide 35 "Step 7: Visualize in Studio" — image `../illustrations/exported/m02-conference-signup-step3-complete.svg`; speaker note about CALM Studio web import flow

    Slides 36-38: Closing (3 slides):
    - Slide 36 "Lab 2 — your turn" — pointer to `labs/lab-02-conference-signup/LAB.md`; speaker note: workshop instructors give learners 35 minutes. **Important execution note:** Author slide 36 speaker notes generically — the Lab 2 LAB.md (created in parallel plan 03-03) may not exist yet during this plan's execution. Do NOT attempt to read `labs/lab-02-conference-signup/LAB.md`. Instead, use the conference-signup scenario details already present in the RESEARCH.md context (6 nodes: conference-attendee, conference-website, load-balancer, attendees, attendees-store, k8s-cluster; steps: inspect starter, add nodes, add relationships, add interface, add control, validate, visualize) to craft the speaker note without reading LAB.md.
    - Slide 37 "Cheatsheet" — pointer to `docs-meta/cheatsheets/module-02-cheatsheet.md` (or a future static path); 1-page printable
    - Slide 38 "Module 2 summary" — recap bullets: 9 node types, 5 relationship types, 12 protocols, 2 interface forms, controls, decorators, validation; what is next (Module 3 — the ecosystem)

    Total: 38 slides. Acceptable range 30-40 per SLIDE-02 requirement.

    Every slide must have a speaker note (HTML comment block immediately after the slide content). Speaker notes should be 2-4 sentences each, providing the instructor with context, FSI examples, transition phrases, or questions to ask the audience.

    Image references use relative path `../illustrations/exported/m02-*.svg`. Marp will render these against the placeholder SVGs created in plans 03-01 and 03-02. When user authors real Excalidraw illustrations later, the deck automatically picks up the real SVGs.

    Code excerpts in slides MUST come from the validated code examples (conference-signup, node-types-reference, relationship-types-reference, with-interfaces, with-controls). Do not invent JSON fragments. STYLE-GUIDE.md "no truncation" rule applies — but slides have limited space, so use short focused excerpts (4-12 lines per code block) and reference the full file at the slide footer or speaker note: "Full file: code-examples/module-02-calm-fundamentals/<name>.architecture.json".

    No "Chapter 2.N" prose on slides — refer to chapters by title only ("Nodes", "Relationships", etc.), matching the STYLE-GUIDE.md cross-reference rule.
  </action>
  <verify>
    <automated>test -f slides/module-02-calm-fundamentals.md || { echo "FAIL: missing slide deck"; exit 1; }; head -5 slides/module-02-calm-fundamentals.md | grep -q "^marp: true" || { echo "FAIL: missing marp frontmatter"; exit 1; }; SLIDE_COUNT=$(grep -c '^---$' slides/module-02-calm-fundamentals.md); test "$SLIDE_COUNT" -ge 30 || { echo "FAIL: only $SLIDE_COUNT slide separators (need 30+)"; exit 1; }; test "$SLIDE_COUNT" -le 45 || { echo "FAIL: too many slide separators ($SLIDE_COUNT, max 45 incl. frontmatter)"; exit 1; }; SPEAKER_NOTES=$(grep -c 'Speaker note:' slides/module-02-calm-fundamentals.md); test "$SPEAKER_NOTES" -ge 25 || { echo "FAIL: only $SPEAKER_NOTES speaker notes"; exit 1; }; IMAGE_REFS=$(grep -cE '!\[.*\]\(\.\./illustrations/exported/m02-' slides/module-02-calm-fundamentals.md); test "$IMAGE_REFS" -ge 6 || { echo "FAIL: only $IMAGE_REFS m02 SVG references in deck (need 6+)"; exit 1; }; for nt in actor ecosystem system service database network webclient ldap data-asset; do grep -qE "\\\`$nt\\\`|^### $nt|^## $nt" slides/module-02-calm-fundamentals.md || { echo "FAIL: node type $nt not present"; exit 1; }; done; grep -E "Chapter 2\.[0-9]" slides/module-02-calm-fundamentals.md &amp;&amp; { echo "FAIL: chapter-number prose found"; exit 1; } || true; grep -q "https://calm.finos.org/release/1.2" slides/module-02-calm-fundamentals.md || { echo "FAIL: missing 1.2 schema URL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `slides/module-02-calm-fundamentals.md` exists with Marp frontmatter (`marp: true`, `theme: default`, `paginate: true`)
    - Slide count is 30-40 (counted via `---` separators that occur AFTER the frontmatter close — total `---` count between 31 and 41 including the frontmatter close)
    - At least 25 speaker note blocks (HTML comments containing `Speaker note:`)
    - At least 6 inline image references to `../illustrations/exported/m02-*.svg` files (any of the 13 m02 stubs)
    - All 9 CALM core node type names appear at least once (`actor`, `ecosystem`, `system`, `service`, `database`, `network`, `webclient`, `ldap`, `data-asset`)
    - All 5 CALM relationship type names appear at least once (`connects`, `interacts`, `deployed-in`, `composed-of`, `options`)
    - The literal $schema URL `https://calm.finos.org/release/1.2/meta/calm.json` appears in at least one slide
    - Zero "Chapter 2.N" prose references (regex `Chapter 2\.[0-9]` returns no matches)
    - At least one slide explicitly references Lab 2 (string `lab-02-conference-signup` appears)
  </acceptance_criteria>
  <done>
    Marp slide deck for Module 2 exists covering all 7 chapters in 30-40 slides with speaker notes, image references to the 13 m02 stubs, and the full vocabulary canon (9 nodes + 5 relationships + protocols + interfaces + controls + decorators + the 7-step build).
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Module 2 cheatsheet (1-page printable Markdown)</name>
  <files>
    docs-meta/cheatsheets/module-02-cheatsheet.md
  </files>
  <read_first>
    - .planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md (section "Cheatsheet Design")
    - content/module-02-calm-fundamentals/nodes.mdx (chapter 2.2 — extract one-line node descriptions)
    - content/module-02-calm-fundamentals/relationships.mdx (chapter 2.3 — extract relationship table)
    - code-examples/module-02-calm-fundamentals/with-interfaces.architecture.json (extract interface forms exactly)
    - code-examples/module-02-calm-fundamentals/with-controls.architecture.json (extract control skeleton exactly)
    - /Users/gshah/work/opsflow-sh/calm/architecture-as-code/calm/release/1.2/meta/core.json (verify enum values one final time)
  </read_first>
  <action>
    Create directory `docs-meta/cheatsheets/` if it does not exist. Then author `docs-meta/cheatsheets/module-02-cheatsheet.md` as a single-page printable Markdown reference.

    The cheatsheet does NOT have Docusaurus frontmatter — it is a static reference file. When the Docusaurus site is built in Phase 5, the team can decide whether to host this as a download link or render as a styled page.

    Required sections in this order (use `## ` H2 headings):

    1. **Title and tagline** — `# CALM 1.2 Cheatsheet — Module 2` followed by a 1-line blockquote "Single-page reference for the CALM 1.2 vocabulary. Print and keep at your desk."
    2. **`## $schema URL`** — fenced code block or inline code with the exact literal `https://calm.finos.org/release/1.2/meta/calm.json`
    3. **`## Required fields`** — Markdown table: Object | Required fields. Rows: Node (`unique-id, node-type, name, description`); Relationship (`unique-id, relationship-type`); Architecture top-level (`$schema, unique-id, name, description, nodes[], relationships[]`)
    4. **`## Node types (9)`** — Markdown table with 3 columns (Node type | Use for | Never use for). One row per type in this order: actor, ecosystem, system, service, database, network, webclient, ldap, data-asset. Use the same one-line "use for" descriptions as Chapter 2.2 (consistency). End with a one-line note: "`container`, `component`, `microservice` are NOT CALM types — they are Docker/Kubernetes terms."
    5. **`## Relationship types (5)`** — Markdown table: Type | Required sub-fields | Example use. One row per type. End with the protocol-placement pitfall reminder: "`protocol` is a TOP-LEVEL field on the relationship, NOT nested inside the `connects` object."
    6. **`## Protocol enum (12)`** — comma-separated inline-code list: `HTTP`, `HTTPS`, `FTP`, `SFTP`, `JDBC`, `WebSocket`, `SocketIO`, `LDAP`, `AMQP`, `TLS`, `mTLS`, `TCP`
    7. **`## Interface quick reference`** — Two subsections (using bold or H3):
       - Freeform (interface-type) — required field `unique-id` only; fenced json block showing example freeform interface from `with-interfaces.architecture.json` (orders-api-rest with name, protocol HTTPS, port 8443)
       - Formal (interface-definition) — required fields `unique-id`, `definition-url`, `config`; fenced json block showing example with `definition-url` pointing at `https://calm.finos.org/release/1.2/prototype/interface/tcp-host-port.json` and config `{host, port}`. Closing line note: "1.0-rc1's `url-interface` and `host-port-interface` are gone in 1.2."
    8. **`## Control quick reference`** — fenced json skeleton showing `"controls": { "encryption-in-transit": { "description": ..., "requirements": [ { "requirement-url": ..., "config": ... } ] } }` matching the with-controls.architecture.json structure. One-line note: "Controls attach at: top-level architecture, per-node, per-relationship, per-flow."
    9. **`## Decorator quick reference`** — One-line statement: "Decorators are SEPARATE documents that reference an architecture." Followed by a fenced json example with `unique-id`, `type: "threat"`, `target: ["conference-signup.architecture.json"]`, `applies-to: ["attendees", "attendees-store"]`, `data: { stride: [...] }`
    10. **`## CLI quick reference`** — fenced bash block with `npx @finos/calm-cli validate -a conference-signup.architecture.json -f pretty`. Below it a one-line: "Visualize in CALM Studio web: <https://studio.calm.finos.org>"
    11. **`## Spec source`** — "Canonical spec: <https://calm.finos.org/release/1.2/>"
    12. **Footer** — horizontal rule then italic line "Module 2 Cheatsheet — CALM Academy. Print on a single A4/Letter page. Cross-reference: Module 2 chapters at `content/module-02-calm-fundamentals/`."

    Keep total file length 60-200 lines. Verify with `wc -l` after writing.

    Example URLs, control URLs, and decorator examples MUST match the patterns used in the code examples (with-interfaces, with-controls, conference-signup) — consistency between cheatsheet and runnable examples is the whole point.

    Do NOT include speaker notes, MDX frontmatter, or Docusaurus directives. This is pure printable Markdown.
  </action>
  <verify>
    <automated>test -d docs-meta/cheatsheets || { echo "FAIL: docs-meta/cheatsheets/ dir missing"; exit 1; }; test -f docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: cheatsheet missing"; exit 1; }; LINES=$(wc -l &lt; docs-meta/cheatsheets/module-02-cheatsheet.md); test "$LINES" -ge 60 || { echo "FAIL: cheatsheet too short ($LINES lines, need 60+)"; exit 1; }; test "$LINES" -le 200 || { echo "FAIL: cheatsheet too long ($LINES lines, max 200)"; exit 1; }; for nt in actor ecosystem system service database network webclient ldap data-asset; do grep -qE "\\\`$nt\\\`|^\\| \`$nt\`" docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: missing node type $nt"; exit 1; }; done; for rt in connects interacts deployed-in composed-of options; do grep -qE "\\\`$rt\\\`" docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: missing relationship type $rt"; exit 1; }; done; for proto in HTTP HTTPS FTP SFTP JDBC WebSocket SocketIO LDAP AMQP TLS mTLS TCP; do grep -q "$proto" docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: missing protocol $proto"; exit 1; }; done; grep -q "https://calm.finos.org/release/1.2/meta/calm.json" docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: missing 1.2 schema URL"; exit 1; }; grep -q "npx @finos/calm-cli validate" docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: missing CLI command"; exit 1; }; grep -q "definition-url" docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: missing interface definition-url ref"; exit 1; }; grep -q "encryption-in-transit" docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: missing control example"; exit 1; }; grep -q "target" docs-meta/cheatsheets/module-02-cheatsheet.md &amp;&amp; grep -q "applies-to" docs-meta/cheatsheets/module-02-cheatsheet.md || { echo "FAIL: missing decorator example"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `docs-meta/cheatsheets/module-02-cheatsheet.md` exists
    - File is 60-200 lines (fits a single printable page)
    - Contains all 9 node type names as backtick-quoted code (`actor`, `ecosystem`, `system`, `service`, `database`, `network`, `webclient`, `ldap`, `data-asset`)
    - Contains all 5 relationship type names as backtick-quoted code (`connects`, `interacts`, `deployed-in`, `composed-of`, `options`)
    - Contains all 12 protocol enum values
    - Contains literal $schema URL `https://calm.finos.org/release/1.2/meta/calm.json`
    - Contains the `npx @finos/calm-cli validate` CLI command
    - Contains both interface forms (one freeform example with name/protocol/port; one formal example with `definition-url` field)
    - Contains a control quick-reference skeleton with `encryption-in-transit` as the example key
    - Contains a decorator quick-reference with `target` and `applies-to` fields shown
    - No Docusaurus or MDX frontmatter (file starts with `# CALM 1.2 Cheatsheet` H1, not `---`)
  </acceptance_criteria>
  <done>
    Single-page Markdown cheatsheet exists covering the full Module 2 vocabulary canon — node types, relationships, protocols, interface forms, controls, decorators, CLI, schema URL.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slide deck → workshop instructors | Public teaching material; risk is incorrect spec claims |
| Cheatsheet → learners' desks (printed) | Reference held at point of use; risk is incorrect or outdated reference values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Spoofing | Slide deck cites wrong $schema URL | mitigate | Acceptance criteria requires literal `https://calm.finos.org/release/1.2/meta/calm.json` in at least one slide; same enforcement on cheatsheet |
| T-03-14 | Tampering | Slide JSON excerpts drift from runnable examples | mitigate | Action instructs author to extract code excerpts FROM the validated code-examples files, not invent fresh JSON. Reviewer can spot-check 2-3 excerpts against the source files |
| T-03-15 | Information Disclosure | Speaker notes contain proprietary FSI client names | accept | Author instructions explicitly call out FSI-aware-but-not-FSI-only voice; review pass should flag any named real institution before publication |
| T-03-16 | Repudiation | Cheatsheet contradicts chapter content | mitigate | Cheatsheet authoring reads the chapter MDX files first; tables (node types, relationships) use the same descriptions verbatim or paraphrased; lint-friendly grep checks confirm presence of all 9 nodes and 5 relationships |
| T-03-SC | Tampering | npm package installs | mitigate | No new packages |

</threat_model>

<verification>

```bash
# 1. Slide deck exists with Marp frontmatter
test -f slides/module-02-calm-fundamentals.md
head -5 slides/module-02-calm-fundamentals.md | grep -q "^marp: true"

# 2. Slide count in target range
SLIDES=$(grep -c '^---$' slides/module-02-calm-fundamentals.md)
test "$SLIDES" -ge 30 && test "$SLIDES" -le 45

# 3. All 9 node types and 5 relationship types covered
for nt in actor ecosystem system service database network webclient ldap data-asset; do
  grep -q "$nt" slides/module-02-calm-fundamentals.md
done
for rt in connects interacts deployed-in composed-of options; do
  grep -q "$rt" slides/module-02-calm-fundamentals.md
done

# 4. Cheatsheet exists, fits printable page
test -f docs-meta/cheatsheets/module-02-cheatsheet.md
LINES=$(wc -l < docs-meta/cheatsheets/module-02-cheatsheet.md)
test "$LINES" -ge 60 && test "$LINES" -le 200

# 5. Cheatsheet has full vocabulary canon
for nt in actor ecosystem system service database network webclient ldap data-asset; do
  grep -q "$nt" docs-meta/cheatsheets/module-02-cheatsheet.md
done
for proto in HTTP HTTPS FTP SFTP JDBC WebSocket SocketIO LDAP AMQP TLS mTLS TCP; do
  grep -q "$proto" docs-meta/cheatsheets/module-02-cheatsheet.md
done
```

</verification>

<success_criteria>

This plan succeeds when:

1. `slides/module-02-calm-fundamentals.md` exists with Marp frontmatter, 30-40 slides, speaker notes on every slide, and at least 6 image references to the m02 SVG placeholders
2. Slide deck covers all 7 chapters with a 9-slide node type gallery for Chapter 2.2 (the visual signature per RESEARCH.md)
3. Slide deck contains the literal CALM 1.2 $schema URL and references Lab 2
4. `docs-meta/cheatsheets/module-02-cheatsheet.md` exists at 60-200 lines (single printable page)
5. Cheatsheet documents all 9 node types, 5 relationship types, 12 protocol enum values, both interface forms, control skeleton, decorator skeleton, $schema URL, and CLI command
6. Cheatsheet content cross-references the runnable code examples (with-interfaces, with-controls, conference-signup) for consistency
7. Zero "Chapter 2.N" prose references in either artifact

</success_criteria>

<output>
Create `.planning/phases/03-module-2-calm-fundamentals/03-04-SUMMARY.md` when done. Include: slide count (exact), speaker note count, list of m02 SVGs referenced, cheatsheet line count, any deviations from plan (e.g. if slide count came in at 36 instead of 38).
</output>
