---
phase: 03-module-2-calm-fundamentals
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json
  - code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json
  - content/module-02-calm-fundamentals/calm-specification.mdx
  - content/module-02-calm-fundamentals/nodes.mdx
  - content/module-02-calm-fundamentals/relationships.mdx
  - illustrations/source/m02-calm-document-structure.excalidraw
  - illustrations/source/m02-node-types-gallery.excalidraw
  - illustrations/source/m02-actor-node.excalidraw
  - illustrations/source/m02-system-vs-service-vs-ecosystem.excalidraw
  - illustrations/source/m02-relationship-types.excalidraw
  - illustrations/source/m02-connects-vs-interacts.excalidraw
  - illustrations/exported/m02-calm-document-structure.svg
  - illustrations/exported/m02-node-types-gallery.svg
  - illustrations/exported/m02-actor-node.svg
  - illustrations/exported/m02-system-vs-service-vs-ecosystem.svg
  - illustrations/exported/m02-relationship-types.svg
  - illustrations/exported/m02-connects-vs-interacts.svg
  - illustrations/INVENTORY.md
autonomous: true
requirements: [MOD-03, ILL-03, CODE-01]
tags: [module-02, calm-fundamentals, mdx-chapters, code-examples, illustrations]

must_haves:
  truths:
    - "Learner can name all 9 CALM core node types and choose the right one for a system component"
    - "Learner can identify all 5 relationship types and place protocol at the correct JSON level"
    - "Two .architecture.json files in code-examples/module-02-calm-fundamentals/ validate against the CALM 1.2 schema"
    - "Chapters 2.1, 2.2, 2.3 read as a coherent foundational vocabulary unit"
    - "Six m02-* illustration stubs exist as valid JSON with embedded DIAGRAM SPEC text"
  artifacts:
    - path: "code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json"
      provides: "Reference architecture demonstrating all 9 CALM 1.2 core node types"
      contains: "\"$schema\": \"https://calm.finos.org/release/1.2/meta/calm.json\""
    - path: "code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json"
      provides: "Reference architecture demonstrating all 5 CALM 1.2 relationship types"
      contains: "\"$schema\": \"https://calm.finos.org/release/1.2/meta/calm.json\""
    - path: "content/module-02-calm-fundamentals/calm-specification.mdx"
      provides: "Chapter 2.1 — document anatomy, $schema, validation discipline"
      min_lines: 80
    - path: "content/module-02-calm-fundamentals/nodes.mdx"
      provides: "Chapter 2.2 — all 9 node types with FSI examples"
      min_lines: 120
    - path: "content/module-02-calm-fundamentals/relationships.mdx"
      provides: "Chapter 2.3 — all 5 relationship types, protocol placement pitfall"
      min_lines: 100
  key_links:
    - from: "content/module-02-calm-fundamentals/nodes.mdx"
      to: "code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json"
      via: "code block reference"
      pattern: "node-types-reference.architecture.json"
    - from: "content/module-02-calm-fundamentals/relationships.mdx"
      to: "code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json"
      via: "code block reference"
      pattern: "relationship-types-reference.architecture.json"
    - from: "content/module-02-calm-fundamentals/*.mdx"
      to: "illustrations/exported/m02-*.svg"
      via: "inline Markdown image"
      pattern: "../illustrations/exported/m02-"
---

## Phase Goal

**As a** working architect new to CALM, **I want to** read three foundational chapters and two validated code examples, **so that** I can name the 9 core node types and 5 relationship types and recognise them in any CALM JSON document.

<objective>
Deliver the foundational vocabulary slice of Module 2: the spec orientation chapter, the 9 node types deep-dive, and the 5 relationship types deep-dive — each anchored to a validated CALM 1.2 reference architecture and illustrated with a stub-pack of six Excalidraw diagrams.

Purpose: Module 2 is the spec vocabulary module. This wave delivers the foundational half — what a CALM document looks like, what nodes are, and how relationships connect them. After this wave, a learner can read any CALM file and identify the core constructs.

Output: 2 validated `.architecture.json` files, 3 MDX chapters, 6 Excalidraw stubs + 6 SVG placeholders, INVENTORY.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md
@CLAUDE.md
@CURRICULUM.md
@docs-meta/STYLE-GUIDE.md
@docs-meta/ILLUSTRATION-STANDARDS.md

# Prior phase patterns
@.planning/phases/02-module-1-the-case-for-architecture-as-code/02-01-SUMMARY.md
@.planning/phases/01-module-0-on-ramp/01-01-SUMMARY.md

# Existing reference artifacts to mirror in pattern
@code-examples/module-00-on-ramp/todo-api.architecture.json
@scripts/validate-calm.sh
@illustrations/INVENTORY.md

# Canonical spec (read-only authoritative source)
# /Users/gshah/work/opsflow-sh/calm/architecture-as-code/calm/release/1.2/meta/core.json
# /Users/gshah/work/opsflow-sh/calm/architecture-as-code/docs/static/calm-example/trading-system.architecture.json

<interfaces>
<!-- Patterns established by prior phases (must reuse, do NOT reinvent) -->

MDX chapter frontmatter pattern (from content/module-01-case-for-aac/introducing-calm.mdx):
```
---
title: "<Chapter title>"
slug: <kebab-case-slug>
module: 2
chapter: 2.N
estimated_minutes: <int>
prerequisites:
  - <slug-of-required-chapter>
---
```

MDX chapter body sections (from STYLE-GUIDE.md, enforced in Module 0 and 1):
1. `## TL;DR` (3-5 bullets, ≤80 words total)
2. `## Why it matters` (50-150 words)
3. `## The concept` (1500-3000 words, H3 subsections)
4. `## Code/CALM examples` (only for chapters that reference code-examples)
5. `## Common mistakes` (3-7 items)
6. `## Knowledge check` (link to quiz)
7. `## Further reading` (cross-refs by slug only — no chapter numbers in prose)

Excalidraw stub format (from illustrations/source/m01-aac-stack.excalidraw):
Valid JSON with single text element containing "DIAGRAM SPEC — <title> (<chapter ref>)" prose describing the visual.

SVG placeholder format (from illustrations/exported/m01-aac-stack.svg):
800x400 viewBox SVG with 4 text lines: placeholder label, description, source path, export command. Must validate as XML.

INVENTORY.md row format (from illustrations/INVENTORY.md):
`| <file-stem> | <topic> | Module N (Ch N.N) | stub — needs Excalidraw authoring | user | YYYY-MM-DD |`

CALM 1.2 architecture file structure (from code-examples/module-00-on-ramp/todo-api.architecture.json):
- Top-level: `$schema`, `unique-id`, `name`, `description`, `nodes[]`, `relationships[]`
- $schema URL MUST be exactly: `https://calm.finos.org/release/1.2/meta/calm.json`
- Each node has: `unique-id`, `node-type`, `name`, `description` (all required)
- Each relationship has: `unique-id`, `description`, `relationship-type` (a one-key object), and protocol at TOP LEVEL of the relationship (sibling of relationship-type)
- relationship-type variants: `{interacts: {actor: "<id>", nodes: ["<id>",...]}}`, `{connects: {source: {node: "<id>"}, destination: {node: "<id>"}}}`, `{deployed-in: {container: "<id>", nodes: ["<id>",...]}}`, `{composed-of: {container: "<id>", nodes: ["<id>",...]}}`, `{options: [{description: "...", nodes: [...], relationships: [...]}]}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author and validate the two foundational code examples</name>
  <files>
    code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json
    code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json
  </files>
  <read_first>
    - /Users/gshah/work/opsflow-sh/calm/architecture-as-code/calm/release/1.2/meta/core.json (lines 1-200 — confirm 9 node types enum and 5 relationship oneOf branches)
    - /Users/gshah/work/opsflow-sh/calm/architecture-as-code/docs/static/calm-example/trading-system.architecture.json (full read — canonical structure reference)
    - code-examples/module-00-on-ramp/todo-api.architecture.json (full read — minimal pattern)
    - scripts/validate-calm.sh (full read — CI gate command pattern)
    - .planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md (section "CALM 1.2 Specification — Authoritative Findings" and "Code Examples Plan")
  </read_first>
  <action>
    Create two CALM 1.2 reference architectures.

    File 1 — `code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json`:
    - `$schema`: exactly `https://calm.finos.org/release/1.2/meta/calm.json`
    - `unique-id`: `node-types-reference`
    - `name`: `CALM 1.2 Node Types Reference`
    - `description`: explains this architecture exists to demonstrate all 9 core node types
    - `nodes[]`: exactly 9 nodes, one of EACH core node-type — `actor`, `ecosystem`, `system`, `service`, `database`, `network`, `ldap`, `webclient`, `data-asset`. Use realistic FSI-flavoured names (e.g. trading-analyst as actor, prod-k8s-cluster as ecosystem, trade-execution-platform as system, order-service as service, trades-db as database, edge-firewall as network, corporate-directory as ldap, trader-portal as webclient, end-of-day-positions as data-asset). Each node needs all 4 required fields: `unique-id`, `node-type`, `name`, `description`. Descriptions must be 1-2 sentences that clarify the role.
    - `relationships[]`: 2-3 simple relationships connecting some of the nodes so the architecture is not just a node bag — e.g. actor interacts with webclient, webclient connects to service, service connects to database. The point of this file is the node gallery, but relationships make it valid as a holistic example.

    File 2 — `code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json`:
    - Same `$schema`, `unique-id: relationship-types-reference`, name and description focused on demonstrating all 5 relationship types.
    - `nodes[]`: 6-8 nodes covering enough variety to host all 5 relationship types meaningfully — at minimum: 1 actor, 1 webclient, 2 services, 1 database, 1 ecosystem, 1 system.
    - `relationships[]`: exactly 5 relationships, one of EACH relationship-type:
      1. `interacts` — actor → webclient (with `actor` field referencing the actor node's unique-id, `nodes` array referencing webclient)
      2. `connects` — webclient → service-a, with `protocol: "HTTPS"` at the relationship top level (NOT inside the connects object)
      3. `connects` — service-a → database, with `protocol: "JDBC"` at top level (second connects example showing JDBC)
      4. `deployed-in` — `container: "<ecosystem-id>"`, `nodes: ["<service-a-id>", "<service-b-id>", "<database-id>"]`
      5. `composed-of` — `container: "<system-id>"`, `nodes: ["<service-a-id>", "<service-b-id>"]`
      6. `options` — at least one decision branch with two `nodes[]` arrays demonstrating an architectural fork (e.g. choose synchronous vs async messaging). Each decision in `options[]` needs `description`, `nodes[]`, `relationships[]` (relationships can be an empty array if simpler).
      Total relationship count: 6 (one of each of the 5 unique relationship-type kinds, plus a second `connects` to show a different protocol — explicitly to teach protocol enum variety).

    After authoring both files, run `bash scripts/validate-calm.sh` from repo root. Both files MUST pass with `hasErrors: false` and 0 warnings. If validation fails, fix the file (NEVER weaken the validator). The validator finds files recursively, so the new files are picked up automatically.

    Use calmstudio-mcp if it accelerates correctness (`claude mcp list` first to confirm availability), but hand-writing the JSON is also acceptable — the validation step is the gate.

    CALM spec discipline: do NOT introduce `container`, `component`, `microservice`, or any other invented node-type. Use only the 9 core types. Per D-research, `options` is the most complex relationship-type — include it because Chapter 2.3 must explain it, but keep its decision array small (1 decision with 2 options is enough).
  </action>
  <verify>
    <automated>bash scripts/validate-calm.sh 2>&amp;1 | tee /tmp/m02-validate.log; grep -E "FAIL|hasErrors.*true" /tmp/m02-validate.log &amp;&amp; exit 1; test "$(jq -r '.nodes | map(.["node-type"]) | sort | unique | length' code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json)" = "9" &amp;&amp; test "$(jq -r '.relationships | map(.["relationship-type"] | keys[0]) | sort | unique | length' code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json)" = "5"</automated>
  </verify>
  <acceptance_criteria>
    - File `code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json` exists and is valid JSON
    - File `code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json` exists and is valid JSON
    - Both files contain literal string `"https://calm.finos.org/release/1.2/meta/calm.json"` as the `$schema` value
    - `node-types-reference` contains exactly 9 nodes, one of each: actor, ecosystem, system, service, database, network, ldap, webclient, data-asset (verified by jq unique count)
    - `relationship-types-reference` contains at least 5 relationships covering all of: interacts, connects, deployed-in, composed-of, options (verified by jq unique key count of relationship-type)
    - At least one `connects` relationship has `protocol` as a TOP-LEVEL field of the relationship (NOT nested inside the `connects` sub-object) — `jq '.relationships[] | select(.["relationship-type"].connects != null) | has("protocol")' relationship-types-reference.architecture.json` returns at least one `true`
    - `bash scripts/validate-calm.sh` exits 0 with no FAIL lines in output
    - Zero occurrences of `container` or `component` as a node-type value (`jq -r '.nodes[]["node-type"]' both files | grep -E '^(container|component|microservice)$'` returns empty)
  </acceptance_criteria>
  <done>
    Two .architecture.json files exist under code-examples/module-02-calm-fundamentals/, validate with `npx @finos/calm-cli`, demonstrate all 9 node types (file 1) and all 5 relationship types (file 2), and conform to CALM 1.2 schema URL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author chapters 2.1, 2.2, 2.3 (spec + nodes + relationships)</name>
  <files>
    content/module-02-calm-fundamentals/calm-specification.mdx
    content/module-02-calm-fundamentals/nodes.mdx
    content/module-02-calm-fundamentals/relationships.mdx
  </files>
  <read_first>
    - docs-meta/STYLE-GUIDE.md (full — voice, structure, tone calibration for Module 2 = "Reference-grade, precise, definitional")
    - .planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md (sections "Module 2 Chapter Structure" and "Common Pitfalls")
    - content/module-01-case-for-aac/introducing-calm.mdx (full — pattern reference: frontmatter, TL;DR, concept H3 subsections, common mistakes, further reading)
    - content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx (lines 1-80 — frontmatter pattern with prerequisites)
    - code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json (created by Task 1 — reference this file from nodes.mdx)
    - code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json (created by Task 1 — reference this file from relationships.mdx)
    - /Users/gshah/work/opsflow-sh/calm/architecture-as-code/calm/release/1.2/meta/core.json (confirm exact enum values when quoting them)
  </read_first>
  <action>
    Author three MDX chapters under `content/module-02-calm-fundamentals/`. Every chapter follows the STYLE-GUIDE.md structure exactly: frontmatter → TL;DR → Why it matters → The concept → Code/CALM examples (where relevant) → Common mistakes → Knowledge check → Further reading. Tone for Module 2 is reference-grade, precise, definitional — second person, plain English, active voice.

    **Chapter 2.1 — `calm-specification.mdx` (slug: `calm-specification`, ~1800 words)**

    Frontmatter:
    ```
    title: "The CALM specification"
    slug: calm-specification
    module: 2
    chapter: 2.1
    estimated_minutes: 18
    prerequisites:
      - introducing-calm
    ```

    Content arc:
    - TL;DR (3-5 bullets): JSON Schema-driven; top-level fields are `$schema`, `unique-id`, `name`, `description`, `nodes[]`, `relationships[]`; 1.2 is current stable; validator enforces shape, course discipline enforces meaning.
    - Why it matters: spec precision is what makes CALM tooling possible — every file format with this guarantee unlocked an ecosystem (Terraform's HCL, OpenAPI's YAML).
    - The concept H3 subsections:
      - "Why JSON Schema" — comparison with YAML DSLs and ad-hoc formats
      - "The CALM document anatomy" — list every top-level field with one-sentence purpose: `$schema`, `unique-id`, `name`, `description`, `nodes`, `relationships`, `metadata`, `controls`, `flows`, `adrs`. Add an inline image reference: `![CALM document top-level structure with required fields highlighted](../illustrations/exported/m02-calm-document-structure.svg)` with descriptive alt text.
      - "The $schema URL and spec versioning" — `https://calm.finos.org/release/1.2/meta/calm.json` is the value to use; explain Draft → RC → Release governance; warn that 1.0-rc1 files referenced obsolete interface types
      - "Schema conformance vs spec discipline" — the validator's `anyOf` extension hook accepts arbitrary node-type strings; course teaching enforces the 9-type canon; this is cultural, not technical (cross-link to nodes.mdx via slug)
      - "What `calm validate` actually checks" — JSON shape, required fields, enum values; what it does NOT check (semantic correctness of relationships, network reachability)
    - Code/CALM examples section: show a minimal valid CALM 1.2 skeleton (the bare-minimum 4 top-level fields plus an empty `nodes: []` and `relationships: []`) as a fenced ```json block. Do NOT reference the reference architectures here — those belong to chapters 2.2 and 2.3.
    - Common mistakes (3-5): wrong $schema URL (1.0-rc1 or 1.1), missing `description` on nodes, treating the validator's "pass" as semantic correctness, embedding decorator-type data inside the architecture instead of using separate decorator docs.
    - Knowledge check: link to `../quizzes/module-02-calm-fundamentals.yaml`
    - Further reading: link to nodes.mdx (next chapter), to introducing-calm.mdx (Module 1), and to https://calm.finos.org/release/1.2/

    **Chapter 2.2 — `nodes.mdx` (slug: `nodes`, ~2500 words)**

    Frontmatter:
    ```
    title: "Nodes: modeling system components"
    slug: nodes
    module: 2
    chapter: 2.2
    estimated_minutes: 28
    prerequisites:
      - calm-specification
    ```

    Content arc:
    - TL;DR: 9 core node types, each has a precise meaning; the validator accepts arbitrary strings but the course canon is exactly these 9; per-type FSI examples follow.
    - Why it matters: choosing the right type is the foundation for everything downstream (relationships, controls, governance overlays).
    - The concept H3 subsections — one per node type, in this order: `actor`, `ecosystem`, `system`, `service`, `database`, `network`, `webclient`, `ldap`, `data-asset`. Each subsection ~200 words including: what it represents, one real-world FSI example, one anti-pattern (when learners reach for it incorrectly), the required-field reminder.
      - Per RESEARCH.md, after the 9 subsections add a "Common confusions" subsection clarifying:
        - `actor` vs `ldap` (actor initiates; ldap is identity infrastructure others depend on)
        - `system` vs `service` vs `ecosystem` — reference inline image `![Three-panel diagram contrasting system, service, and ecosystem boundaries](../illustrations/exported/m02-system-vs-service-vs-ecosystem.svg)`
        - the `actor` callout — reference inline image `![Close-up of an actor node initiating an interaction with a service node](../illustrations/exported/m02-actor-node.svg)`
      - Add a "The node-types gallery" closing subsection referencing inline image `![Visual gallery of all 9 CALM 1.2 core node types with their CALM Studio canvas shapes](../illustrations/exported/m02-node-types-gallery.svg)` and pointing readers to the `node-types-reference.architecture.json` code example.
    - Code/CALM examples: reference the full file at `code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json` with a fenced ```json block showing the file's exact contents (full file, no truncation per STYLE-GUIDE.md — copy-paste rule).
    - Common mistakes (5-7): inventing `container`/`component`/`microservice`; missing `description`; using `system` where `service` is correct; using `ecosystem` for a logical boundary that should be `system`; treating `network` as an `ecosystem`; using `actor` for a service-to-service caller.
    - Knowledge check + Further reading (link to relationships.mdx, to calm-specification.mdx by slug).

    **Chapter 2.3 — `relationships.mdx` (slug: `relationships`, ~2200 words)**

    Frontmatter:
    ```
    title: "Relationships: connecting the dots"
    slug: relationships
    module: 2
    chapter: 2.3
    estimated_minutes: 25
    prerequisites:
      - nodes
    ```

    Content arc:
    - TL;DR: 5 relationship types — `connects`, `interacts`, `deployed-in`, `composed-of`, `options`; protocol is a TOP-LEVEL field on `connects` relationships, NOT inside the connects sub-object; relationship semantics drive what tools can infer.
    - Why it matters: relationship types carry meaning that arrows on a slide cannot — `deployed-in` (runtime) vs `composed-of` (structural) is the kind of distinction that enables governance automation.
    - The concept H3 subsections — one per relationship type, in this order: `connects`, `interacts`, `deployed-in`, `composed-of`, `options`. Each subsection includes a fenced ```json block showing ONLY that relationship-type's structure (extracted from `relationship-types-reference.architecture.json`).
      - In the `connects` subsection: include the protocol-placement pitfall callout EXPLICITLY — show a Correct example with `protocol` at the top of the relationship object, then a Wrong example with `protocol` nested inside the `connects` sub-object. Reference inline image `![Side-by-side diagram contrasting connects (node-to-node with protocol) and interacts (actor-initiates-to-node)](../illustrations/exported/m02-connects-vs-interacts.svg)`.
      - In the `interacts` subsection: emphasise the rule that the `actor` field must reference a node whose `node-type` is `actor` (per pitfall 6 in RESEARCH.md).
      - In the `deployed-in` subsection: teach the container field as follows — "The `container` field can be any node type, but `ecosystem` or `system` are the most common choices — `ecosystem` for cloud platforms and provider accounts, `system` for cluster-level runtimes. The conference signup example in the [Building your first architecture](./building-your-first-architecture.mdx) chapter uses a `system`-typed k8s-cluster as its container." Do NOT state "the container must be an ecosystem" — the schema does not restrict the type, and the canonical Lab 2 example uses `system`.
      - Add a closing "All five at a glance" subsection referencing inline image `![All 5 CALM relationship types in a single diagram with example arrows and one-line definitions](../illustrations/exported/m02-relationship-types.svg)` and the `relationship-types-reference.architecture.json` code example.
    - Add a "Protocol enum" subsection listing all 12 protocol values (HTTP, HTTPS, FTP, SFTP, JDBC, WebSocket, SocketIO, LDAP, AMQP, TLS, mTLS, TCP) and one-line guidance on when each is used.
    - Code/CALM examples: link to `code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json` with a fenced ```json block showing the full file contents.
    - Common mistakes (5-7): protocol-placement (top-level vs nested), `interacts.actor` pointing to a non-actor node, omitting `description`, using `deployed-in` where `composed-of` is correct, missing `unique-id`.
    - Knowledge check + Further reading.

    Cross-reference rule (enforced from STYLE-GUIDE.md and prior phase patterns): use slug-based relative links only. No "Chapter 2.N" prose references — use the chapter title instead, e.g. `[The CALM specification](./calm-specification.mdx)` not `[Chapter 2.1](...)`.

    All three chapters MUST inline-reference at least one m02-*.svg image. The SVG files are created in Task 3 as placeholders, so the path references are valid by phase end (Task 3 must run after Task 2, OR placeholders can be created in either order — see dependency note). Task 3 runs sequentially after Task 2 to keep things simple, but the references compile either way because placeholders are valid XML SVG.
  </action>
  <verify>
    <automated>test -f content/module-02-calm-fundamentals/calm-specification.mdx &amp;&amp; test -f content/module-02-calm-fundamentals/nodes.mdx &amp;&amp; test -f content/module-02-calm-fundamentals/relationships.mdx &amp;&amp; for f in content/module-02-calm-fundamentals/calm-specification.mdx content/module-02-calm-fundamentals/nodes.mdx content/module-02-calm-fundamentals/relationships.mdx; do grep -q '^slug:' "$f" || { echo "FAIL: missing slug frontmatter in $f"; exit 1; }; grep -q '^## TL;DR' "$f" || { echo "FAIL: missing TL;DR in $f"; exit 1; }; grep -q '^## Why it matters' "$f" || { echo "FAIL: missing Why it matters in $f"; exit 1; }; grep -q '^## The concept' "$f" || { echo "FAIL: missing The concept in $f"; exit 1; }; grep -q '^## Common mistakes' "$f" || { echo "FAIL: missing Common mistakes in $f"; exit 1; }; done; grep -l "container\|component\|microservice" content/module-02-calm-fundamentals/*.mdx | xargs -I{} grep -E "node-type.*(container|component|microservice)" {} | grep -v "DO NOT\|never\|wrong\|invented\|❌\|anti-pattern" &amp;&amp; { echo "FAIL: invented node-types appear positively"; exit 1; } || true; grep -c 'illustrations/exported/m02-' content/module-02-calm-fundamentals/calm-specification.mdx | awk '$1 &lt; 1 { exit 1 }'; grep -c 'illustrations/exported/m02-' content/module-02-calm-fundamentals/nodes.mdx | awk '$1 &lt; 1 { exit 1 }'; grep -c 'illustrations/exported/m02-' content/module-02-calm-fundamentals/relationships.mdx | awk '$1 &lt; 1 { exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - Three MDX files exist under `content/module-02-calm-fundamentals/`: `calm-specification.mdx`, `nodes.mdx`, `relationships.mdx`
    - Each file has frontmatter with `title`, `slug`, `module: 2`, `chapter`, `estimated_minutes`, `prerequisites` fields
    - Each file has sections: `## TL;DR`, `## Why it matters`, `## The concept`, `## Common mistakes`, `## Knowledge check`, `## Further reading`
    - Chapter 2.1 concept section has at least 1500 words (`wc -w` on concept section)
    - Chapter 2.2 concept section has at least 2000 words and contains H3 subsection headings for all 9 node types (one of each: `### actor`, `### ecosystem`, `### system`, `### service`, `### database`, `### network`, `### webclient`, `### ldap`, `### data-asset` — exact strings or kebab-case variants `## ### The actor type` etc.)
    - Chapter 2.3 concept section has at least 1800 words and contains H3 subsection headings for all 5 relationship types
    - Chapter 2.2 references `code-examples/module-02-calm-fundamentals/node-types-reference.architecture.json` by relative path in a fenced ```json code block
    - Chapter 2.3 references `code-examples/module-02-calm-fundamentals/relationship-types-reference.architecture.json` by relative path in a fenced ```json code block
    - Each chapter inline-references at least one `../illustrations/exported/m02-*.svg` file
    - Zero occurrences of `container`, `component`, or `microservice` used POSITIVELY as a node-type (occurrences only allowed in "anti-pattern", "wrong", "never", "DO NOT", or "❌" callouts — grep for the strings and verify each occurrence is in a negation context)
    - Zero "Chapter 2.N" prose cross-references; only slug-based markdown links
    - All `## Knowledge check` sections link to `../quizzes/module-02-calm-fundamentals.yaml`
  </acceptance_criteria>
  <done>
    Three chapters authored covering the foundational CALM 1.2 vocabulary (spec anatomy, 9 node types, 5 relationship types), each following STYLE-GUIDE.md structure, each referencing the corresponding validated code example, each inline-referencing at least one m02-*.svg illustration.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create 6 Excalidraw stubs + SVG placeholders + INVENTORY update for chapters 2.1–2.3</name>
  <files>
    illustrations/source/m02-calm-document-structure.excalidraw
    illustrations/source/m02-node-types-gallery.excalidraw
    illustrations/source/m02-actor-node.excalidraw
    illustrations/source/m02-system-vs-service-vs-ecosystem.excalidraw
    illustrations/source/m02-relationship-types.excalidraw
    illustrations/source/m02-connects-vs-interacts.excalidraw
    illustrations/exported/m02-calm-document-structure.svg
    illustrations/exported/m02-node-types-gallery.svg
    illustrations/exported/m02-actor-node.svg
    illustrations/exported/m02-system-vs-service-vs-ecosystem.svg
    illustrations/exported/m02-relationship-types.svg
    illustrations/exported/m02-connects-vs-interacts.svg
    illustrations/INVENTORY.md
  </files>
  <read_first>
    - illustrations/source/m01-aac-stack.excalidraw (full — stub JSON template to mirror)
    - illustrations/exported/m01-aac-stack.svg (full — SVG placeholder template to mirror)
    - illustrations/INVENTORY.md (full — row format to append)
    - docs-meta/ILLUSTRATION-STANDARDS.md (full — naming, B&amp;W rule, CALM node shape conventions)
    - .planning/phases/03-module-2-calm-fundamentals/03-RESEARCH.md (section "Illustration Plan" — DIAGRAM SPEC content per stub)
  </read_first>
  <action>
    Create 6 Excalidraw stub files (illustrations/source/m02-*.excalidraw) and 6 SVG placeholder files (illustrations/exported/m02-*.svg), then append 6 new rows to illustrations/INVENTORY.md. Mirror the m01-* pattern exactly — see m01-aac-stack.excalidraw for the JSON template and m01-aac-stack.svg for the SVG template.

    Per stub, the .excalidraw file is valid JSON with one text element containing a "DIAGRAM SPEC — <title> (<chapter ref>)" string with full prose describing the visual. Use the DIAGRAM SPECS from RESEARCH.md "Illustration Plan" section verbatim where present, and expand into the same format for any stub that needs a fresh spec.

    Stubs to create (with DIAGRAM SPEC summary; full spec text from RESEARCH.md plus extensions):

    1. `m02-calm-document-structure` (Ch 2.1)
       DIAGRAM SPEC — CALM document top-level structure (Chapter 2.1)
       Layout: Outer rounded rectangle labelled `.architecture.json`. Inside, list 7 top-level field names stacked vertically: `$schema`, `unique-id`, `name`, `description`, `nodes[]`, `relationships[]`, `metadata`. The first 4 marked REQUIRED with heavier border / bold; the last 3 marked optional. To the right of `nodes[]`, draw a small expanded callout showing one example node object with required fields (`unique-id`, `node-type`, `name`, `description`). To the right of `relationships[]`, callout showing one connects relationship with `protocol` as a top-level sibling of `relationship-type`. B&amp;W only.

    2. `m02-node-types-gallery` (Ch 2.2)
       DIAGRAM SPEC — All 9 CALM 1.2 core node types in a single visual gallery (Chapter 2.2, slide deck)
       Layout: 3x3 grid. Each cell contains the canonical Studio canvas shape (per ILLUSTRATION-STANDARDS.md visual language table) and the node-type label below. Cells: actor (stick figure), ecosystem (dashed-border container), system (large rounded rect), service (hexagon), database (cylinder), network (cloud/pipe), webclient (browser window), ldap (tree/directory), data-asset (document icon). Title above grid: "The 9 CALM 1.2 core node types". Each cell labelled with the exact node-type string. B&amp;W only, no fills.

    3. `m02-actor-node` (Ch 2.2)
       DIAGRAM SPEC — Close-up of an actor initiating an interaction (Chapter 2.2)
       Layout: Large stick figure on the left labelled "actor", solid arrow with open arrowhead pointing right to a hexagon labelled "service". Above the arrow, the relationship-type string `interacts`. Annotation below: "An actor pulls the trigger; services do not." Side-note: "interacts.actor field must reference a node of type actor". B&amp;W only.

    4. `m02-system-vs-service-vs-ecosystem` (Ch 2.2)
       DIAGRAM SPEC — Three-panel contrast: system vs service vs ecosystem (Chapter 2.2)
       Layout: Three panels side-by-side, vertical separators.
       Panel 1 (left): heading "system" — large rounded rectangle containing 2 smaller hexagons (services), captioned "a logical boundary / domain".
       Panel 2 (middle): heading "service" — single hexagon, captioned "a running process exposing capabilities".
       Panel 3 (right): heading "ecosystem" — dashed-border container holding 2 hexagons, captioned "a runtime environment that other nodes are deployed-in".
       Bottom annotation: "Choose based on what you mean: boundary (system), process (service), or runtime (ecosystem)."
       B&amp;W only.

    5. `m02-relationship-types` (Ch 2.3, slide deck)
       DIAGRAM SPEC — All 5 CALM relationship types in a single diagram (Chapter 2.3)
       Layout: 5 rows. Each row shows two abstract node boxes with the relationship type's line style between them (per ILLUSTRATION-STANDARDS.md):
         Row 1: `connects` — solid arrow, label "node to node (with protocol)"
         Row 2: `interacts` — solid arrow with open arrowhead, label "actor to node(s)"
         Row 3: `deployed-in` — dashed line, label "node lives inside container"
         Row 4: `composed-of` — diamond endpoint, label "container structurally composed of nodes"
         Row 5: `options` — multiple branching arrows from a decision point, label "decision branches (ADR-in-spec)"
       Title: "The 5 CALM relationship types". B&amp;W only.

    6. `m02-connects-vs-interacts` (Ch 2.3)
       DIAGRAM SPEC — Side-by-side: connects vs interacts (Chapter 2.3)
       Layout: Two panels.
       Panel 1 (left): heading "connects" — webclient (browser shape) → solid arrow → service (hexagon), arrow labelled "protocol: HTTPS". Caption: "node-to-node with a protocol".
       Panel 2 (right): heading "interacts" — actor (stick figure) → solid open-arrowhead arrow → webclient (browser shape). Caption: "actor pulls the trigger; protocol omitted".
       Bottom annotation: "connects is symmetric data flow with a wire protocol; interacts is human/external-system intent."
       B&amp;W only.

    SVG placeholder format (mirror illustrations/exported/m01-aac-stack.svg exactly): 800x400 viewBox, white background with light grey 2px border, 4 lines of grey text — line 1 the placeholder label `[Illustration placeholder: <stub-name>]`, line 2 a one-line topic, line 3 the source path, line 4 the export command. Each SVG must be valid XML and at least 600 bytes.

    INVENTORY.md update: append 6 new rows after the m01 rows, preserving all existing rows. Use the existing row format. Status for all 6: `stub — needs Excalidraw authoring`. Author: `user`. Date: `2026-06-16`. Module column: `Module 2 (Ch 2.N)` matching the chapter that uses each stub (per the DIAGRAM SPEC chapter ref). The `m02-relationship-types` row should also note slide deck use: `Module 2 (Ch 2.3, slide deck)`.
  </action>
  <verify>
    <automated>for stub in m02-calm-document-structure m02-node-types-gallery m02-actor-node m02-system-vs-service-vs-ecosystem m02-relationship-types m02-connects-vs-interacts; do test -f "illustrations/source/${stub}.excalidraw" || { echo "FAIL: missing source ${stub}"; exit 1; }; jq -e '.type == "excalidraw" and (.elements | length &gt;= 1) and (.elements[0].text | contains("DIAGRAM SPEC"))' "illustrations/source/${stub}.excalidraw" &gt; /dev/null || { echo "FAIL: invalid stub ${stub}"; exit 1; }; test -f "illustrations/exported/${stub}.svg" || { echo "FAIL: missing svg ${stub}"; exit 1; }; xmllint --noout "illustrations/exported/${stub}.svg" 2&gt;/dev/null || python3 -c "import xml.etree.ElementTree as ET; ET.parse('illustrations/exported/${stub}.svg')" || { echo "FAIL: invalid SVG ${stub}"; exit 1; }; test "$(wc -c &lt; illustrations/exported/${stub}.svg)" -gt 400 || { echo "FAIL: svg ${stub} too small"; exit 1; }; done; test "$(grep -c '^| m02-' illustrations/INVENTORY.md)" = "6" || { echo "FAIL: INVENTORY does not contain 6 m02 rows"; exit 1; }; test "$(grep -c '^| m01-' illustrations/INVENTORY.md)" = "8" || { echo "FAIL: m01 rows damaged"; exit 1; }; test "$(grep -c '^| m00-' illustrations/INVENTORY.md)" = "3" || { echo "FAIL: m00 rows damaged"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - 6 files under `illustrations/source/`: `m02-calm-document-structure.excalidraw`, `m02-node-types-gallery.excalidraw`, `m02-actor-node.excalidraw`, `m02-system-vs-service-vs-ecosystem.excalidraw`, `m02-relationship-types.excalidraw`, `m02-connects-vs-interacts.excalidraw`
    - Each .excalidraw is valid JSON conforming to the schema `{type:"excalidraw", elements: [{type:"text", text: "<DIAGRAM SPEC...>"}], ...}` — confirmed by jq
    - 6 corresponding files under `illustrations/exported/`, each at least 400 bytes, each valid XML SVG
    - `illustrations/INVENTORY.md` contains exactly 6 rows starting `| m02-` (the 6 new stubs)
    - `illustrations/INVENTORY.md` preserves all 8 m01-* rows and all 3 m00-* rows
    - Each MDX chapter from Task 2 has its inline `../illustrations/exported/m02-*.svg` references pointing to files that now exist
  </acceptance_criteria>
  <done>
    Six Excalidraw stubs + six SVG placeholders + INVENTORY.md updated, matching the m01 pattern exactly. Chapters 2.1–2.3 inline image references resolve to real files (placeholder content). Phase 1+2 illustration inventory preserved.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CALM JSON examples → learners | Static tutorial content; no auth, no PII, no live network surface |
| MDX content → Docusaurus site | Static prose with relative image and code paths; rendered at build time |
| INVENTORY.md → CI gates | Phase-gate check reads inventory; tampering is contained to this repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | code-examples/*.architecture.json | mitigate | All .architecture.json files validated by `scripts/validate-calm.sh` before commit; CI gate prevents merge of invalid schemas |
| T-03-02 | Information Disclosure | MDX chapter prose | accept | Public educational content by design; no secrets, no PII, no internal architecture leaked. Examples use generic FSI names (trading-analyst, order-service) that are not specific to any real institution |
| T-03-03 | Tampering | Excalidraw stubs claiming "user-authored" status | mitigate | INVENTORY.md status column is `stub — needs Excalidraw authoring`; phase verification (next phase) requires status flip to `authored` and SVG size &gt; 1KB before user marks complete |
| T-03-04 | Spoofing | $schema URL pointing to wrong CALM version | mitigate | Acceptance criteria literal-string match on `https://calm.finos.org/release/1.2/meta/calm.json`; validator rejects non-1.2 files; chapter 2.1 explicitly teaches the wrong-URL pitfall |
| T-03-SC | Tampering | npm/npx package installs | mitigate | No new npm package installs in this plan. `npx @finos/calm-cli` confirmed available at 1.44.1 in Phase 1; same version used here. No `[ASSUMED]` packages in this plan, so no blocking-human checkpoint required |

</threat_model>

<verification>

Phase-level checks for this plan (executor must confirm before SUMMARY):

```bash
# 1. Code examples validate
bash scripts/validate-calm.sh

# 2. Three chapters exist with correct frontmatter
for f in content/module-02-calm-fundamentals/{calm-specification,nodes,relationships}.mdx; do
  grep -q "^slug:" "$f" && grep -q "^module: 2" "$f" || { echo "FAIL: $f"; exit 1; }
done

# 3. Six Excalidraw stubs are valid JSON with DIAGRAM SPEC
for f in illustrations/source/m02-*.excalidraw; do
  jq -e '.elements[0].text | contains("DIAGRAM SPEC")' "$f" > /dev/null
done

# 4. Six SVG placeholders are valid XML
for f in illustrations/exported/m02-*.svg; do
  python3 -c "import xml.etree.ElementTree as ET; ET.parse('$f')"
done

# 5. INVENTORY preserved + 6 m02 rows
test "$(grep -c '^| m00-' illustrations/INVENTORY.md)" = "3"
test "$(grep -c '^| m01-' illustrations/INVENTORY.md)" = "8"
test "$(grep -c '^| m02-' illustrations/INVENTORY.md)" = "6"

# 6. No invented node-types appear positively in chapters or examples
! grep -E '"node-type"\s*:\s*"(container|component|microservice)"' code-examples/module-02-calm-fundamentals/*.architecture.json
```

</verification>

<success_criteria>

This plan succeeds when:

1. Two `.architecture.json` files exist under `code-examples/module-02-calm-fundamentals/`, both validate with `npx @finos/calm-cli validate -a`, both use the CALM 1.2 schema URL
2. `node-types-reference.architecture.json` demonstrates all 9 core node types (one of each)
3. `relationship-types-reference.architecture.json` demonstrates all 5 relationship types (one of each) with `protocol` correctly placed at relationship top-level on `connects` examples
4. Three MDX chapters (`calm-specification.mdx`, `nodes.mdx`, `relationships.mdx`) exist with STYLE-GUIDE.md structure, kebab-case slugs, prerequisites chain (`calm-specification` → `nodes` → `relationships`), and inline references to both the code examples and at least one m02-*.svg per chapter
5. Six Excalidraw stubs + six SVG placeholders exist for the six illustrations referenced by chapters 2.1–2.3
6. INVENTORY.md preserves all prior rows and adds 6 m02 rows
7. No occurrence of `container` / `component` / `microservice` as a positive node-type anywhere

</success_criteria>

<output>
Create `.planning/phases/03-module-2-calm-fundamentals/03-01-SUMMARY.md` when done, following the SUMMARY template. Include: list of files created, validation results, decisions made (e.g. specific FSI node names chosen), known stubs (the 6 m02 SVGs), and any deviations from the plan.
</output>
