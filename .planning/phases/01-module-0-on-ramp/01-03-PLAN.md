---
phase: "01-module-0-on-ramp"
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - content/module-00-on-ramp/your-first-calm-document.mdx
  - labs/lab-00-on-ramp/LAB.md
  - labs/lab-00-on-ramp/starter/README.md
  - labs/lab-00-on-ramp/solution/my-system.architecture.json
  - quizzes/module-00-on-ramp.yaml
  - scripts/lint-quizzes.sh
autonomous: true
requirements:
  - MOD-01
  - LAB-01
  - QUIZ-01
must_haves:
  truths:
    - "A student can read Chapter 0.5 and know exactly what they will do in Lab 0 before opening it"
    - "A student can open labs/lab-00-on-ramp/LAB.md and follow all 4 steps end-to-end on a fresh machine with no hand-holding"
    - "The solution file (my-system.architecture.json) validates with calm validate before it is committed"
    - "The quiz YAML has 4 questions covering all 3 QUIZ-01 topic areas (setup, first CALM doc, three AI paths) and validates with lint-quizzes.sh"
    - "The lab is Docker-free: requires_docker: false is set and no docker steps exist"
  artifacts:
    - path: "content/module-00-on-ramp/your-first-calm-document.mdx"
      provides: "Chapter 0.5 — Hands-On (MOD-01 final chapter)"
      contains: "Lab 0 wrapper chapter: what to do, how to validate, how to visualize; hooks to the lab"
    - path: "labs/lab-00-on-ramp/LAB.md"
      provides: "Lab 0 (LAB-01)"
      contains: "YAML frontmatter: id, title, module, chapter, estimated_minutes, difficulty, requires_docker: false, workspace, objectives, steps (4), resources"
    - path: "labs/lab-00-on-ramp/solution/my-system.architecture.json"
      provides: "Lab 0 reference solution (validates with calm CLI)"
      contains: "A minimal valid CALM 1.2 architecture with at least 3 nodes and 2 relationships"
    - path: "quizzes/module-00-on-ramp.yaml"
      provides: "Module 0 quiz (QUIZ-01)"
      contains: "4+ questions: 3 multiple_choice + 1 short_answer; covers setup, first CALM doc produced, three AI paths"
    - path: "scripts/lint-quizzes.sh"
      provides: "Updated quiz linter with schema checks"
      contains: "Validates question IDs match qNN.N.N format, each multiple_choice has exactly one correct answer"
  key_links:
    - from: "content/module-00-on-ramp/your-first-calm-document.mdx"
      to: "labs/lab-00-on-ramp/LAB.md"
      via: "Markdown link"
      pattern: "lab-00-on-ramp/LAB.md"
    - from: "labs/lab-00-on-ramp/LAB.md"
      to: "labs/lab-00-on-ramp/solution/my-system.architecture.json"
      via: "Solution reference in LAB.md body"
      pattern: "solution/"
    - from: "quizzes/module-00-on-ramp.yaml"
      to: "content/module-00-on-ramp/*.mdx"
      via: "reference_section paths in each question"
      pattern: "../content/module-00-on-ramp/"
---

<objective>
This plan delivers the hands-on half of Module 0: Chapter 0.5 (the lab wrapper chapter), Lab 0 itself (LAB.md with starter scaffold and validated solution), and the Module 0 quiz YAML. It also upgrades lint-quizzes.sh from a stub to a working schema checker.

Purpose: After completing Plans 01-01, 01-02, and 01-03, a student has read all 5 chapters, has a setup environment, understands the three paths, has seen the demo, understands the mechanism, and can now do the 15-minute hands-on exercise. The quiz closes the loop — self-testing after the full module. This plan runs in parallel with Plan 01-02 (Wave 2) since it depends only on Plan 01-01's code example artifact, not on Chapters 0.2 and 0.3.

Output: Chapter 0.5 MDX, Lab 0 LAB.md + starter/solution, quiz YAML, updated lint-quizzes.sh.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/ROADMAP.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/CLAUDE.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/CURRICULUM.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/STYLE-GUIDE.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/LAB-FORMAT.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/QUIZ-FORMAT.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-RESEARCH.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01-01 outputs (confirmed available before this plan runs) -->
<!-- code-examples/module-00-on-ramp/todo-api.architecture.json — validated CALM 1.2 example -->
<!-- scripts/validate-calm.sh — CALM JSON validation script -->
<!-- scripts/lint-quizzes.sh — quiz linter stub (to be upgraded in this plan) -->

<!-- LAB-FORMAT.md step check kinds -->
<!-- exit_code: runs command, asserts exit code -->
<!-- file_state: asserts file exists/absent/matches regex -->
<!-- command_regex: runs command, matches output against regex pattern -->
<!-- ai_judge: LLM-graded open-ended check -->

<!-- QUIZ-FORMAT.md question types -->
<!-- multiple_choice: 3-5 options, exactly one correct: true -->
<!-- short_answer: accepted_answers array, case_sensitive flag -->
<!-- code_completion: CALM JSON snippet with <BLANK> marker -->

<!-- QUIZ-01 topic areas from REQUIREMENTS.md -->
<!-- 1. Setup completed (Option 1/2/3 chosen and verified) -->
<!-- 2. First CALM doc produced (validate output, node types) -->
<!-- 3. Three AI paths understood (Talk/Sketch/Markdown, what they each take as input) -->

<!-- LAB-01 spec from REQUIREMENTS.md -->
<!-- Docker-free. Steps: describe system → .calm.json → calm validate → CALM Studio visualize -->
<!-- estimated_minutes: 15, difficulty: 1, requires_docker: false -->

<!-- Solution file: must validate with npx @finos/calm-cli validate -a -->
<!-- Must use only core CALM node types (no container, component, etc.) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Lab 0 — LAB.md, starter, and validated solution</name>
  <files>
    labs/lab-00-on-ramp/LAB.md
    labs/lab-00-on-ramp/starter/README.md
    labs/lab-00-on-ramp/solution/my-system.architecture.json
  </files>
  <read_first>
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/LAB-FORMAT.md (full file — YAML frontmatter schema, step check kinds, authoring rules)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-RESEARCH.md (sections: "Lab Format Pattern (LAB-01)" — full LAB.md YAML pattern to use; "Open Questions" — calmstudio-mcp availability fallback; "Environment Availability" — CALM Studio web URL)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/CURRICULUM.md (Chapter 0.5 — "Your First CALM Document (Hands-on, 10 min)" — the hook paragraph for closing the lab)
  </read_first>
  <action>
    **Create the solution file first, then validate it, then create the LAB.md.**

    Step A: Create `labs/lab-00-on-ramp/solution/my-system.architecture.json`

    This is the reference solution that learners can inspect if they get stuck. It must:
    - Be a valid CALM 1.2 architecture file
    - Use only the 9 core node types
    - Represent a simple but realistic system (a "simple blog" — different enough from todo-api that learners can compare but not copy-paste)
    - Have at least 3 nodes and 2 relationships

    Create the file with:
    - `$schema`: "https://calm.finos.org/release/1.2/meta/calm.json"
    - `unique-id`: "simple-blog"
    - `name`: "Simple Blog"
    - `description`: "A user visits a blog website, the website serves content from an API, the API reads posts from a database."
    - Nodes:
      - reader (actor): "Reader browsing blog posts"
      - blog-web (webclient): "Browser-rendered blog frontend"
      - blog-api (service): "API serving blog post content"
      - posts-db (database): "Database storing blog posts and metadata"
    - Relationships:
      - reader-reads-blog: interacts — actor: reader, nodes: [blog-web]
      - web-to-api: connects — source: blog-web, dest: blog-api, protocol: HTTPS
      - api-to-db: connects — source: blog-api, dest: posts-db, protocol: JDBC

    Run IMMEDIATELY after writing: `npx @finos/calm-cli validate -a labs/lab-00-on-ramp/solution/my-system.architecture.json -f pretty`
    If validation fails, fix the JSON before proceeding. Only move to LAB.md creation after the solution validates.

    Step B: Create `labs/lab-00-on-ramp/LAB.md`

    Use the YAML frontmatter format from LAB-FORMAT.md. The LAB.md must have two parts: (1) YAML frontmatter (machine-parseable by Learnforge), (2) Markdown body (human-readable walkthrough).

    YAML frontmatter (between `---` delimiters):
    ```yaml
    id: lab-00-on-ramp
    title: "Your First CALM Document in 15 Minutes"
    module: 0
    chapter: 0.5
    estimated_minutes: 15
    difficulty: 1
    prerequisites: []
    requires_docker: false
    workspace:
      cumulative: false
      base_dir: lab-00-on-ramp
      creates:
        - my-system.architecture.json
    objectives:
      - Describe a real system you know in 3 sentences
      - Generate a .calm.json using AI-assisted tooling (calmstudio-mcp)
      - Validate the architecture with calm validate
      - Visualize the result in CALM Studio
    steps:
      - id: step-1
        title: "Verify calmstudio-mcp is ready"
        check:
          kind: exit_code
          command: "npx @calmstudio/mcp --version"
          expected: 0
        hints:
          - "If this fails, run: claude mcp add --scope user calmstudio-mcp -- npx @calmstudio/mcp"
          - "Alternatively, use CALM Studio web (browser, no install) for the visualization step"
          - "If @calmstudio/mcp is unavailable on npm, check https://github.com/finos/architecture-as-code for the current package name"
      - id: step-2
        title: "Describe your system and generate a .calm.json"
        check:
          kind: file_state
          path: my-system.architecture.json
          exists: true
        hints:
          - "Open your AI client (Claude Code, Cursor, or Windsurf with calmstudio-mcp active)"
          - "Describe a real system you know — 3 sentences: what it does, what talks to what, where data lives"
          - "Ask: 'Using calmstudio-mcp, create a CALM 1.2 architecture for the system I just described'"
          - "Save the output to my-system.architecture.json in your working directory"
      - id: step-3
        title: "Validate the architecture"
        check:
          kind: command_regex
          command: "npx @finos/calm-cli validate -a my-system.architecture.json -f pretty"
          pattern: "(?i)has.*error.*false|0 error"
        hints:
          - "Run: npx @finos/calm-cli validate -a my-system.architecture.json -f pretty"
          - "Look for hasErrors: false in the output"
          - "If you see errors: common cause is an invented node type (container, component) — ask the AI to replace it with a valid type (actor, service, database, webclient, etc.)"
          - "Valid types: actor, ecosystem, system, service, database, network, ldap, webclient, data-asset"
      - id: step-4
        title: "Visualize in CALM Studio"
        check:
          kind: ai_judge
          prompt: "The learner opened CALM Studio and imported their my-system.architecture.json. They can describe seeing nodes (boxes) connected by arrows (relationships). Ask them: 'What node types can you see in your diagram?' — a correct answer names at least one of: actor, webclient, service, database, network, ecosystem, ldap, data-asset."
        hints:
          - "Open https://studio.calm.finos.org (or the current CALM Studio web URL)"
          - "Import your my-system.architecture.json file using the import button"
          - "You should see boxes (nodes) connected by arrows (relationships)"
          - "If the URL has changed, check https://github.com/finos/architecture-as-code for the current Studio link"
    resources:
      - title: "Chapter 0.1 — Three Paths to Your First CALM Document"
        url: "../../content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx"
      - title: "Chapter 0.4 — Get Set Up in 60 Seconds"
        url: "../../content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx"
      - title: "Chapter 0.3 — Why This Works (and When It Doesn't)"
        url: "../../content/module-00-on-ramp/why-this-works-and-when-it-doesnt.mdx"
    ```

    Markdown body (after the closing `---`):
    ```markdown
    # Lab 0: Your First CALM Document in 15 Minutes

    ## Goal

    Produce a validated CALM architecture document describing a real system you know or work on — using AI tooling, in under 15 minutes, with no prior CALM knowledge required.

    ## Setup

    No Docker required. Prerequisites:
    - Node.js with npx available (`node --version` should show v18+)
    - An AI client with calmstudio-mcp configured (see Chapter 0.4)
    - OR: CALM Studio web for the visualization step (browser only, no install)

    ## Walkthrough

    ### Step 1: Verify calmstudio-mcp is ready

    Open your terminal and run:
    ```bash
    npx @calmstudio/mcp --version
    ```
    Expected: the version number prints. If it errors, see the hints in Chapter 0.4 for setup options.

    ### Step 2: Describe your system and generate .calm.json

    Think of a real system you know — your team's API, a side project, a product you use daily. Write 3 sentences:
    1. What the system does for users
    2. What components talk to each other
    3. Where data is stored or processed

    Open your AI client and send a message like:
    > "Using calmstudio-mcp, create a CALM 1.2 architecture for my system: [your 3 sentences here]"

    The AI will make a sequence of tool calls (read_calm_guide, create_architecture, add_node, add_relationship, finalize_architecture, export_calm). Watch the tool calls appear — you saw these explained in Chapter 0.2.

    Save the output to `my-system.architecture.json`.

    ### Step 3: Validate the architecture

    ```bash
    npx @finos/calm-cli validate -a my-system.architecture.json -f pretty
    ```

    Expected output (success):
    ```
    hasErrors: false
    hasWarnings: false
    ```

    If you see errors, the most common cause is an invented node type. Ask the AI:
    > "The validate command returned errors. Fix the invalid node types — only use types from: actor, ecosystem, system, service, database, network, ldap, webclient, data-asset"

    Run validate again after the AI fixes the file.

    ### Step 4: Visualize in CALM Studio

    Open CALM Studio in your browser: https://studio.calm.finos.org

    1. Click Import (or drag-and-drop)
    2. Select your `my-system.architecture.json` file
    3. Your architecture should render as boxes (nodes) connected by arrows (relationships)

    What you're seeing is the same information as your JSON — expressed visually. Try clicking on a node to see its properties.

    ## Stretch goals

    - Add a `network` node (load balancer, CDN, or API gateway) to your architecture and validate again
    - Describe a second system (different from the first) and generate a separate CALM file
    - Open the solution file in `solution/my-system.architecture.json` and compare its structure with yours

    ## Solution

    A reference solution is available at `solution/my-system.architecture.json`. Try the lab without looking — then compare.

    The solution models a "Simple Blog" (reader → blog-web → blog-api → posts-db). Your architecture will be different and that is correct — CALM captures your system, not ours.
    ```

    Step C: Create `labs/lab-00-on-ramp/starter/README.md`
    Content: brief 3-sentence file explaining the starter directory is intentionally empty (learners generate their own .calm.json from their AI client), what file they should create (my-system.architecture.json), and where to find help (LAB.md).
  </action>
  <verify>
    <automated>npx @finos/calm-cli validate -a /Users/gshah/work/opsflow-sh/calm/calm-academy/labs/lab-00-on-ramp/solution/my-system.architecture.json -f pretty</automated>
  </verify>
  <acceptance_criteria>
    - `labs/lab-00-on-ramp/LAB.md` exists with YAML frontmatter + Markdown body
    - YAML frontmatter has: id, title, module, chapter, estimated_minutes, difficulty, prerequisites, requires_docker: false, workspace, objectives, steps (4 steps with IDs step-1 through step-4), resources
    - All 4 steps have `id`, `title`, `check` (with `kind`), and `hints`
    - Check kinds used correctly: step-1 uses exit_code, step-2 uses file_state, step-3 uses command_regex, step-4 uses ai_judge
    - requires_docker: false is explicitly set (not absent, not true)
    - `labs/lab-00-on-ramp/solution/my-system.architecture.json` validates with `npx @finos/calm-cli validate -a` exit code 0 and hasErrors: false
    - Solution file uses only 9 core CALM node types
    - No `container` or `component` types in solution file
    - `labs/lab-00-on-ramp/starter/README.md` exists
    - LAB.md body section includes numbered walkthrough steps matching the YAML steps
  </acceptance_criteria>
  <done>
    Lab 0 LAB.md created with Learnforge-compatible YAML frontmatter (4 Docker-free steps), solution validates with calm CLI, starter README exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Module 0 quiz YAML (QUIZ-01) and upgrade lint-quizzes.sh</name>
  <files>
    quizzes/module-00-on-ramp.yaml
    scripts/lint-quizzes.sh
  </files>
  <read_first>
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/QUIZ-FORMAT.md (full file — schema, question types, authoring rules, validation rules for lint-quizzes.sh)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-RESEARCH.md (section: "Quiz Format Pattern (QUIZ-01)" — the 4 questions and correct answers already specified)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/REQUIREMENTS.md (QUIZ-01 — 3 topic areas: setup completed, first CALM doc produced, three AI paths understood)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/scripts/lint-quizzes.sh (current stub to upgrade)
  </read_first>
  <action>
    Create `quizzes/module-00-on-ramp.yaml` with:

    Top-level fields:
    - module: 0
    - title: "Module 0: The 15-Minute On-Ramp"

    chapters array with 3 chapter entries. Each chapter entry has: chapter number, slug, title, questions array.

    **Chapter 0.1 — Three Paths**
    ```yaml
    - chapter: 0.1
      slug: three-paths-to-first-calm-doc
      title: "Three Paths to Your First CALM Document"
      questions:
        - id: q0.1.1
          type: multiple_choice
          prompt: |
            Which three input types can the calmstudio-mcp skill convert to a valid CALM document?
          options:
            - label: "Plain English description, diagram or image, existing markdown document"
              correct: true
              explanation: "These are the three paths: Talk (describe in plain English), Sketch (image or diagram), Markdown (existing architecture doc like an ARB or RFC)."
            - label: "YAML, JSON, and XML files"
              correct: false
              explanation: "CALM outputs JSON, but these are data serialisation formats, not the three named input paths. The paths describe what the human brings, not what CALM produces."
            - label: "PowerPoint, Visio, and Lucidchart files"
              correct: false
              explanation: "You can upload a screenshot of a Visio diagram as an image (Path B: Sketch), but PowerPoint, Visio, and Lucidchart themselves are not the three named paths."
          reference_section: "../content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx#path-a-talk--describe-in-plain-english"

        - id: q0.1.2
          type: multiple_choice
          prompt: |
            What does Path C (Markdown) do that the other two paths cannot do automatically?
          options:
            - label: "Attach threat model decorators based on the architecture described in the document"
              correct: true
              explanation: "Correct. The calm-arb-convert skill (used in Path C) fetches the architecture markdown and a companion threat model document, producing CALM JSON with threat decorators already attached."
            - label: "Produce a CALM file with more nodes than the other paths"
              correct: false
              explanation: "The number of nodes depends on the description, not the input path. A detailed plain English description (Path A) can produce more nodes than a sparse markdown document."
            - label: "Validate the output automatically so no calm validate step is needed"
              correct: false
              explanation: "All three paths call finalize_architecture which validates internally, but running calm validate afterwards is still recommended as a final check for all paths."
          reference_section: "../content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx#path-c-markdown--from-an-existing-architecture-doc"
    ```

    **Chapter 0.4 — Setup**
    ```yaml
    - chapter: 0.4
      slug: get-set-up-in-sixty-seconds
      title: "Get Set Up in 60 Seconds"
      questions:
        - id: q0.4.1
          type: short_answer
          prompt: |
            What is the Claude Code CLI command to add calmstudio-mcp as a user-scoped MCP server?
          accepted_answers:
            - "claude mcp add --scope user calmstudio-mcp -- npx @calmstudio/mcp"
          case_sensitive: false
          explanation: "This single command registers calmstudio-mcp as a persistent MCP server across all Claude Code sessions. The --scope user flag makes it available globally (not just for one project)."
          reference_section: "../content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx#option-1-claude-code-cli-recommended-for-developers"
    ```

    **Chapter 0.5 — First CALM Document**
    ```yaml
    - chapter: 0.5
      slug: your-first-calm-document
      title: "Your First CALM Document"
      questions:
        - id: q0.5.1
          type: multiple_choice
          prompt: |
            After running `npx @finos/calm-cli validate -a my-system.architecture.json -f pretty`,
            what does a successful result look like?
          options:
            - label: "hasErrors: false in the output (and exit code 0)"
              correct: true
              explanation: "Correct. The validate command exits with code 0 and prints hasErrors: false when the architecture conforms to the CALM 1.2 schema. In pretty format, it lists any errors individually — an empty list means success."
            - label: "A green checkmark in the browser"
              correct: false
              explanation: "calm validate is a CLI command — all output goes to the terminal. There is no browser UI for validation."
            - label: "The .calm.json file is automatically saved in a corrected form"
              correct: false
              explanation: "calm validate only checks validity, it does not modify files. If errors are found, you fix the source file manually (or ask the AI to fix it)."
          reference_section: "../content/module-00-on-ramp/your-first-calm-document.mdx#validate-your-architecture"

        - id: q0.5.2
          type: code_completion
          prompt: |
            Complete the missing `node-type` for a node representing the end user of a system.
          snippet: |
            {
              "unique-id": "end-user",
              "name": "End User",
              "node-type": "<BLANK>",
              "description": "Person using the application"
            }
          accepted_answers:
            - "actor"
          explanation: "actor is the CALM type for users and any external entity that initiates interactions with the system. Common mistakes: 'user', 'person', 'human' — none of these exist in the CALM spec."
          reference_section: "../content/module-00-on-ramp/why-this-works-and-when-it-doesnt.mdx#the-9-core-node-types-a-reference-table"

        - id: q0.5.3
          type: multiple_choice
          prompt: |
            Which of these is NOT a valid CALM 1.2 node type?
          options:
            - label: "container"
              correct: true
              explanation: "Correct — container does not exist in the CALM spec. It is a Docker/Kubernetes term that beginners frequently try to use. The 9 valid core types are: actor, ecosystem, system, service, database, network, ldap, webclient, data-asset."
            - label: "database"
              correct: false
              explanation: "database IS a valid CALM node type. It represents any data persistence layer: SQL, NoSQL, cache, queues used as storage."
            - label: "webclient"
              correct: false
              explanation: "webclient IS a valid CALM node type. It represents browser apps, mobile apps, and any frontend interface."
            - label: "actor"
              correct: false
              explanation: "actor IS a valid CALM node type. It represents users and external systems that initiate interactions."
          reference_section: "../content/module-00-on-ramp/why-this-works-and-when-it-doesnt.mdx#the-9-core-node-types-a-reference-table"
    ```

    After creating the quiz file, upgrade `scripts/lint-quizzes.sh` (replacing the stub from Plan 01-01):

    The upgraded linter should:
    1. Find all files in `quizzes/*.yaml`
    2. For each file, use `yq` (or `python3 -c "import yaml"` as fallback) to:
       - Check the file is valid YAML (exits 0 if parsed successfully)
       - For each question, verify `id` matches pattern `q[0-9]+\.[0-9]+\.[0-9]+`
       - For each `multiple_choice` question, count options with `correct: true` — must be exactly 1
       - For each question, check `reference_section` is present (not empty)
    3. Report PASS or FAIL per check
    4. Exit 1 if any check fails; exit 0 if all pass

    If `yq` is not available, fall back to `python3` with `import yaml`. Write the script to try `yq` first, then `python3`, then fail with a helpful message if neither is available.

    Make lint-quizzes.sh executable: `chmod +x scripts/lint-quizzes.sh`

    Run the linter against the newly created quiz file: `bash scripts/lint-quizzes.sh`
    Fix any issues and re-run until it exits 0.
  </action>
  <verify>
    <automated>bash /Users/gshah/work/opsflow-sh/calm/calm-academy/scripts/lint-quizzes.sh</automated>
  </verify>
  <acceptance_criteria>
    - `quizzes/module-00-on-ramp.yaml` exists and is valid YAML
    - Contains 3 chapter entries (0.1, 0.4, 0.5) with a total of 5+ questions
    - 3 topic areas from QUIZ-01 covered: three AI paths (q0.1.x), setup (q0.4.1), first CALM doc produced (q0.5.x)
    - All question IDs match pattern qNN.N.N: q0.1.1, q0.1.2, q0.4.1, q0.5.1, q0.5.2, q0.5.3
    - Each multiple_choice question has exactly one option with correct: true
    - Each option has an explanation field
    - Each question has a reference_section pointing to a real MDX file path
    - code_completion question (q0.5.2) has a snippet with <BLANK> and accepted_answers: ["actor"]
    - scripts/lint-quizzes.sh exits 0 when run against quizzes/module-00-on-ramp.yaml
    - lint-quizzes.sh validates: ID format, single-correct-answer for multiple_choice, reference_section present
  </acceptance_criteria>
  <done>
    Module 0 quiz YAML created with 5 questions across 3 chapters covering all QUIZ-01 topic areas. lint-quizzes.sh upgraded and exits 0 against the quiz file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author Chapter 0.5 — Your First CALM Document</name>
  <files>
    content/module-00-on-ramp/your-first-calm-document.mdx
  </files>
  <read_first>
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/STYLE-GUIDE.md (tone for M0, section structure)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/CURRICULUM.md (Chapter 0.5 — "Your First CALM Document (Hands-on, 10 min)" — the hook paragraph at end)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-RESEARCH.md (section: "CALM validate output format"; the hook from REQUIREMENTS.md core value statement)
    - labs/lab-00-on-ramp/LAB.md (just created — this chapter wraps the lab)
  </read_first>
  <action>
    Author `content/module-00-on-ramp/your-first-calm-document.mdx` with:

    Frontmatter:
    - title: "Your First CALM Document"
    - slug: your-first-calm-document
    - module: 0
    - chapter: 0.5
    - estimated_minutes: 15
    - prerequisites: [three-paths-to-first-calm-doc, get-set-up-in-sixty-seconds]

    This is the lab wrapper chapter — it motivates the lab and provides enough context so a learner knows exactly what to expect when they open LAB.md. Shorter than the concept chapters (target: 1000–1500 words total).

    Required sections:

    ## TL;DR
    - Pick a real system you know — describe it in 3 sentences
    - Use calmstudio-mcp to generate the .calm.json — watch the 6 tool calls happen
    - Run `npx @finos/calm-cli validate -a` — confirm hasErrors: false
    - Open CALM Studio, import the file, see your architecture visualized
    - Takes 10–15 minutes. No spec knowledge required.

    ## Why it matters
    Keep it tight. The hook from CURRICULUM.md: "You just produced a versionable, AI-consumable, validated architecture document. You'll spend the rest of this course learning why that document is structured the way it is — and what you can do with it that you couldn't do with a diagram."

    ## The concept

    ### Choosing your system
    The best system for this exercise is one you already know — not a tutorial example. Your team's production API, your side project's data pipeline, a SaaS product you use daily (describe what it does, not its internals).

    A 3-sentence frame:
    1. Who uses it and for what (identifies your actor node)
    2. What software components exist (identifies your service/webclient/system nodes)
    3. Where data lives (identifies your database/data-asset nodes)

    Example: "A financial advisor accesses a dashboard to view client portfolio data. The dashboard calls a portfolio API, which aggregates positions from a trading system and a custodian. The API stores cached positions in a Redis cache." → 1 actor, 1 webclient, 1 service (API), 1 service (trading system), 1 service (custodian), 1 database (Redis).

    ### Generating the .calm.json
    Walk through the steps at the high level (the full walkthrough is in LAB.md):
    - Open your AI client with calmstudio-mcp active
    - Describe your system; ask for CALM architecture
    - Watch read_calm_guide → create_architecture → add_node (x N) → add_relationship (x M) → finalize_architecture → export_calm
    - Save the output as `my-system.architecture.json`

    ### Validating your architecture
    Show the validate command:
    ```bash
    npx @finos/calm-cli validate -a my-system.architecture.json -f pretty
    ```
    Show what success looks like (hasErrors: false). Explain the most common error: invented node type. Show how to fix it (ask the AI: "replace [invented-type] with a valid CALM type").

    ### Visualizing in CALM Studio
    Brief paragraph: import the .calm.json into CALM Studio. The visual output is a direct rendering of the structure you described. If a node is missing, add it. If a relationship direction looks wrong, fix it. The canvas and the JSON are always in sync.

    ### What you just produced
    End with the hook from CURRICULUM.md (reworded in second person): You just produced a versionable, validated, AI-consumable architecture document — not a diagram, not a Confluence page, not a PowerPoint. A machine-readable specification. Module 1 explains why this matters. Module 2 teaches you to read and write it precisely. Module 3 shows you what to do with it in production.

    ## Code/CALM examples
    Reference the solution file as a code block (do NOT embed it in full — keep this chapter lean and link to the lab's solution instead):
    ```
    // Reference solution: labs/lab-00-on-ramp/solution/my-system.architecture.json
    // Your architecture will look similar in structure but describe your actual system.
    ```

    Then show just the validate output format (short):
    ```bash
    # Expected success output
    npx @finos/calm-cli validate -a my-system.architecture.json -f pretty
    # hasErrors: false
    # hasWarnings: false
    ```

    ## Common mistakes (3–4 items)
    - Describing implementation instead of architecture — "uses PostgreSQL with UUID PKs" is implementation; "stores records in a database" is architecture
    - Only describing one component — the 3-sentence frame ensures you capture at least actor + frontend/backend + storage
    - Not running validate before moving on — the AI output is a draft; validate catches what finalize_architecture misses
    - Treating CALM as complete documentation — CALM captures structure, not behavior; your architecture will have more nodes than 3 sentences can capture

    ## Knowledge check
    Link to: `../../quizzes/module-00-on-ramp.yaml` with anchor text "Take the Module 0 quiz — closes the loop on what you just built"

    ## Lab
    Primary call to action (not just a footnote — this is the deliverable chapter):
    "**Ready? Open the lab:** [Lab 0: Your First CALM Document in 15 Minutes](../../labs/lab-00-on-ramp/LAB.md)"
    Short instruction: complete all 4 steps, then come back here and take the quiz.

    ## Further reading
    - Link to why-this-works-and-when-it-doesnt.mdx (understand what you just produced)
    - Link to live-demo-diagram-to-calm-in-five-minutes.mdx (replay the demo with what you now know)
    - CALM CLI reference: https://github.com/finos/architecture-as-code/tree/main/calm-ai/tools
  </action>
  <verify>
    <automated>grep -c "lab-00-on-ramp/LAB.md" /Users/gshah/work/opsflow-sh/calm/calm-academy/content/module-00-on-ramp/your-first-calm-document.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `content/module-00-on-ramp/your-first-calm-document.mdx`
    - Frontmatter has all required fields including prerequisites
    - All 8 sections present (TL;DR through Further reading)
    - The lab link to `../../labs/lab-00-on-ramp/LAB.md` is present and prominent (not buried)
    - The hook closing paragraph from CURRICULUM.md is present (versionable, AI-consumable, validated)
    - validate command shown with expected output (hasErrors: false)
    - Does NOT embed the full todo-api.architecture.json again (Chapter 0.1 and 0.2 cover that — this chapter stays lean)
    - Word count total: 1000–1500 words (lab wrapper, not a concept deep-dive)
  </acceptance_criteria>
  <done>
    Chapter 0.5 authored as a lab wrapper: 3-sentence frame, generate/validate/visualize steps outlined, prominent lab link, module hook closing paragraph.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learner → lab environment | Learner runs npx commands from LAB.md; commands are read-only (validate) and use official FINOS CLI |
| quiz YAML → MDX component (Phase 5) | Quiz data rendered later; data file must have correct types now to avoid rendering errors later |
| solution file → learner inspection | Solution is a teaching artifact; must not contain real credentials or PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npx @finos/calm-cli (used in lab step-3) | mitigate | Package version 1.44.1 confirmed; lab instructs `npx @finos/calm-cli` (pulls latest compatible) — executor should pin to 1.44.1 in lab instructions if version drift risk materialises |
| T-03-01 | Information disclosure | labs/lab-00-on-ramp/solution/my-system.architecture.json | accept | Solution is fictional "Simple Blog" — no real credentials, no PII, no production architecture data |
| T-03-02 | Tampering | quizzes/module-00-on-ramp.yaml | accept | Static YAML data file; no user input processed at this phase; MDX component (Phase 5) will handle sanitisation |
| T-03-03 | Denial of service | ai_judge step in lab (step-4) | accept | ai_judge is LLM-graded — "use sparingly" per LAB-FORMAT.md; only 1 of 4 steps uses it; budget is acceptable for a module-entry lab |
</threat_model>

<verification>
After all three tasks complete:

1. Chapter count — all 5 Module 0 chapters must now exist:
   `ls /Users/gshah/work/opsflow-sh/calm/calm-academy/content/module-00-on-ramp/*.mdx | wc -l`
   Expected: 5

2. Lab solution validates:
   `npx @finos/calm-cli validate -a /Users/gshah/work/opsflow-sh/calm/calm-academy/labs/lab-00-on-ramp/solution/my-system.architecture.json -f pretty`
   Expected: exit 0, hasErrors: false

3. Quiz linter passes:
   `bash /Users/gshah/work/opsflow-sh/calm/calm-academy/scripts/lint-quizzes.sh`
   Expected: exit 0

4. No invented node types in solution:
   `grep "node-type" /Users/gshah/work/opsflow-sh/calm/calm-academy/labs/lab-00-on-ramp/solution/my-system.architecture.json | grep -vE '"(actor|ecosystem|system|service|database|network|ldap|webclient|data-asset)"'`
   Expected: empty output

5. Lab has requires_docker: false:
   `grep "requires_docker" /Users/gshah/work/opsflow-sh/calm/calm-academy/labs/lab-00-on-ramp/LAB.md`
   Expected: "requires_docker: false"

6. Lab link present in chapter 0.5:
   `grep -c "lab-00-on-ramp/LAB.md" /Users/gshah/work/opsflow-sh/calm/calm-academy/content/module-00-on-ramp/your-first-calm-document.mdx`
   Expected: 1 or more

7. Quiz question IDs conform to pattern:
   `grep "^        - id:" /Users/gshah/work/opsflow-sh/calm/calm-academy/quizzes/module-00-on-ramp.yaml | grep -v "q[0-9]\.[0-9]\.[0-9]"`
   Expected: empty output (all IDs match pattern)

8. Full CALM validation script:
   `bash /Users/gshah/work/opsflow-sh/calm/calm-academy/scripts/validate-calm.sh`
   Expected: exit 0 (validates todo-api + solution file)
</verification>

<success_criteria>
- Chapter 0.5 (your-first-calm-document.mdx): lab wrapper chapter with 3-sentence frame, prominent lab link, hook closing paragraph, validate command shown, 1000–1500 words
- Lab 0 (labs/lab-00-on-ramp/LAB.md): Learnforge-compatible YAML (4 steps, requires_docker: false, objectives, resources) + Markdown walkthrough
- Solution file: validates with calm CLI exit 0; simple-blog 4-node architecture; no invented types
- Quiz YAML: 5 questions across 3 chapters, all IDs correct format, all multiple_choice single-correct, all reference_sections present
- lint-quizzes.sh: exits 0 against the quiz file; upgraded from stub to actual schema checks
- Combined with Plans 01-01 and 01-02: all 5 Module 0 chapters exist, lab complete, quiz complete, 3 illustration stubs ready, code example validated
</success_criteria>

<output>
Create `.planning/phases/01-module-0-on-ramp/01-03-SUMMARY.md` when done.
</output>
