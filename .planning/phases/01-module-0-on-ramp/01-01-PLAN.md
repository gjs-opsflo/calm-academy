---
phase: "01-module-0-on-ramp"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx
  - content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx
  - code-examples/module-00-on-ramp/todo-api.architecture.json
  - scripts/validate-calm.sh
  - scripts/lint-quizzes.sh
autonomous: true
requirements:
  - MOD-01
must_haves:
  truths:
    - "A student can read Chapter 0.1 and understand there are exactly three input paths to a CALM doc"
    - "A student can read Chapter 0.4 and pick one of three setup flavours and be ready to generate CALM"
    - "The todo-api.architecture.json example validates cleanly with calm validate before anything else references it"
    - "Both MDX chapters have all required frontmatter fields and section headers"
  artifacts:
    - path: "content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx"
      provides: "Chapter 0.1 — Three Paths (MOD-01 partial)"
      contains: "TL;DR, Why it matters, The concept, Common mistakes, Knowledge check, Lab, Further reading"
    - path: "content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx"
      provides: "Chapter 0.4 — Setup (MOD-01 partial)"
      contains: "Three setup flavours: Claude Code CLI (check existing), Claude Desktop/Cursor mcpServers block, CALM Studio web (zero-install, recommended for Lab 0)"
    - path: "code-examples/module-00-on-ramp/todo-api.architecture.json"
      provides: "Validated CALM 1.2 architecture example for all Module 0 chapters"
      contains: "actor, webclient, service, database nodes; interacts and connects relationships"
    - path: "scripts/validate-calm.sh"
      provides: "Per-phase CALM JSON validation gate"
      contains: "npx @finos/calm-cli validate"
    - path: "scripts/lint-quizzes.sh"
      provides: "Quiz YAML schema linter (stub — full checks added in Plan C)"
  key_links:
    - from: "content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx"
      to: "code-examples/module-00-on-ramp/todo-api.architecture.json"
      via: "code block reference comment"
      pattern: "todo-api.architecture.json"
    - from: "content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx"
      to: "calmstudio-mcp"
      via: "claude mcp list verification step and FINOS aac repo reference for local install"
      pattern: "claude mcp list"
---

<objective>
This plan delivers the two "entry gate" chapters — the conceptual frame (Chapter 0.1, Three Paths) and the setup guide (Chapter 0.4, Get Set Up). It also creates and validates the todo-api CALM JSON code example that all five chapters reference, and scaffolds the two validation scripts needed to CI-gate content quality.

Purpose: A student who completes this plan can read what CALM is, understand the three AI-assisted paths to producing a CALM document, pick their setup option, and configure calmstudio-mcp — all before encountering the live demo or hands-on lab. These two chapters are the pre-reading gate for the rest of Module 0.

Output: 2 authored MDX chapters, 1 validated CALM 1.2 JSON code example, 2 validation shell scripts.
</objective>

<execution_context>
@/Users/gshah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gshah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/ROADMAP.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/CLAUDE.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/CURRICULUM.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/STYLE-GUIDE.md
@/Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-RESEARCH.md

<interfaces>
<!-- CALM 1.2 required architecture top-level fields (VERIFIED from architecture-as-code repo) -->
<!-- "$schema", "unique-id", "name", "description", "nodes", "relationships" -->

<!-- 9 core CALM node types (ONLY valid types for Module 0 — no others) -->
<!-- actor | ecosystem | system | service | database | network | ldap | webclient | data-asset -->

<!-- 5 CALM relationship types -->
<!-- interacts: { actor: string, nodes: string[] } -->
<!-- connects: { source: { node: string }, destination: { node: string } } -->
<!-- deployed-in: { container: string, nodes: string[] } -->
<!-- composed-of: { container: string, nodes: string[] } -->
<!-- options: array -->

<!-- Valid protocol values only -->
<!-- HTTP, HTTPS, FTP, SFTP, JDBC, WebSocket, SocketIO, LDAP, AMQP, TLS, mTLS, TCP -->

<!-- CALM validate CLI invocation -->
<!-- npx @finos/calm-cli validate -a <file>.architecture.json -->
<!-- npx @finos/calm-cli validate -a <file>.architecture.json -f pretty -->
<!-- Exit 0 = valid. Output JSON has hasErrors: false when valid. -->

<!-- MDX chapter frontmatter (from STYLE-GUIDE.md) -->
<!-- --- -->
<!-- title: <string> -->
<!-- slug: <kebab-case> -->
<!-- module: <int> -->
<!-- chapter: <float N.N> -->
<!-- estimated_minutes: <int> -->
<!-- prerequisites: [] -->
<!-- --- -->

<!-- calmstudio-mcp install reality (RESOLVED in RESEARCH.md Open Questions) -->
<!-- @calmstudio/mcp is NOT on npm. Local .cjs file registered via ~/.claude.json. -->
<!-- Claude Code users: run `claude mcp list` to check existing config. -->
<!-- If missing: refer to https://github.com/finos/architecture-as-code releases for local install. -->
<!-- Zero-install path: CALM Studio web at https://studio.calm.finos.org [verify URL before publication] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and validate todo-api.architecture.json + validation scripts</name>
  <files>
    code-examples/module-00-on-ramp/todo-api.architecture.json
    scripts/validate-calm.sh
    scripts/lint-quizzes.sh
  </files>
  <read_first>
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-RESEARCH.md (sections: "CALM JSON Minimal Architecture", "Package Legitimacy Audit", "The 9 core CALM node types", "CALM validate output format")
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/CLAUDE.md (sections: "Critical reminders" — CALM node type discipline)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/scripts/README.md (existing scripts inventory)
  </read_first>
  <action>
    Create `code-examples/module-00-on-ramp/todo-api.architecture.json` with:
    - `$schema`: "https://calm.finos.org/release/1.2/meta/calm.json"
    - `unique-id`: "todo-api"
    - `name`: "Simple Todo API"
    - `description`: "A minimal web application for managing todo items — a user accesses a web frontend, the frontend calls an API service, the API stores todos in a database."
    - 4 nodes: user (actor), todo-web (webclient), todo-api (service), todo-db (database)
    - 3 relationships: user interacts todo-web; todo-web connects todo-api (protocol: HTTPS); todo-api connects todo-db (protocol: JDBC)
    - All relationship `unique-id` fields must be present. All nodes must have `unique-id`, `node-type`, `name`, `description`.
    - No invented node types. No `container`, `component`, or any type not in the 9 core list.

    Run: `npx @finos/calm-cli validate -a code-examples/module-00-on-ramp/todo-api.architecture.json -f pretty`
    The output must contain "hasErrors: false" or "0 errors". If validation fails, fix the JSON and re-run until it passes. Do NOT proceed to chapter authoring until the code example validates.

    Create `scripts/validate-calm.sh` (executable, bash shebang):
    - Finds all files matching `code-examples/**/*.architecture.json`
    - Runs `npx @finos/calm-cli validate -a` on each
    - Exits 1 if any file fails validation, prints the failing file path
    - Exits 0 with a summary count if all pass

    Create `scripts/lint-quizzes.sh` (executable, bash shebang):
    - For Phase 1 this is a stub that checks `quizzes/*.yaml` files exist and are non-empty YAML
    - Prints "PASS" for each file found; exits 0
    - The full schema checks will be added in Plan C (Plan 01-03) after the quiz YAML exists
    - Must exit 0 with a message if no quiz files exist yet ("No quiz files found — skipping")

    Make both scripts executable: `chmod +x scripts/validate-calm.sh scripts/lint-quizzes.sh`
  </action>
  <verify>
    <automated>npx @finos/calm-cli validate -a /Users/gshah/work/opsflow-sh/calm/calm-academy/code-examples/module-00-on-ramp/todo-api.architecture.json -f pretty</automated>
  </verify>
  <acceptance_criteria>
    - `code-examples/module-00-on-ramp/todo-api.architecture.json` exists and passes `npx @finos/calm-cli validate -a` with exit code 0 and hasErrors: false
    - All node types are from the 9 core CALM types (grep confirms: actor, webclient, service, database only)
    - `scripts/validate-calm.sh` is executable and exits 0 when run against the todo-api example
    - `scripts/lint-quizzes.sh` is executable and exits 0 (stub behaviour when no quiz files exist)
    - No `container`, `component`, or other non-spec types appear anywhere in the JSON
  </acceptance_criteria>
  <done>
    todo-api.architecture.json validates with calm CLI (exit 0, hasErrors: false). Both scripts exist and are executable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author Chapter 0.1 — Three Paths to Your First CALM Document</name>
  <files>
    content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx
  </files>
  <read_first>
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/STYLE-GUIDE.md (full file — tone for M0: enthusiastic, hook-heavy; required section structure)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/CURRICULUM.md (Chapter 0.1 section — Three Paths table; Chapter 0.3 concepts for cross-references)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-RESEARCH.md (section: "Quiz Format Pattern" — q0.1.1 tests understanding of three paths)
    - code-examples/module-00-on-ramp/todo-api.architecture.json (the validated example to reference in the chapter)
  </read_first>
  <action>
    Author `content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx` with:

    Frontmatter:
    - title: "Three Paths to Your First CALM Document"
    - slug: three-paths-to-first-calm-doc
    - module: 0
    - chapter: 0.1
    - estimated_minutes: 5
    - prerequisites: [] (this is the entry chapter)

    Required sections in order (per STYLE-GUIDE.md structure):

    ## TL;DR (3-5 bullets, max 80 words total)
    Bullets must name the three paths: Talk (plain English description), Sketch (diagram or image), Markdown (existing architecture doc). State that all three produce identical output (.calm.json). State that no spec knowledge required for any path.

    ## Why it matters
    Hook: architects already have what they need — either words in their head, a diagram on a whiteboard, or a Confluence doc. The three paths meet them there. Real-world stake: the first CALM doc is usually free — cost is learning the spec to maintain and extend it.

    ## The concept (1500–3000 words)
    Must cover all three paths with their own H3 subheadings:

    ### Path A: Talk — describe in plain English
    Explain how to describe a system in 3 sentences and how calmstudio-mcp (the AI tool) converts that to CALM. Include the todo-api example as the teaching artifact (reference the validated file at `code-examples/module-00-on-ramp/todo-api.architecture.json`). Show the 3-sentence description that produced it: "A user opens a web app to manage their todos. The web app calls a REST API. The API stores todo items in a database." Show the resulting CALM JSON structure (paste the full todo-api.architecture.json content as a ```json code block with a comment header: "// from code-examples/module-00-on-ramp/todo-api.architecture.json"). Explain why the AI used actor for the user, webclient for the web app, service for the API, and database for the storage.

    ### Path B: Sketch — from diagram or image
    Explain the workflow: drop an image (whiteboard photo, Lucidchart export screenshot, PowerPoint slide) into an AI client with calmstudio-mcp configured. The skill's `read_calm_guide` call bakes in spec knowledge before generation. Note the limitation: image quality affects accuracy; a clean diagram produces cleaner CALM than a blurry whiteboard photo.

    ### Path C: Markdown — from an existing architecture doc
    Explain that any structured markdown — an ARB document, an RFC, a Confluence architecture page — can serve as input. The calm-arb-convert skill (available at `~/.claude/skills/calm-arb-convert/`) handles the workflow: fetch doc → extract system description → generate CALM with threat decorators where applicable. Note this path is especially powerful for teams migrating existing architecture documentation.

    ### What all three paths produce
    All three produce the same artifact: a `.calm.json` file containing CALM 1.2 schema nodes and relationships. The quality ceiling is the same; the input friction differs. This section should also briefly explain that the generated draft still benefits from validation (`calm validate`) and human review.

    ### The limits of the AI path
    Briefly explain: the AI enforces the spec (via `read_calm_guide`), but generated architectures still need review. Missing nodes, inaccurate relationship directions, and coarse descriptions all produce imprecise CALM. Module 1+ teaches the spec so learners can review and refine confidently.

    Tone: M0 = enthusiastic, hook-heavy, "watch this work". Active voice. Second person.

    ## Code/CALM examples
    Include the todo-api.architecture.json as a fully-printed ```json code block (no truncation, no ...). Label it with a comment: "// from code-examples/module-00-on-ramp/todo-api.architecture.json". This is the anchor CALM example for the whole module.

    ## Common mistakes (3–5 items)
    - Using invented node types (container, component, microservice) instead of the 9 core types
    - Describing the implementation, not the architecture ("uses React with Redux" → "webclient node")  
    - Skipping the 3-sentence constraint and dumping a 10-paragraph spec at the AI
    - Forgetting to validate with `npx @finos/calm-cli validate -a` before treating the output as final

    ## Knowledge check
    Link to: `../../quizzes/module-00-on-ramp.yaml` with anchor text "Take the Module 0 quiz"

    ## Lab
    Link to: `../../labs/lab-00-on-ramp/LAB.md` with anchor text "Lab 0: Your First CALM Document"

    ## Further reading
    - Link to get-set-up-in-sixty-seconds.mdx (next step: set up the tool)
    - Link to CALM 1.2 spec: https://calm.finos.org/release/1.2/meta/calm.json
    - Link to FINOS CALM repo: https://github.com/finos/architecture-as-code

    Word count target for "The concept" section: 1500–2000 words (Chapter 0.1 is the intro chapter — lean toward the lower bound). Total chapter: aim for 2000–2500 words.
  </action>
  <verify>
    <automated>grep -c "## TL;DR" /Users/gshah/work/opsflow-sh/calm/calm-academy/content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx`
    - Frontmatter has all required fields: title, slug, module, chapter, estimated_minutes, prerequisites
    - All 8 required sections present: TL;DR, Why it matters, The concept, Code/CALM examples, Common mistakes, Knowledge check, Lab, Further reading
    - Three H3 subheadings under The concept: Path A, Path B, Path C
    - todo-api.architecture.json is referenced and its full JSON content is shown (no truncation)
    - No invented node types referenced positively (they appear only in "Common mistakes")
    - Word count of the "The concept" section: 1500–2000 words (run: wc -w on the file and verify total >1500)
    - Links to quiz and lab use relative paths that will resolve correctly from this file's location
  </acceptance_criteria>
  <done>
    Chapter 0.1 authored with all 8 required sections, correct frontmatter, three-path concept explained, todo-api.architecture.json embedded in full, common mistakes listed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author Chapter 0.4 — Get Set Up in 60 Seconds</name>
  <files>
    content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx
  </files>
  <read_first>
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/docs-meta/STYLE-GUIDE.md (section structure, tone)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/CURRICULUM.md (Chapter 0.4 — three flavours, exact CLI commands)
    - /Users/gshah/work/opsflow-sh/calm/calm-academy/.planning/phases/01-module-0-on-ramp/01-RESEARCH.md (section: "Open Questions" — RESOLVED: calmstudio-mcp is NOT on npm, use claude mcp list / local install / CALM Studio web)
  </read_first>
  <action>
    Author `content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx` with:

    Frontmatter:
    - title: "Get Set Up in 60 Seconds"
    - slug: get-set-up-in-sixty-seconds
    - module: 0
    - chapter: 0.4
    - estimated_minutes: 3
    - prerequisites: [three-paths-to-first-calm-doc]

    Note: This chapter intentionally precedes Chapter 0.2 (Demo) and 0.3 (Why) in the reading order because a learner who wants to follow along during the demo needs their environment ready. It is the shorter chapter in the module.

    Required sections:

    ## TL;DR
    - Three options: Claude Code (check if already configured), Claude Desktop/Cursor/Windsurf (JSON config block), or CALM Studio web (zero install, recommended for Lab 0)
    - Claude Code users: run `claude mcp list` — calmstudio-mcp may already be registered
    - Zero-install path (CALM Studio web) is sufficient for Lab 0 and the visualization step
    - Setup takes under 60 seconds for any path

    ## Why it matters
    Explain: Chapter 0.2 is a live demo you can follow along with. Having calmstudio-mcp installed before watching the demo means you can pause the video and try it yourself. Setup first, watch second.

    ## The concept (target: 800–1200 words — shorter chapter, procedural content)

    ### Option 1: Claude Code CLI (check existing config first)
    IMPORTANT: `@calmstudio/mcp` is NOT available on npm. It is distributed as a local install from the FINOS architecture-as-code repo.

    First step: Check if it is already configured:
    ```bash
    claude mcp list
    ```
    If `calmstudio-mcp` appears in the list, you are ready — skip to the verification step below.

    If it is NOT listed: install from the FINOS architecture-as-code repository. The releases page at https://github.com/finos/architecture-as-code/releases contains the calmstudio-mcp package. Follow the install instructions there for your OS. Once the `.cjs` file is downloaded, register it:
    ```bash
    claude mcp add --scope user calmstudio-mcp -- node /path/to/calmstudio-mcp.cjs
    ```
    Replace `/path/to/calmstudio-mcp.cjs` with the actual path of the downloaded file.

    Explain what `--scope user` means (persists across all projects). Note that once registered, calmstudio-mcp is available in all future Claude Code sessions.

    ### Option 2: Claude Desktop, Cursor, or Windsurf (manual config)
    Show the mcpServers JSON block that goes into the relevant config file. For Claude Desktop: `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS) or the OS-equivalent. For Cursor/Windsurf: `.cursor/mcp.json` or `.windsurf/mcp.json` in the project root.

    The block format (replace the path with the actual location of your downloaded calmstudio-mcp.cjs):
    ```json
    {
      "mcpServers": {
        "calmstudio-mcp": {
          "command": "node",
          "args": ["/path/to/calmstudio-mcp.cjs"]
        }
      }
    }
    ```

    Include a cross-reference note: "Full config reference and download links at https://github.com/finos/architecture-as-code/releases."

    ### Option 3: CALM Studio web (zero install — recommended for Lab 0)
    Explain: if the learner doesn't want to install anything, they can use CALM Studio in a browser. Open https://studio.calm.finos.org [verify URL before publication] for the visual canvas.

    Note the limitation: CALM Studio web provides the visual canvas but not the AI-driven generation workflow (calmstudio-mcp). For the AI paths (Path A/B/C from Chapter 0.1), Options 1 or 2 are required. CALM Studio web is sufficient for the visualization step of Lab 0 (Step 4).

    ### Verify your setup (Options 1 and 2)
    After configuring calmstudio-mcp, verify it is active in your AI client. Open your AI client and type:
    "Using calmstudio-mcp, create a simple CALM architecture for a web app with a database."
    If calmstudio-mcp is connected, the AI will make tool calls to `read_calm_guide` and `create_architecture`. If you see no tool calls, restart the AI client and try again.

    ## Common mistakes (3–4 items)
    - Trying to install with `npx @calmstudio/mcp` — the package is not on npm; use the local install from the aac releases page
    - Forgetting `--scope user` in the Claude Code CLI command (without it, the MCP server is per-project, not global)
    - Confusing calmstudio-mcp (architecture generation) with CALM Studio (visualization canvas) — they complement each other
    - Not restarting the AI client after adding the MCP server (most clients require a restart to pick up new MCP config)

    ## Knowledge check
    Link to: `../../quizzes/module-00-on-ramp.yaml` with anchor text "Take the Module 0 quiz"

    ## Lab
    Link to: `../../labs/lab-00-on-ramp/LAB.md` with anchor text "Lab 0: Your First CALM Document"

    ## Further reading
    - Link to three-paths-to-first-calm-doc.mdx (what you can do once set up)
    - Link to your-first-calm-document.mdx (the hands-on chapter)
    - FINOS architecture-as-code repo: https://github.com/finos/architecture-as-code
  </action>
  <verify>
    <automated>grep -c "claude mcp list" /Users/gshah/work/opsflow-sh/calm/calm-academy/content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx`
    - Frontmatter has all required fields including prerequisites: [three-paths-to-first-calm-doc]
    - All 8 required sections present (TL;DR through Further reading)
    - `claude mcp list` verification command present (check before install, not after)
    - No `npx @calmstudio/mcp` install command — the package is not on npm
    - mcpServers JSON block for Claude Desktop/Cursor/Windsurf present using `node /path/to/calmstudio-mcp.cjs` (not npx)
    - CALM Studio web option present as zero-install path with URL annotated [verify URL before publication]
    - Three H3 subheadings (Option 1, Option 2, Option 3)
    - "Common mistakes" includes the npm install mistake (trying npx @calmstudio/mcp)
    - All code blocks syntactically correct (bash and json blocks)
    - No invented node types or CALM concepts introduced (setup chapter stays procedural)
  </acceptance_criteria>
  <done>
    Chapter 0.4 authored covering all three setup options with correct calmstudio-mcp install instructions (check claude mcp list first; local .cjs install, not npx; CALM Studio web as zero-install path).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content authoring → CALM CLI | Generated JSON is passed to npx @finos/calm-cli; malformed JSON produces error output, no security concern |
| published content → learner machine | Learners copy-paste commands from the chapter; commands must be accurate and not introduce harmful patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npx @finos/calm-cli (1.44.1) | mitigate | Package verified on npm registry at research time (VERIFIED); version pinned in scripts. Executor should run `npm view @finos/calm-cli version` before using |
| T-01-02 | Tampering | calmstudio-mcp (local .cjs) | mitigate | Not from npm — local file from FINOS aac releases. Chapter 0.4 instructs learners to download from official FINOS repo only (https://github.com/finos/architecture-as-code/releases) |
| T-01-03 | Information disclosure | code-examples/module-00-on-ramp/todo-api.architecture.json | accept | Tutorial example only — fictional system, no real credentials, no PII; explicitly a teaching artifact |
</threat_model>

<verification>
After both chapters and the code example are authored:

1. Validate CALM example:
   `npx @finos/calm-cli validate -a code-examples/module-00-on-ramp/todo-api.architecture.json -f pretty`
   Expected: exit 0, hasErrors: false

2. Confirm chapter file count:
   `ls content/module-00-on-ramp/*.mdx | wc -l`
   Expected: 2

3. Confirm no invented node types in code example:
   `grep -v "^#" code-examples/module-00-on-ramp/todo-api.architecture.json | grep "node-type" | grep -vE '"(actor|ecosystem|system|service|database|network|ldap|webclient|data-asset)"'`
   Expected: no output (empty = all types valid)

4. Confirm required frontmatter fields in each MDX:
   `grep -c "^slug:" content/module-00-on-ramp/three-paths-to-first-calm-doc.mdx`
   Expected: 1
   `grep -c "^slug:" content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx`
   Expected: 1

5. Confirm Chapter 0.4 does NOT use npx @calmstudio/mcp install command:
   `grep -c "npx @calmstudio/mcp" content/module-00-on-ramp/get-set-up-in-sixty-seconds.mdx`
   Expected: 0

6. Run the validation script:
   `bash scripts/validate-calm.sh`
   Expected: exit 0
</verification>

<success_criteria>
- Chapter 0.1 (three-paths-to-first-calm-doc.mdx): exists, all 8 sections present, todo-api.architecture.json embedded in full, 3 paths explained, word count in 1500-2500 range
- Chapter 0.4 (get-set-up-in-sixty-seconds.mdx): exists, all 8 sections present, 3 setup options with correct instructions (claude mcp list check, local .cjs install, CALM Studio web zero-install), no npx @calmstudio/mcp install command
- todo-api.architecture.json: validates with calm CLI exit 0, uses only core CALM types (actor, webclient, service, database), no invented types
- Both scripts executable and functional
- A student who reads Chapter 0.1 + 0.4 has everything needed to understand what they will do and set up their environment
</success_criteria>

<output>
Create `.planning/phases/01-module-0-on-ramp/01-01-SUMMARY.md` when done.
</output>
